require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $send = Opal.send, $truthy = Opal.truthy, $neqeq = Opal.neqeq, $rb_lt = Opal.rb_lt, $rb_ge = Opal.rb_ge, $rb_plus = Opal.rb_plus, $rb_gt = Opal.rb_gt, $rb_times = Opal.rb_times, $rb_minus = Opal.rb_minus, $thrower = Opal.thrower, $eqeq = Opal.eqeq, $rb_divide = Opal.rb_divide, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'GehennaAn');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "GehennaAn");
        $const_set($nesting[0], 'NAME', "ゲヘナ・アナスタシス");
        $const_set($nesting[0], 'SORT_KEY', "けへなあなすたしす");
        $const_set($nesting[0], 'HELP_MESSAGE', "戦闘判定と通常判定に対応。幸運の助け、連撃増加値(戦闘判定)、闘技チット(戦闘判定)を自動表示します。\n" + "・戦闘判定　(nGAt+m)\n" + "　ダイス数n、目標値t、修正値mで戦闘判定を行います。\n" + "　幸運の助け、連撃増加値、闘技チットを自動処理します。\n" + "・通常判定　(nGt+m)\n" + "　ダイス数n、目標値t、修正値mで通常判定を行います。\n" + "　幸運の助けを自動処理します。(連撃増加値、闘技チットを表示抑制します)\n");
        self.$register_prefix("\\d+G\\d+", "\\d+GA\\d+", "\\d+R6");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          return (self.sort_barabara_dice = true);
        });
        self.$private();
        
        $def(self, '$replace_text', function $$replace_text(string) {
          
          return $send($send($send($send(string, 'gsub', [/(\d+)GA(\d+)([+-][+\-\d]+)/], function $$1(){
            return "" + ($$('Regexp').$last_match(1)) + "R6" + ($$('Regexp').$last_match(3)) + ">=" + ($$('Regexp').$last_match(2)) + "[1]"}), 'gsub', [/(\d+)GA(\d+)/], function $$2(){
            return "" + ($$('Regexp').$last_match(1)) + "R6>=" + ($$('Regexp').$last_match(2)) + "[1]"}), 'gsub', [/(\d+)G(\d+)([+-][+\-\d]+)/], function $$3(){
            return "" + ($$('Regexp').$last_match(1)) + "R6" + ($$('Regexp').$last_match(3)) + ">=" + ($$('Regexp').$last_match(2)) + "[0]"}), 'gsub', [/(\d+)G(\d+)/], function $$4(){
            return "" + ($$('Regexp').$last_match(1)) + "R6>=" + ($$('Regexp').$last_match(2)) + "[0]"})
        });
        self.$public();
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(string) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, diceCount = nil, modText = nil, diff = nil, mode = nil, mod = nil, diceArray = nil, diceValue = nil, diceText = nil, dice_1st = nil, isLuck = nil, output = nil, success = nil, failed = nil;

          
          string = self.$replace_text(string);
          if (!$truthy(/(^|\s)S?((\d+)[rR]6([+\-\d]+)?([>=]+(\d+))(\[(\d)\]))(\s|$)/i['$=~'](string))) {
            return nil
          };
          string = $$('Regexp').$last_match(2);
          diceCount = $$('Regexp').$last_match(3).$to_i();
          modText = $$('Regexp').$last_match(4);
          diff = $$('Regexp').$last_match(6).$to_i();
          mode = $$('Regexp').$last_match(8).$to_i();
          mod = $$('ArithmeticEvaluator').$eval(modText);
          diceArray = self.randomizer.$roll_barabara(diceCount, 6).$sort();
          diceValue = diceArray.$sum();
          diceText = diceArray.$join(",");
          dice_1st = "";
          isLuck = true;
          diceValue = 0;
          $send(diceArray, 'each', [], function $$5(i){
            
            if (i == null) i = nil;
            if ($neqeq(dice_1st, "")) {
              if (($neqeq(dice_1st, i) || ($truthy($rb_lt(i, diff))))) {
                isLuck = false
              }
            } else {
              dice_1st = i
            };
            if ($truthy($rb_ge(i, diff))) {
              return (diceValue = $rb_plus(diceValue, 1))
            } else {
              return nil
            };});
          if (($truthy(isLuck) && ($truthy($rb_gt(diceCount, 1))))) {
            diceValue = $rb_times(diceValue, 2)
          };
          output = "" + (diceValue) + "[" + (diceText) + "]";
          success = $rb_plus(diceValue, mod);
          if ($truthy($rb_lt(success, 0))) {
            success = 0
          };
          failed = $rb_minus(diceCount, diceValue);
          if ($truthy($rb_lt(failed, 0))) {
            failed = 0
          };
          if ($truthy($rb_gt(mod, 0))) {
            output = $rb_plus(output, "+" + (mod))
          } else if ($truthy($rb_lt(mod, 0))) {
            output = $rb_plus(output, mod.$to_s())
          };
          if ($truthy(/[^\d\[\]]+/['$=~'](output))) {
            output = "(" + (string) + ") ＞ " + (output) + " ＞ 成功" + (success) + "、失敗" + (failed)
          } else {
            output = "(" + (string) + ") ＞ " + (output)
          };
          output = $rb_plus(output, self.$getAnastasisBonusText(mode, success));
          return output;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        self.$private();
        
        $def(self, '$getAnastasisBonusText', function $$getAnastasisBonusText(mode, success) {
          var self = this, ma_bonus = nil, bonus_str = nil;

          
          if ($eqeq(mode, 0)) {
            return ""
          };
          ma_bonus = $rb_divide($rb_minus(success, 1), 2).$to_i();
          if ($truthy($rb_gt(ma_bonus, 7))) {
            ma_bonus = 7
          };
          bonus_str = "";
          if ($truthy($rb_gt(ma_bonus, 0))) {
            bonus_str = $rb_plus(bonus_str, "連撃[+" + (ma_bonus) + "]/")
          };
          bonus_str = $rb_plus(bonus_str, "闘技[" + (self.$getTougiBonus(success)) + "]");
          return " ＞ " + (bonus_str);
        });
        return $def(self, '$getTougiBonus', function $$getTougiBonus(success) {
          var self = this, table = nil;

          
          table = [[6, "1"], [13, "2"], [18, "3"], [22, "4"], [99, "5"]];
          return self.$get_table_by_number(success, table);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
