require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $send = Opal.send, $rb_gt = Opal.rb_gt, $rb_times = Opal.rb_times, $rb_ge = Opal.rb_ge, $thrower = Opal.thrower, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'DarkSouls');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "DarkSouls");
        $const_set($nesting[0], 'NAME', "ダークソウルTRPG");
        $const_set($nesting[0], 'SORT_KEY', "たあくそうるTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・行為判定：[n]DS[a±b][@t]　　[]内のコマンドは省略可\n" + "・能動判定：[n]ADS[a±b][@t]　　FP消費を判定\n" + "　n：ダイス数。省略時は「2」\n" + "　a±b：修正値。「1+2-1」のように、複数定可\n" + "　@t：目標値。省略時は達成値を、指定時は判定の正否を表示\n" + "例）DS → 2D6の達成値を表示\n" + "　　1DS → 1D6の達成値を表示\n" + "　　ADS+2-2 → 2D6+2の達成値を表示（能動判定）\n" + "　　DS+2@10 → 2D6+2で目標値10の判定\n");
        self.$register_prefix("(\\d+)?(A)?DS([-+\\d]*)(@\\d+)?");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, m = nil, diceCount = nil, $ret_or_1 = nil, isActive = nil, modify = nil, target = nil, output = nil;

          
          if (!$truthy((m = /(\d+)?(A)?DS([-+\d]*)(@(\d+))?$/i.$match(command.$upcase())))) {
            return nil
          };
          diceCount = ($truthy(($ret_or_1 = m['$[]'](1))) ? ($ret_or_1) : (2)).$to_i();
          isActive = m['$[]'](2)['$nil?']()['$!']();
          modify = self.$getValue(m['$[]'](3));
          target = ($truthy(($ret_or_1 = m['$[]'](5))) ? ($ret_or_1) : (0)).$to_i();
          output = self.$checkRoll(diceCount, isActive, modify, target);
          return output;
        });
        
        $def(self, '$checkRoll', function $$checkRoll(diceCount, isActive, modify, target) {
          var self = this, dice_list = nil, dice = nil, diceText = nil, successValue = nil, modifyText = nil, targetText = nil, diceArray = nil, focusDamage = nil, focusText = nil, result = nil;

          
          dice_list = self.randomizer.$roll_barabara(diceCount, 6);
          dice = dice_list.$sum();
          diceText = dice_list.$join(",");
          successValue = $rb_plus(dice, modify);
          modifyText = self.$getValueText(modify);
          targetText = ($eqeq(target, 0) ? ("") : (">=" + (target)));
          if ($truthy(isActive)) {
            
            diceArray = $send(diceText.$split(/,/), 'map', [], "to_i".$to_proc());
            focusDamage = $send(diceArray, 'count', [], function $$1(i){
              
              if (i == null) i = nil;
              return i['$=='](1);});
            if ($truthy($rb_gt(focusDamage, 0))) {
              
              focusText = $rb_times("■", focusDamage);
              focusText = "（FP" + (focusText) + "消費）";
            };
          };
          result = "(" + (diceCount) + "D6" + (modifyText) + (targetText) + ")";
          result = $rb_plus(result, " ＞ " + (dice) + "(" + (diceText) + ")" + (modifyText));
          result = $rb_plus(result, " ＞ " + (successValue) + (targetText));
          if ($truthy($rb_gt(target, 0))) {
            if ($truthy($rb_ge(successValue, target))) {
              result = $rb_plus(result, " ＞ 【成功】")
            } else {
              result = $rb_plus(result, " ＞ 【失敗】")
            }
          };
          result = $rb_plus(result, focusText.$to_s());
          return result;
        });
        
        $def(self, '$getValue', function $$getValue(text) {try { var $t_eval_return = $thrower('eval_return'); 
          var $ret_or_1 = nil;

          
          text = ($truthy(($ret_or_1 = text)) ? ($ret_or_1) : (""));
          return $$('ArithmeticEvaluator').$eval(text);} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        return $def(self, '$getValueText', function $$getValueText(value) {
          
          
          if ($eqeq(value, 0)) {
            return ""
          };
          if ($truthy($rb_lt(value, 0))) {
            return value.$to_s()
          };
          return "+" + (value);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
