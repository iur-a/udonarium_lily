require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_lt = Opal.rb_lt, $rb_minus = Opal.rb_minus, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $rb_ge = Opal.rb_ge, $rb_times = Opal.rb_times, $rb_le = Opal.rb_le, $def = Opal.def, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'PhantasmAdventure');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "PhantasmAdventure");
        $const_set($nesting[0], 'NAME', "ファンタズム・アドベンチャー");
        $const_set($nesting[0], 'SORT_KEY', "ふあんたすむあとへんちやあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "成功、失敗、決定的成功、決定的失敗の表示とクリティカル・ファンブル値計算の実装。\n");
        return $def(self, '$result_1d20', function $$result_1d20(total, _dice_total, cmp_op, diff) {
          var self = this, skill_mod = nil, fumble = nil, critical = nil, dice_now = nil, fum_num = nil, fum_str = nil, crit_num = nil;

          
          if ($eqeq(diff, "?")) {
            return $$('Result').$nothing()
          };
          if (!$eqeq(cmp_op, "<=")) {
            return nil
          };
          skill_mod = 0;
          if ($truthy($rb_lt(diff, 1))) {
            skill_mod = $rb_minus(diff, 1)
          } else if ($truthy($rb_gt(diff, 20))) {
            skill_mod = $rb_minus(diff, 20)
          };
          fumble = $rb_plus(20, skill_mod);
          if ($truthy($rb_gt(fumble, 20))) {
            fumble = 20
          };
          critical = $rb_plus(1, skill_mod);
          dice_now = self.randomizer.$roll_once(20);
          if (($truthy($rb_ge(total, fumble)) || ($truthy($rb_ge(total, 20))))) {
            
            fum_num = $rb_minus(dice_now, skill_mod);
            if ($truthy($rb_gt(fum_num, 20))) {
              fum_num = 20
            };
            if ($truthy($rb_lt(fum_num, 1))) {
              fum_num = 1
            };
            fum_str = dice_now.$to_s();
            if ($truthy($rb_lt(skill_mod, 0))) {
              fum_str = $rb_plus(fum_str, "+" + ($rb_times(skill_mod, -1)) + "=" + (fum_num))
            } else {
              fum_str = $rb_plus(fum_str, "-" + (skill_mod) + "=" + (fum_num))
            };
            return $$('Result').$fumble("致命的失敗(" + (fum_str) + ")");
          } else if (($truthy($rb_le(total, critical)) || ($truthy($rb_le(total, 1))))) {
            
            crit_num = $rb_plus(dice_now, skill_mod);
            if ($truthy($rb_gt(crit_num, 20))) {
              crit_num = 20
            };
            if ($truthy($rb_lt(crit_num, 1))) {
              crit_num = 1
            };
            if ($truthy($rb_lt(skill_mod, 0))) {
              return $$('Result').$success("成功")
            };
            return $$('Result').$critical("決定的成功(" + (dice_now) + "+" + (skill_mod) + "=" + (crit_num) + ")");
          } else if ($truthy($rb_le(total, diff))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
