require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $rb_times = Opal.rb_times, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $rb_lt = Opal.rb_lt, $rb_le = Opal.rb_le, $send = Opal.send, $eqeq = Opal.eqeq, $rb_minus = Opal.rb_minus, $rb_ge = Opal.rb_ge, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Raisondetre');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Raisondetre");
        $const_set($nesting[0], 'NAME', "叛逆レゾンデートル");
        $const_set($nesting[0], 'SORT_KEY', "はんきやくれそんてとおる");
        $const_set($nesting[0], 'HELP_MESSAGE', "判定：[判定値]RD[技能][@目標値]\n" + "ダメージロール：[ダイス数]DD[装甲]\n" + "\n" + "[]内のコマンドは省略可能。\n" + "「判定値」で判定に使用するダイス数を指定。省略時は「1」。0以下も指定可。\n" + "「技能」で有効なダイス数を指定。省略時は「1」。\n" + "達成値はクリティカルを含めて、「最も高くなる」ように計算します。\n" + "「@目標値」指定で、判定の成否を追加表示します。\n" + "\n" + "ダメージロールは[装甲]指定で、有効なダイス数と0の出目の数を表示します。\n" + "[装甲]省略時は、ダイス結果のみ表示します。（複数の対象への攻撃時用）\n" + "\n" + "【書式例】\n" + "・RD → 1Dで達成値を表示。\n" + "・2RD1@8 → 2D（1個選択）で目標値8の判定。\n" + "・-3RD → 1Dでダイスペナルティ-4の判定。\n" + "・4DD2 → 4Dで装甲2のダメージロール。\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        self.$register_prefix("(-)?(\\d+)?RD", "(-)?(\\d+)?DD");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, diceCount = nil, $ret_or_1 = nil, choiceCount = nil, target = nil, armor = nil;

          
          if ($truthy(command['$=~'](/(-)?(\d+)?RD(\d+)?(@(\d+))?$/i))) {
            
            diceCount = ($truthy(($ret_or_1 = $$('Regexp').$last_match(2))) ? ($ret_or_1) : (1)).$to_i();
            if (!$truthy($$('Regexp').$last_match(1)['$nil?']())) {
              diceCount = $rb_times(diceCount, -1)
            };
            choiceCount = ($truthy(($ret_or_1 = $$('Regexp').$last_match(3))) ? ($ret_or_1) : (1)).$to_i();
            target = ($truthy(($ret_or_1 = $$('Regexp').$last_match(5))) ? ($ret_or_1) : (0)).$to_i();
            return self.$checkRoll(diceCount, choiceCount, target);
          } else if ($truthy(command['$=~'](/(-)?(\d+)?DD([1-9])?([+-]\d+)?$/i))) {
            
            diceCount = ($truthy(($ret_or_1 = $$('Regexp').$last_match(2))) ? ($ret_or_1) : (1)).$to_i();
            if (!$truthy($$('Regexp').$last_match(1)['$nil?']())) {
              diceCount = $rb_times(diceCount, -1)
            };
            armor = ($truthy(($ret_or_1 = $$('Regexp').$last_match(3))) ? ($ret_or_1) : (0)).$to_i();
            if ($truthy($rb_gt(armor, 0))) {
              
              armor = $rb_plus(armor, ($truthy(($ret_or_1 = $$('Regexp').$last_match(4))) ? ($ret_or_1) : (0)).$to_i());
              if ($truthy($rb_lt(armor, 1))) {
                armor = 1
              };
              if ($truthy($rb_gt(armor, 9))) {
                armor = 9
              };
            };
            return self.$checkDamage(diceCount, armor);
          };
          return nil;
        });
        
        $def(self, '$checkRoll', function $$checkRoll(diceCount, choiceCount, target) {
          var self = this, correction = nil, rollCount = nil, diceArray = nil, diceText = nil, diceText2 = nil, funbleArray = nil, isFunble = nil, dice = nil, success = nil, criticalCount = nil, critical = nil, choiceArray = nil, choiceText = nil, result = nil;

          
          if ($truthy($rb_le(diceCount, 0))) {
            
            correction = $rb_plus(1, $rb_times(diceCount, -1));
            rollCount = 1;
          } else {
            
            correction = 0;
            rollCount = diceCount;
          };
          diceArray = self.randomizer.$roll_barabara(rollCount, 10).$sort();
          diceText = diceArray.$join(",");
          $send(diceArray, 'map!', [], function $$1(x){
            
            if (x == null) x = nil;
            if ($eqeq(x, 10)) {
              return 0
            } else {
              return x
            };});
          $send(diceArray, 'map!', [], function $$2(i){
            
            if (i == null) i = nil;
            return $rb_minus(i, correction);});
          diceText2 = diceArray.$sort().$join(",");
          funbleArray = $send(diceArray, 'select', [], function $$3(i){
            
            if (i == null) i = nil;
            return $rb_le(i, 1);});
          isFunble = $rb_ge(funbleArray.$size(), rollCount);
          dice = 0;
          success = 0;
          if (!$truthy(isFunble)) {
            
            criticalCount = diceArray.$count(0);
            critical = $rb_times(criticalCount, 10);
            choiceArray = diceArray.$reverse();
            choiceArray.$delete(0);
            choiceArray = choiceArray.$slice(Opal.Range.$new(0, $rb_minus(choiceCount, 1), false));
            choiceText = choiceArray.$join(",");
            dice = choiceArray.$inject("+");
            success = $rb_plus(dice, critical);
          };
          result = "" + (rollCount) + "D10";
          if ($truthy($rb_gt(correction, 0))) {
            result = $rb_plus(result, "-" + (correction))
          };
          result = $rb_plus(result, " ＞ [" + (diceText) + "] ＞ [" + (diceText2) + "] ＞ ");
          if ($truthy(isFunble)) {
            result = $rb_plus(result, "達成値：0 (Funble)")
          } else {
            
            result = $rb_plus(result, "" + (dice) + "[" + (choiceText) + "]");
            if ($truthy($rb_gt(critical, 0))) {
              result = $rb_plus(result, "+" + (critical))
            };
            result = $rb_plus(result, "=達成値：" + (success));
            if ($truthy($rb_gt(critical, 0))) {
              result = $rb_plus(result, " (" + (criticalCount) + "Critical)")
            };
          };
          if ($truthy($rb_gt(target, 0))) {
            
            result = $rb_plus(result, ">=" + (target) + " ");
            if ($truthy($rb_ge(success, target))) {
              result = $rb_plus(result, "【成功】")
            };
            if ($truthy($rb_lt(success, target))) {
              result = $rb_plus(result, "【失敗】")
            };
          };
          return result;
        });
        return $def(self, '$checkDamage', function $$checkDamage(diceCount, armor) {
          var self = this, correction = nil, rollCount = nil, dice_list = nil, diceText = nil, diceArray = nil, criticalCount = nil, diceText2 = nil, result = nil, resultArray = nil, success = nil, resultText = nil;

          
          if ($truthy($rb_le(diceCount, 0))) {
            
            correction = $rb_plus(1, $rb_times(diceCount, -1));
            rollCount = 1;
          } else {
            
            correction = 0;
            rollCount = diceCount;
          };
          dice_list = self.randomizer.$roll_barabara(rollCount, 10).$sort();
          diceText = dice_list.$join(",");
          diceArray = $send(dice_list, 'map', [], function $$4(x){
            
            if (x == null) x = nil;
            if ($eqeq(x, 10)) {
              return 0
            } else {
              return x
            };}).$sort();
          criticalCount = diceArray.$count(0);
          $send(diceArray, 'map!', [], function $$5(i){
            
            if (i == null) i = nil;
            return $rb_minus(i, correction);});
          diceText2 = diceArray.$join(",");
          result = "" + (rollCount) + "D10";
          if ($truthy($rb_gt(correction, 0))) {
            result = $rb_plus(result, "-" + (correction))
          };
          result = $rb_plus(result, " ＞ [" + (diceText) + "] ＞ [" + (diceText2) + "]");
          if ($truthy($rb_gt(armor, 0))) {
            
            resultArray = [];
            success = 0;
            $send(diceArray, 'each', [], function $$6(i){
              
              if (i == null) i = nil;
              if ($truthy($rb_ge(i, armor))) {
                
                resultArray.$push(i);
                return (success = $rb_plus(success, 1));
              } else {
                return resultArray.$push("×")
              };});
            resultText = resultArray.$join(",");
            result = $rb_plus(result, " ＞ [" + (resultText) + "]>=" + (armor) + " 有効数：" + (success));
          };
          result = $rb_plus(result, "　0=" + (criticalCount) + "個");
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
