require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $send = Opal.send, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Illusio');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Illusio");
        $const_set($nesting[0], 'NAME', "晃天のイルージオ");
        $const_set($nesting[0], 'SORT_KEY', "こうてんのいるうしお");
        $const_set($nesting[0], 'HELP_MESSAGE', "判定：[n]IL(BNo)[P]\n" + "\n" + "[]内のコマンドは省略可能。\n" + "「n」でダイス数を指定。省略時は「1」。\n" + "(BNo)でブロックナンバーを指定。「236」のように記述。順不同可。\n" + "コマンド末に「P」を指定で、(BNo)のパリィ判定。（一応、複数指定可）\n" + "\n" + "【書式例】\n" + "・6IL236 → 6dでブロックナンバー「2,3,6」の判定。\n" + "・IL4512 → 1dでブロックナンバー「1,2,4,5」の判定。\n" + "・2IL1P → 2dでパリィナンバー「1」の判定。\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        self.$register_prefix("(\\d+)?IL([1-6]{0,6})(P)?");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, m = nil, dice_count = nil, $ret_or_1 = nil, block_no = nil, is_parry = nil;

          
          m = command.$match(/(\d+)?IL([1-6]{0,6})(P)?$/i);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = ($truthy(($ret_or_1 = m['$[]'](1))) ? ($ret_or_1) : (1)).$to_i();
          block_no = $send(($truthy(($ret_or_1 = m['$[]'](2))) ? ($ret_or_1) : ("")).$each_char(), 'map', [], "to_i".$to_proc()).$uniq().$sort();
          is_parry = m['$[]'](3)['$nil?']()['$!']();
          return self.$check_roll(dice_count, block_no, is_parry);
        });
        return $def(self, '$check_roll', function $$check_roll(dice_count, block_no, is_parry) {
          var self = this, dice_array = nil, dice_text = nil, result_array = nil, success = nil, block_text = nil, block_text2 = nil, result_text = nil, result = nil;

          
          dice_array = self.randomizer.$roll_barabara(dice_count, 6).$sort();
          dice_text = dice_array.$join(",");
          result_array = [];
          success = 0;
          $send(dice_array, 'each', [], function $$1(i){
            
            if (i == null) i = nil;
            if ($truthy($rb_gt(block_no.$count(i), 0))) {
              return result_array.$push("×")
            } else {
              
              result_array.$push(i);
              return (success = $rb_plus(success, 1));
            };});
          block_text = block_no.$join(",");
          block_text2 = ($truthy(is_parry) ? ("Parry") : ("Block"));
          result_text = result_array.$join(",");
          result = "" + (dice_count) + "D6(" + (block_text2) + ":" + (block_text) + ") ＞ " + (dice_text) + " ＞ " + (result_text) + " ＞ ";
          if (!$truthy(is_parry)) {
            return "" + (result) + "成功数：" + (success)
          };
          if ($truthy($rb_lt(success, dice_count))) {
            return "" + (result) + "パリィ成立！　次の非ダメージ2倍。"
          } else {
            return "" + (result) + "成功数：" + (success) + "　パリィ失敗"
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
