require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $hash2 = Opal.hash2, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Liminal');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Liminal");
        $const_set($nesting[0], 'NAME', "リミナル");
        $const_set($nesting[0], 'SORT_KEY', "りみなる");
        $const_set($nesting[0], 'HELP_MESSAGE', "■技能判定　LMx+b>=t+m   x:技能レベル b:ボーナス t:難易度 m:敵の技能レベル(対抗判定)\n" + "\n" + "例)LM2>=8:  技能レベル2,難易度8で技能判定し、その結果を表示。(クリティカル成功も表示)\n" + "   LM3+2>=9:技能レベル3,ボーナス+2,難易度9で技能判定し、その結果を表示。( 〃 )\n" + "   LM0>=8:  技能なし,難易度8で技能判定する。(難易度+2は自動的に足されます)\n" + "\n" + "■イニシアティヴ判定　LIx+b>=t+m   x:認識力レベル b:ボーナス t:難易度 m:敵の認識力レベル\n" + "例)LI2>=8+2:  認識力レベル2,難易度8,敵認識力レベル2で技能判定し、その結果を表示。\n" + "   LI0>=8+2:  認識力なし,難易度8,敵認識力レベル2で技能判定する。(難易度加算なし)\n");
        self.$register_prefix("LI", "LM");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = self.$resolute_action(command)))) {
            return $ret_or_1
          } else {
            return self.$resolute_initiative(command)
          }
        });
        self.$private();
        
        $def(self, '$resolute_action', function $$resolute_action(command) {
          var self = this, parser = nil, parsed = nil, skill_level = nil, bonus = nil, difficulty = nil, dice = nil, dice_total = nil, total = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("LM", $hash2(["round_type"], {"round_type": self.round_type})).$has_suffix_number().$restrict_cmp_op_to(">=");
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          skill_level = parsed.$suffix_number();
          bonus = parsed.$modify_number();
          difficulty = parsed.$target_number();
          dice = self.randomizer.$roll_barabara(2, 6);
          dice_total = dice.$sum();
          total = $rb_plus($rb_plus(dice_total, skill_level), bonus);
          if ($eqeq(skill_level, 0)) {
            difficulty = $rb_plus(difficulty, 2)
          };
          return $send($$('Result').$new(), 'tap', [], function $$1(result){var $a, self = $$1.$$s == null ? this : $$1.$$s, sequence = nil;

            
            if (result == null) result = nil;
            result['$condition=']($rb_ge(total, difficulty));
            result['$critical=']($rb_ge(total, $rb_plus(difficulty, 5)));
            if ($eqeq(dice_total, 2)) {
              
              result['$fumble='](true);
              result['$critical='](false);
              result['$condition='](false);
            };
            sequence = ["(LM" + (skill_level) + (self.$with_symbol(bonus)) + ">=" + (difficulty) + ")", "" + (dice_total) + "[" + (dice.$join(",")) + "]" + (self.$with_symbol($rb_plus(skill_level, bonus))), total.$to_s(), ($truthy(result['$fumble?']()) ? ("1ゾロ") : ($truthy(result['$critical?']()) ? ("クリティカル") : ($truthy(result['$success?']()) ? ("成功") : ("失敗"))))].$compact();
            return ($a = [sequence.$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);}, {$$s: self});
        });
        
        $def(self, '$with_symbol', function $$with_symbol(number) {
          
          if ($eqeq(number, 0)) {
            return "+0"
          } else if ($truthy($rb_gt(number, 0))) {
            return "+" + (number)
          } else {
            return number.$to_s()
          }
        });
        return $def(self, '$resolute_initiative', function $$resolute_initiative(command) {
          var self = this, parser = nil, parsed = nil, skill_level = nil, bonus = nil, difficulty = nil, dice = nil, dice_total = nil, total = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("LI", $hash2(["round_type"], {"round_type": self.round_type})).$has_suffix_number().$restrict_cmp_op_to(">=");
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          skill_level = parsed.$suffix_number();
          bonus = parsed.$modify_number();
          difficulty = parsed.$target_number();
          dice = self.randomizer.$roll_barabara(2, 6);
          dice_total = dice.$sum();
          total = $rb_plus($rb_plus(dice_total, skill_level), bonus);
          return $send($$('Result').$new(), 'tap', [], function $$2(result){var $a, self = $$2.$$s == null ? this : $$2.$$s, sequence = nil;

            
            if (result == null) result = nil;
            result['$condition=']($rb_ge(total, difficulty));
            result['$critical=']($rb_ge(total, $rb_plus(difficulty, 5)));
            if ($eqeq(dice_total, 2)) {
              
              result['$fumble='](true);
              result['$critical='](false);
              result['$condition='](false);
            };
            sequence = ["(LI" + (skill_level) + (self.$with_symbol(bonus)) + ">=" + (difficulty) + ")", "" + (dice_total) + "[" + (dice.$join(",")) + "]" + (self.$with_symbol($rb_plus(skill_level, bonus))), total.$to_s(), ($truthy(result['$fumble?']()) ? ("1ゾロ") : ($truthy(result['$critical?']()) ? ("クリティカル") : ($truthy(result['$success?']()) ? ("成功") : ("失敗"))))].$compact();
            return ($a = [sequence.$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);}, {$$s: self});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
