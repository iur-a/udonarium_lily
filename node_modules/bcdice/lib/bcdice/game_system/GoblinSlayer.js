require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $eqeqeq = Opal.eqeqeq, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $to_ary = Opal.to_ary, $rb_plus = Opal.rb_plus, $rb_le = Opal.rb_le, $rb_ge = Opal.rb_ge, $not = Opal.not, $rb_lt = Opal.rb_lt, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'GoblinSlayer');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "GoblinSlayer");
        $const_set($nesting[0], 'NAME', "ゴブリンスレイヤーTRPG");
        $const_set($nesting[0], 'SORT_KEY', "こふりんすれいやあTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定　GS(x)@c#f>=y\n" + "　2d6の判定を行い、達成値を出力します。\n" + "　xは基準値、yは目標値、cは大成功の下限、fは大失敗の上限です。いずれも省略可能です。\n" + "　cが未指定の場合には c=12 、fが未指定の場合には f=2 となります。\n" + "　yが設定されている場合、大成功/成功/失敗/大失敗を自動判定します。\n" + "　例）GS>=12　GS>10　GS(10)>14　GS+10>=15　GS10>=15　GS(10)　GS+10　GS10　GS\n" + "　　　GS@10　GS@10#3　GS#3@10\n" + "\n" + "・祈念　MCPI(n)$m\n" + "　祈念を行います。\n" + "　nは【幸運】などによるボーナスです。この値は省略可能です。\n" + "　mは因果点の現在値です。\n" + "　因果点の現在値を使用して祈念を行い、成功/失敗を自動判定します。\n" + "　例）MCPI$3　MCPI(1)$4　MCPI+2$5　MCPI2$6\n" + "\n" + "・命中判定の効力値によるボーナス　DB(n)\n" + "　ダメージ効力表による威力へのボーナスを自動で求めます。\n" + "　nは命中判定の効力値です。\n" + "　例）DB(15)　DB12\n" + "\n" + "※上記コマンドの計算内で割り算を行った場合、小数点以下は切り上げされます。\n" + "　ただしダイス出目を割り算した場合、小数点以下は切り捨てされます。\n" + "　例）入力：GS(8+3/2)　実行結果：(GS10) ＞ 10 + 3[1,2] ＞ 13\n" + "　　　入力：2d6/2    　実行結果：(2D6/2) ＞ 3[1,2]/2 ＞ 1\n" + "\n" + "※MCPIでは、シークレットダイスを使用できません。\n");
        self.$register_prefix("GS", "^MCPI.*$", "DB");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.round_type = $$$($$('RoundType'), 'CEIL'));
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($eqeqeq(/^GS/i, ($ret_or_1 = command))) {
            return self.$getCheckResult(command)
          } else if ($eqeqeq(/^MCPI/i, $ret_or_1)) {
            return self.$murmurChantPrayInvoke(command)
          } else if ($eqeqeq(/^DB/i, $ret_or_1)) {
            return self.$damageBonus(command)
          } else {
            return nil
          }
        });
        
        $def(self, '$getCheckResult', function $$getCheckResult(command) {
          var $a, $b, self = this, m = nil, basis = nil, target = nil, cmp_op = nil, without_compare = nil, option1 = nil, option1_value = nil, option2 = nil, option2_param = nil, threshold_critical = nil, threshold_fumble = nil, dice_list = nil, total = nil, diceText = nil, achievement = nil, fumble = nil, critical = nil, result = nil, basis_str = nil;

          
          m = /^GS([-+]?\d+)?(?:(?:([@#])([-+]?\d+))(?:([@#])([-+]?\d+))?)?(?:(>=?)(\d+))?$/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          basis = m['$[]'](1).$to_i();
          target = m['$[]'](7).$to_i();
          cmp_op = m['$[]'](6);
          without_compare = cmp_op['$nil?']();
          option1 = m['$[]'](2);
          option1_value = m['$[]'](3);
          option2 = m['$[]'](4);
          option2_param = m['$[]'](5);
          if (($truthy(option1) && ($eqeq(option1, option2)))) {
            return nil
          };
          $b = self.$calc_threshold(option1, option1_value, option2, option2_param), $a = $to_ary($b), (threshold_critical = ($a[0] == null ? nil : $a[0])), (threshold_fumble = ($a[1] == null ? nil : $a[1])), $b;
          dice_list = self.randomizer.$roll_barabara(2, 6);
          total = dice_list.$sum();
          diceText = dice_list.$join(",");
          achievement = $rb_plus(basis, total);
          fumble = $rb_le(total, threshold_fumble);
          critical = $rb_ge(total, threshold_critical);
          result = " ＞ " + (self.$resultStr(achievement, target, cmp_op, fumble, critical));
          if ((($truthy(without_compare) && ($not(fumble))) && ($not(critical)))) {
            result = ""
          };
          basis_str = ($eqeq(basis, 0) ? ("") : ("" + (basis) + " + "));
          return "(" + (command) + ") ＞ " + (basis_str) + (total) + "[" + (diceText) + "] ＞ " + (achievement) + (result);
        });
        $const_set($nesting[0], 'CRITICAL', 12);
        $const_set($nesting[0], 'FUMBLE', 2);
        
        $def(self, '$calc_threshold', function $$calc_threshold(option1, option1_value, _option2, option2_value) {
          var $a, $b, critical = nil, fumble = nil, $ret_or_1 = nil;

          
          $b = ($eqeq(option1, "@") ? ([option1_value, option2_value]) : ([option2_value, option1_value])), $a = $to_ary($b), (critical = ($a[0] == null ? nil : $a[0])), (fumble = ($a[1] == null ? nil : $a[1])), $b;
          return [($truthy(($ret_or_1 = ($a = critical, ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : ($$('CRITICAL'))), ($truthy(($ret_or_1 = ($b = fumble, ($b === nil || $b == null) ? nil : $b.$to_i()))) ? ($ret_or_1) : ($$('FUMBLE')))];
        });
        
        $def(self, '$murmurChantPrayInvoke', function $$murmurChantPrayInvoke(command) {
          var self = this, m = nil, luck = nil, volition = nil, dice_list = nil, total = nil, diceText = nil, achievement = nil, result = nil, luck_str = nil;

          
          m = /^MCPI(\+?\d+)?\$(\d+)$/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          luck = m['$[]'](1).$to_i();
          volition = m['$[]'](2).$to_i();
          if ($truthy($rb_ge(volition, 12))) {
            return "因果点が12点以上の場合、因果点は使用できません。"
          };
          dice_list = self.randomizer.$roll_barabara(2, 6);
          total = dice_list.$sum();
          diceText = dice_list.$join(",");
          achievement = $rb_plus(total, luck);
          result = " ＞ " + (self.$resultStr(achievement, volition, ">=", false, false));
          luck_str = ($eqeq(luck, 0) ? ("") : ("+" + (luck)));
          return "祈念(2d6" + (luck_str) + ") ＞ " + (total) + "[" + (diceText) + "]" + (luck_str) + " ＞ " + (achievement) + (result) + ", 因果点：" + (volition) + "点 → " + ($rb_plus(volition, 1)) + "点";
        });
        
        $def(self, '$damageBonus', function $$damageBonus(command) {
          var self = this, m = nil, num = nil, fmt = nil, times = nil, dice_list = nil, total = nil, diceText = nil;

          
          m = /^DB(\d+)$/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num = m['$[]'](1).$to_i();
          fmt = "命中判定の効力値によるボーナス ＞ ";
          if ($truthy($rb_lt(num, 15))) {
            return $rb_plus(fmt, "なし")
          };
          times = ($truthy($rb_ge(num, 40)) ? (5) : ($truthy($rb_ge(num, 30)) ? (4) : ($truthy($rb_ge(num, 25)) ? (3) : ($truthy($rb_ge(num, 20)) ? (2) : (1)))));
          dice_list = self.randomizer.$roll_barabara(times, 6);
          total = dice_list.$sum();
          diceText = dice_list.$join(",");
          return $rb_plus(fmt, "" + (total) + "[" + (diceText) + "] ＞ " + (total));
        });
        return $def(self, '$resultStr', function $$resultStr(achievement, target, cmp_op, fumble, critical) {
          
          
          if ($truthy(fumble)) {
            return "大失敗"
          };
          if ($truthy(critical)) {
            return "大成功"
          };
          if ($eqeq(cmp_op, ">=")) {
            return ($truthy($rb_ge(achievement, target)) ? ("成功") : ("失敗"))
          } else {
            return ($truthy($rb_gt(achievement, target)) ? ("成功") : ("失敗"))
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
