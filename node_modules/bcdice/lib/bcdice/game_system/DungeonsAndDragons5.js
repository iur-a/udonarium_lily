require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $neqeq = Opal.neqeq, $rb_ge = Opal.rb_ge, $send = Opal.send, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'DungeonsAndDragons5');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "DungeonsAndDragons5");
        $const_set($nesting[0], 'NAME', "ダンジョンズ＆ドラゴンズ第5版");
        $const_set($nesting[0], 'SORT_KEY', "たんしよんすあんととらこんす5");
        $const_set($nesting[0], 'HELP_MESSAGE', "・攻撃ロール　AT[x][@c][>=t][y]\n" + "　x：+-修正。省略可。\n" + "　c:クリティカル値。省略可。\n" + "　t:敵のアーマークラス。>=を含めて省略可。\n" + "　y:有利(A), 不利(D)。省略可。\n" + "　ファンブル／失敗／成功／クリティカル を自動判定。\n" + "　例）AT AT>=10 AT+5>=18 AT-3>=16 ATA AT>=10A AT+3>=18A AT-3>=16 ATD AT>=10D AT+5>=18D AT-5>=16D\n" + "　    AT@19 AT+5@18 AT-2@19>=15\n" + "・能力値判定　AR[x][>=t][y]\n" + "　攻撃ロールと同様。失敗／成功を自動判定。\n" + "　例）AR AR>=10 AR+5>=18 AR-3>=16 ARA AR>=10A AR+3>=18A AR-3>=16 ARD AR>=10D AR+5>=18D AR-5>=16D\n" + "・両手持ちのダメージ　2HnDx[m]\n" + "　n:ダイスの個数\n" + "　x:ダイスの面数\n" + "　m:+-修正。省略可。\n" + "　パラディンとファイターの武器の両手持ちによるダメージダイスの1,2の出目の振り直しを行います。\n" + "　例)2H3D6 2H1D10+3 2H2D8-1\n");
        self.$register_prefix("AT([+-]\\d+)?(@\\d+)?(>=\\d+)?[AD]?", "AR([+-]\\d+)?(>=\\d+)?[AD]?", "2H(\\d+)D(\\d+)([+-]\\d+)?");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = false);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$attack_roll(command))) ? ($ret_or_2) : (self.$ability_roll(command)))))) {
            return $ret_or_1
          } else {
            return self.$twohands_damage_roll(command)
          }
        });
        
        $def(self, '$number_with_sign_from_int', function $$number_with_sign_from_int(number) {
          
          if ($eqeq(number, 0)) {
            return ""
          } else if ($truthy($rb_gt(number, 0))) {
            return "+" + (number)
          } else {
            return number.$to_s()
          }
        });
        
        $def(self, '$attack_roll', function $$attack_roll(command) {
          var self = this, m = nil, modify = nil, critical_no = nil, difficulty = nil, advantage = nil, usedie = nil, roll_die = nil, dice_command = nil, output = nil, dice = nil, result = nil;

          
          m = /^AT([-+]\d+)?(@(\d+))?(>=(\d+))?([AD]?)/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = m['$[]'](1).$to_i();
          critical_no = m['$[]'](3).$to_i();
          difficulty = m['$[]'](5).$to_i();
          advantage = m['$[]'](6);
          usedie = 0;
          roll_die = "";
          dice_command = "AT" + (self.$number_with_sign_from_int(modify));
          if ($truthy($rb_gt(critical_no, 0))) {
            dice_command = $rb_plus(dice_command, "@" + (critical_no))
          } else {
            critical_no = 20
          };
          if ($truthy($rb_gt(difficulty, 0))) {
            dice_command = $rb_plus(dice_command, ">=" + (difficulty))
          };
          if (!$truthy(advantage['$empty?']())) {
            dice_command = $rb_plus(dice_command, advantage.$to_s())
          };
          output = ["(" + (dice_command) + ")"];
          if ($truthy(advantage['$empty?']())) {
            
            usedie = self.randomizer.$roll_once(20);
            roll_die = usedie;
          } else {
            
            dice = self.randomizer.$roll_barabara(2, 20);
            roll_die = $rb_plus($rb_plus("[", dice.$join(",")), "]");
            if ($eqeq(advantage, "A")) {
              usedie = dice.$max()
            } else {
              usedie = dice.$min()
            };
          };
          if ($neqeq(modify, 0)) {
            
            output.$push("" + (roll_die) + (self.$number_with_sign_from_int(modify)));
            output.$push($rb_plus(usedie, modify).$to_s());
          } else {
            
            if (!$truthy(advantage['$empty?']())) {
              output.$push(roll_die)
            };
            output.$push(usedie.$to_s());
          };
          result = $$('Result').$new();
          if ($truthy($rb_ge(usedie, critical_no))) {
            
            result['$critical='](true);
            result['$success='](true);
            output.$push("クリティカル");
          } else if ($eqeq(usedie, 1)) {
            
            result['$fumble='](true);
            output.$push("ファンブル");
          } else if ($truthy($rb_gt(difficulty, 0))) {
            if ($truthy($rb_ge($rb_plus(usedie, modify), difficulty))) {
              
              result['$success='](true);
              output.$push("成功");
            } else {
              output.$push("失敗")
            }
          };
          return $send($$('Result').$new(), 'tap', [], function $$1(r){var $a;

            
            if (r == null) r = nil;
            r['$text='](output.$join(" ＞ "));
            if ($truthy(result)) {
              
              if ((($truthy($rb_gt(difficulty, 0)) || ($truthy(result['$critical?']()))) || ($truthy(result['$fumble?']())))) {
                r['$condition='](result['$success?']())
              };
              r['$critical='](result['$critical?']());
              return ($a = [result['$fumble?']()], $send(r, 'fumble=', $a), $a[$a.length - 1]);
            } else {
              return nil
            };});
        });
        
        $def(self, '$ability_roll', function $$ability_roll(command) {
          var self = this, m = nil, modify = nil, difficulty = nil, advantage = nil, usedie = nil, roll_die = nil, dice_command = nil, output = nil, dice = nil, result = nil;

          
          m = /^AR([-+]\d+)?(>=(\d+))?([AD]?)/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = m['$[]'](1).$to_i();
          difficulty = m['$[]'](3).$to_i();
          advantage = m['$[]'](4);
          usedie = 0;
          roll_die = "";
          dice_command = "AR" + (self.$number_with_sign_from_int(modify));
          if ($truthy($rb_gt(difficulty, 0))) {
            dice_command = $rb_plus(dice_command, ">=" + (difficulty))
          };
          if (!$truthy(advantage['$empty?']())) {
            dice_command = $rb_plus(dice_command, advantage.$to_s())
          };
          output = ["(" + (dice_command) + ")"];
          if ($truthy(advantage['$empty?']())) {
            
            usedie = self.randomizer.$roll_once(20);
            roll_die = usedie;
          } else {
            
            dice = self.randomizer.$roll_barabara(2, 20);
            roll_die = $rb_plus($rb_plus("[", dice.$join(",")), "]");
            if ($eqeq(advantage, "A")) {
              usedie = dice.$max()
            } else {
              usedie = dice.$min()
            };
          };
          if ($neqeq(modify, 0)) {
            
            output.$push("" + (roll_die) + (self.$number_with_sign_from_int(modify)));
            output.$push($rb_plus(usedie, modify).$to_s());
          } else {
            
            if (!$truthy(advantage['$empty?']())) {
              output.$push(roll_die)
            };
            output.$push(usedie.$to_s());
          };
          result = $$('Result').$new();
          if ($truthy($rb_gt(difficulty, 0))) {
            if ($truthy($rb_ge($rb_plus(usedie, modify), difficulty))) {
              
              result['$success='](true);
              output.$push("成功");
            } else {
              output.$push("失敗")
            }
          };
          return $send($$('Result').$new(), 'tap', [], function $$2(r){var $a;

            
            if (r == null) r = nil;
            r['$text='](output.$join(" ＞ "));
            if ($truthy(result)) {
              
              if ($truthy($rb_gt(difficulty, 0))) {
                r['$condition='](result['$success?']())
              };
              r['$critical='](result['$critical?']());
              return ($a = [result['$fumble?']()], $send(r, 'fumble=', $a), $a[$a.length - 1]);
            } else {
              return nil
            };});
        });
        return $def(self, '$twohands_damage_roll', function $$twohands_damage_roll(command) {
          var self = this, m = nil, dice_count = nil, dice_number = nil, modify = nil, mod_str = nil, output = nil, dice = nil, roll_dice = nil, ex_dice = nil, new_dice = nil, sum_dice = nil;

          
          m = /^2H(\d+)D(\d+)([+-]\d+)?/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = m['$[]'](1).$to_i();
          dice_number = m['$[]'](2).$to_i();
          modify = m['$[]'](3).$to_i();
          mod_str = self.$number_with_sign_from_int(modify);
          output = ["(2H" + (dice_count) + "D" + (dice_number) + (mod_str) + ")"];
          dice = self.randomizer.$roll_barabara(dice_count, dice_number);
          roll_dice = $rb_plus($rb_plus("[", dice.$join(",")), "]");
          output.$push("" + (roll_dice) + (mod_str));
          ex_dice = [];
          new_dice = [];
          sum_dice = 0;
          $send(dice, 'each', [], function $$3(num){var self = $$3.$$s == null ? this : $$3.$$s, one_die = nil;
            if (self.randomizer == null) self.randomizer = nil;

            
            if (num == null) num = nil;
            if ($truthy($rb_gt(num.$to_i(), 2))) {
              
              sum_dice = $rb_plus(sum_dice, num.$to_i());
              return ex_dice.$push(num);
            } else {
              
              one_die = self.randomizer.$roll_once(dice_number);
              sum_dice = $rb_plus(sum_dice, one_die.$to_i());
              return new_dice.$push(one_die);
            };}, {$$s: self});
          if (!$truthy(new_dice['$empty?']())) {
            output.$push($rb_plus($rb_plus($rb_plus($rb_plus("[", ex_dice.$join(",")), "]["), new_dice.$join(",")), "]" + (mod_str)))
          };
          output.$push($rb_plus(sum_dice, modify).$to_s());
          return $send($$('Result').$new(), 'tap', [], function $$4(r){var $a;

            
            if (r == null) r = nil;
            return ($a = [output.$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
