require('./../opal');

Opal.modules["bcdice/game_system/Magius"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_ge = Opal.rb_ge, $rb_plus = Opal.rb_plus, $thrower = Opal.thrower, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Magius');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Magius");
        $const_set($nesting[0], 'NAME', "MAGIUS");
        $const_set($nesting[0], 'SORT_KEY', "まきうす");
        $const_set($nesting[0], 'HELP_MESSAGE', "■能力値判定　MA+x>=t        x:修正値 t:目標値\n" + "例)MA>=7: ダイスを2個振って、その結果を表示\n" + "\n" + "■技能値判定　MS+x>=t        x:修正値 t:目標値\n" + "例)MS>=7: ダイスを3個振って、そのうち上位2つを採用し、結果を表示\n" + "\n");
        self.$register_prefix("M[AS]");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = self.$resolute_ability_action(command)))) {
            return $ret_or_1
          } else {
            return self.$resolute_skill_action(command)
          }
        });
        self.$private();
        
        $def(self, '$with_symbol', function $$with_symbol(number) {
          
          if ($eqeq(number, 0)) {
            return ""
          } else if ($truthy($rb_gt(number, 0))) {
            return "+" + (number)
          } else {
            return number.$to_s()
          }
        });
        
        $def(self, '$get_result_of_ability_action', function $$get_result_of_ability_action(total, _dice_add, target) {
          
          if ($truthy($rb_ge(total, target))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          }
        });
        
        $def(self, '$resolute_ability_action', function $$resolute_ability_action(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, modify = nil, target = nil, dices = nil, dice_text = nil, dice_add = nil, total = nil, result = nil, sequence = nil;

          
          m = /MA([+-]\d+)*>=(\d+)/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = ($truthy(m['$[]'](1)) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (0));
          target = m['$[]'](2).$to_i();
          dices = self.randomizer.$roll_barabara(2, 6).$sort();
          dice_text = dices.$join(",");
          dice_add = dices.$sum();
          total = $rb_plus(dice_add, modify);
          result = self.$get_result_of_ability_action(total, dice_add, target);
          sequence = ["(" + (command) + ")", "[" + (dice_text) + "]" + (self.$with_symbol(modify)), total, result.$text()].$compact();
          result['$text='](sequence.$join(" ＞ "));
          return result;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$get_result_of_skill_action', function $$get_result_of_skill_action(total, _dice_add, target) {
          
          if ($truthy($rb_ge(total, target))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          }
        });
        return $def(self, '$resolute_skill_action', function $$resolute_skill_action(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, modify = nil, target = nil, dices = nil, dice_text = nil, dice_add = nil, total = nil, result = nil, sequence = nil;

          
          m = /MS([+-]\d+)*>=(\d+)/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = ($truthy(m['$[]'](1)) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (0));
          target = m['$[]'](2).$to_i();
          dices = self.randomizer.$roll_barabara(3, 6).$sort();
          dice_text = dices.$join(",");
          dice_add = $rb_plus(dices['$[]'](1).$to_i(), dices['$[]'](2).$to_i());
          total = $rb_plus(dice_add, modify);
          result = self.$get_result_of_skill_action(total, dice_add, target);
          sequence = ["(" + (command) + ")", "[" + (dice_text) + "]" + (self.$with_symbol(modify)), total, result.$text()].$compact();
          result['$text='](sequence.$join(" ＞ "));
          return result;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_ge = Opal.rb_ge, $def = Opal.def, self = Opal.top, $nesting = [], nil = Opal.nil;

  
  self.$require("bcdice/game_system/Magius");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Magius_3rdNewTokyoCity');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "Magius_3rdNewTokyoCity");
        $const_set($nesting[0], 'NAME', "MAGIUS:新世紀エヴァンゲリオンRPG 決戦！第3新東京市");
        $const_set($nesting[0], 'SORT_KEY', "まきうすしんせいきえうあんけりおんRPGけつせんたい3しんとうきようし");
        $const_set($nesting[0], 'HELP_MESSAGE', "■能力値判定　MA+x>=t        x:修正値 t:目標値\n" + "例)MA>=7: ダイスを2個振って、その結果(成功,失敗,絶対成功,絶対失敗)を表示\n" + "\n" + "■技能値判定　MS+x>=t        x:修正値 t:目標値\n" + "例)MS>=7: ダイスを3個振って、そのうち上位2つを採用し、結果(成功,失敗,絶対成功,絶対失敗)を表示\n" + "\n");
        self.$register_prefix("M[AS]");
        self.$private();
        
        $def(self, '$get_result_of_ability_action', function $$get_result_of_ability_action(total, dice_add, target) {
          
          if ($eqeq(dice_add, 12)) {
            return $$('Result').$critical("絶対成功")
          } else if ($eqeq(dice_add, 2)) {
            return $$('Result').$fumble("絶対失敗")
          } else if ($truthy($rb_ge(total, target))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          }
        });
        return $def(self, '$get_result_of_skill_action', function $$get_result_of_skill_action(total, dice_add, target) {
          
          if ($eqeq(dice_add, 12)) {
            return $$('Result').$critical("絶対成功")
          } else if ($eqeq(dice_add, 2)) {
            return $$('Result').$fumble("絶対失敗")
          } else if ($truthy($rb_ge(total, target))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          }
        });
      })($nesting[0], $$('Magius'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
