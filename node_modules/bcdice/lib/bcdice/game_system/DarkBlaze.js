require('./../opal');

Opal.modules["bcdice/normalize"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base) {
      var self = $module($base, 'Normalize');

      
      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq(/<=|=</, ($ret_or_1 = op))) {
          return "<="
        } else if ($eqeqeq(/>=|=>/, $ret_or_1)) {
          return ">="
        } else if ($eqeqeq(/<>|!=|=!/, $ret_or_1)) {
          return "!="
        } else if ($eqeqeq(/</, $ret_or_1)) {
          return "<"
        } else if ($eqeqeq(/>/, $ret_or_1)) {
          return ">"
        } else if ($eqeqeq(/=/, $ret_or_1)) {
          return "=="
        } else {
          return nil
        }
      });
      return $def(self, '$target_number', function $$target_number(val) {
        
        if ($eqeq(val, "?")) {
          return val
        } else {
          return val.$to_i()
        }
      });
    })($nesting[0])
  })($nesting[0], $nesting)
};

Opal.modules["bcdice/format"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'Format');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq("==", ($ret_or_1 = op))) {
          return "="
        } else if ($eqeqeq("!=", $ret_or_1)) {
          return "<>"
        } else if ($eqeqeq($$('Symbol'), $ret_or_1)) {
          return op.$to_s()
        } else {
          return nil
        }
      });
      return $def(self, '$modifier', function $$modifier(number) {
        
        if ($truthy(number['$nil?']())) {
          return nil
        } else if ($eqeq(number, 0)) {
          return ""
        } else if ($truthy($rb_gt(number, 0))) {
          return "+" + (number)
        } else {
          return number.$to_s()
        }
      });
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $send = Opal.send, $def = Opal.def, $to_ary = Opal.to_ary, $neqeq = Opal.neqeq, $rb_plus = Opal.rb_plus, $thrower = Opal.thrower, $rb_lt = Opal.rb_lt, $rb_minus = Opal.rb_minus, $rb_le = Opal.rb_le, $rb_ge = Opal.rb_ge, $rb_divide = Opal.rb_divide, $eqeq = Opal.eqeq, self = Opal.top, $nesting = [], nil = Opal.nil;

  
  self.$require("bcdice/normalize");
  self.$require("bcdice/format");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'DarkBlaze');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "DarkBlaze");
        $const_set($nesting[0], 'NAME', "ダークブレイズ");
        $const_set($nesting[0], 'SORT_KEY', "たあくふれいす");
        $const_set($nesting[0], 'HELP_MESSAGE', "・行為判定　(DBxy#n)\n" + "　行為判定専用のコマンドです。\n" + "　\"DB(能力)(技能)#(修正)\"でロールします。Rコマンド(3R6+n[x,y]>=m mは難易度)に読替をします。\n" + "　クリティカルとファンブルも自動で処理されます。\n" + "　DB@x@y#m と DBx,y#m にも対応しました。\n" + "　例）DB33　　　DB32#-1　　　DB@3@1#1　　　DB3,2　　　DB23#1>=4　　　3R6+1[3,3]>=4\n" + "\n" + "・掘り出し袋表　(BTx)\n" + "　\"BT(ダイス数)\"で掘り出し袋表を自動で振り、結果を表示します。\n" + "　例）BT1　　　BT2　　　BT[1...3]\n");
        self.$register_prefix("DB", "BT", "3R6");
        
        $def(self, '$replace_text', function $$replace_text(string) {
          
          
          if (!$truthy(string['$=~'](/DB/i))) {
            return string
          };
          string = $send(string, 'gsub', [/DB(\d),(\d)/], function $$1(){
            return "DB" + ($$('Regexp').$last_match(1)) + ($$('Regexp').$last_match(2))});
          string = $send(string, 'gsub', [/DB@(\d)@(\d)/], function $$2(){
            return "DB" + ($$('Regexp').$last_match(1)) + ($$('Regexp').$last_match(2))});
          string = $send(string, 'gsub', [/DB(\d)(\d)(#(\d[+\-\d]*))/], function $$3(){
            return "3R6+" + ($$('Regexp').$last_match(4)) + "[" + ($$('Regexp').$last_match(1)) + "," + ($$('Regexp').$last_match(2)) + "]"});
          string = $send(string, 'gsub', [/DB(\d)(\d)(#([+\-\d]*))/], function $$4(){
            return "3R6" + ($$('Regexp').$last_match(4)) + "[" + ($$('Regexp').$last_match(1)) + "," + ($$('Regexp').$last_match(2)) + "]"});
          string = $send(string, 'gsub', [/DB(\d)(\d)/], function $$5(){
            return "3R6[" + ($$('Regexp').$last_match(1)) + "," + ($$('Regexp').$last_match(2)) + "]"});
          return string;
        });
        
        $def(self, '$check_roll', function $$check_roll(string) {try { var $t_eval_return = $thrower('eval_return'); 
          var $a, $b, self = this, m = nil, mod = nil, abl = nil, skl = nil, signOfInequality = nil, diff = nil, cmp_op = nil, total = nil, out_str = nil, output = nil;

          
          string = self.$replace_text(string);
          if (!$truthy((m = /(^|\s)S?(3[rR]6([+\-\d]+)?(\[(\d+),(\d+)\])(([>=]+)(\d+))?)(\s|$)/i.$match(string)))) {
            return nil
          };
          string = m['$[]'](2);
          mod = 0;
          abl = 1;
          skl = 1;
          signOfInequality = "";
          diff = 0;
          if ($truthy(m['$[]'](3))) {
            mod = $$('ArithmeticEvaluator').$eval(m['$[]'](3))
          };
          if ($truthy(m['$[]'](4))) {
            
            abl = m['$[]'](5).$to_i();
            skl = m['$[]'](6).$to_i();
          };
          if ($truthy(m['$[]'](7))) {
            
            cmp_op = $$('Normalize').$comparison_operator(m['$[]'](8));
            signOfInequality = $$('Format').$comparison_operator(cmp_op);
            diff = m['$[]'](9).$to_i();
          };
          $b = self.$get_dice(mod, abl, skl), $a = $to_ary($b), (total = ($a[0] == null ? nil : $a[0])), (out_str = ($a[1] == null ? nil : $a[1])), $b;
          output = "(" + (string) + ") ＞ " + (out_str);
          if ($neqeq(signOfInequality, "")) {
            
            cmp_op = $$('Normalize').$comparison_operator(signOfInequality);
            output = $rb_plus(output, ($truthy(total.$send(cmp_op, diff)) ? (" ＞ 成功") : (" ＞ 失敗")));
          };
          return output;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$get_dice', function $$get_dice(mod, abl, skl) {
          var self = this, total = nil, crit = nil, fumble = nil, dice_c = nil, dice_arr = nil, dice_str = nil, resultText = nil, output = nil;

          
          total = 0;
          crit = 0;
          fumble = 0;
          dice_c = $rb_plus(3, mod.$abs());
          dice_arr = self.randomizer.$roll_barabara(dice_c, 6).$sort();
          dice_str = dice_arr.$join(",");
          $send((3), 'times', [], function $$6(i){var ch = nil;

            
            if (i == null) i = nil;
            ch = dice_arr['$[]'](i);
            if ($truthy($rb_lt(mod, 0))) {
              ch = dice_arr['$[]']($rb_minus($rb_minus(dice_c, i), 1))
            };
            if ($truthy($rb_le(ch, abl))) {
              total = $rb_plus(total, 1)
            };
            if ($truthy($rb_le(ch, skl))) {
              total = $rb_plus(total, 1)
            };
            if ($truthy($rb_le(ch, 2))) {
              crit = $rb_plus(crit, 1)
            };
            if ($truthy($rb_ge(ch, 5))) {
              return (fumble = $rb_plus(fumble, 1))
            } else {
              return nil
            };});
          resultText = "";
          if ($truthy($rb_ge(crit, 3))) {
            
            resultText = " ＞ クリティカル";
            total = $rb_plus(6, skl);
          };
          if ($truthy($rb_ge(fumble, 3))) {
            
            resultText = " ＞ ファンブル";
            total = 0;
          };
          output = "" + (total) + "[" + (dice_str) + "]" + (resultText);
          return [total, output];
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, self = this, m = nil, dice = nil, $ret_or_1 = nil;

          
          if ($truthy((m = /^BT(\d)?$/i.$match(command)))) {
            
            dice = ($truthy(($ret_or_1 = ($a = m['$[]'](1), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (1));
            return self.$get_horidasibukuro_table(dice);
          };
          return self.$check_roll(command);
        });
        return $def(self, '$get_horidasibukuro_table', function $$get_horidasibukuro_table(dice) {
          var self = this, output = nil, material_kind = nil, magic_stone = nil, num1 = nil, num2 = nil, magic_stone_result = nil, type = nil;

          
          output = "1";
          material_kind = ["蟲甲", "金属", "金貨", "植物", "獣皮", "竜鱗", "レアモノ", "レアモノ"];
          magic_stone = ["火炎石", "雷撃石", "氷結石"];
          num1 = self.randomizer.$roll_sum(2, 6);
          num2 = self.randomizer.$roll_sum(dice, 6);
          self.$debug("dice", dice);
          self.$debug("num1", num1);
          self.$debug("num2", num2);
          if ($truthy($rb_le(num1, 4))) {
            
            num2 = self.randomizer.$roll_once(6);
            magic_stone_result = magic_stone['$[]']($rb_minus($rb_divide(num2, 2).$to_i(), 1));
            output = "《" + (magic_stone_result) + "》を" + (dice) + "個獲得";
          } else if ($eqeq(num1, 7)) {
            output = "《金貨》を" + (num2) + "枚獲得"
          } else {
            
            type = material_kind['$[]']($rb_minus(num1, 5));
            if ($truthy($rb_le(num2, 3))) {
              output = "《" + (type) + " I》を1個獲得"
            } else if ($truthy($rb_le(num2, 5))) {
              output = "《" + (type) + " I》を2個獲得"
            } else if ($truthy($rb_le(num2, 7))) {
              output = "《" + (type) + " I》を3個獲得"
            } else if ($truthy($rb_le(num2, 9))) {
              output = "《" + (type) + " II》を1個獲得"
            } else if ($truthy($rb_le(num2, 11))) {
              output = "《" + (type) + " I》を2個《" + (type) + " II》を1個獲得"
            } else if ($truthy($rb_le(num2, 13))) {
              output = "《" + (type) + " I》を2個《" + (type) + " II》を2個獲得"
            } else if ($truthy($rb_le(num2, 15))) {
              output = "《" + (type) + " III》を1個獲得"
            } else if ($truthy($rb_le(num2, 17))) {
              output = "《" + (type) + " II》を2個《" + (type) + " III》を1個獲得"
            } else {
              output = "《" + (type) + " II》を2個《" + (type) + " III》を2個獲得"
            };
          };
          if ($neqeq(output, "1")) {
            output = "掘り出し袋表[" + (num1) + "," + (num2) + "] ＞ " + (output)
          };
          return output;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
