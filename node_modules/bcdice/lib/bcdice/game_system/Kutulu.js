require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Kutulu');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Kutulu");
        $const_set($nesting[0], 'NAME', "Kutulu");
        $const_set($nesting[0], 'SORT_KEY', "くとうるう");
        $const_set($nesting[0], 'HELP_MESSAGE', "■判定　nKU        n: ダイス数\n" + "\n" + "例)3KU: ダイスを3個振って、その結果を表示(ギリギリでの成功も表示)\n" + "\n" + "■対抗判定　nKR        n: ダイス数\n" + "\n" + "例)2KR: ダイスを2個振って、その結果を表示。対抗判定用の3桁の数字も出力。(大きい方が勝利)\n");
        self.$register_prefix("\\dK[UR]");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = self.$resolute_action(command)))) {
            return $ret_or_1
          } else {
            return self.$resolute_competition(command)
          }
        });
        self.$private();
        
        $def(self, '$resolute_action', function $$resolute_action(command) {
          var self = this, m = nil, num_dices = nil, dices = nil, dice_text = nil, output = nil, success_num = nil, counts_4 = nil;

          
          m = /(\d)KU/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num_dices = m['$[]'](1).$to_i();
          dices = self.randomizer.$roll_barabara(num_dices, 6).$sort();
          dice_text = dices.$join(",");
          output = "(" + (num_dices) + "KU) ＞ " + (dice_text);
          success_num = $send(dices, 'count', [], function $$1(val){
            
            if (val == null) val = nil;
            return $rb_ge(val, 4);});
          counts_4 = dices.$count(4);
          if ($truthy($rb_gt(success_num, 0))) {
            
            output = $rb_plus(output, " ＞ 成功数" + (success_num));
            if (($eqeq(success_num, 1) && ($eqeq(counts_4, 1)))) {
              output = $rb_plus(output, " ＞ *ギリギリでの成功")
            };
            return $$('Result').$success(output);
          } else {
            
            output = $rb_plus(output, " ＞ 失敗");
            return $$('Result').$failure(output);
          };
        });
        return $def(self, '$resolute_competition', function $$resolute_competition(command) {
          var self = this, m = nil, num_dices = nil, dices = nil, dice_text = nil, counts_6 = nil, counts_5 = nil, success_num = nil, com_text = nil, output = nil;

          
          m = /(\d)KR/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num_dices = m['$[]'](1).$to_i();
          dices = self.randomizer.$roll_barabara(num_dices, 6).$sort();
          dice_text = dices.$join(",");
          counts_6 = dices.$count(6);
          counts_5 = dices.$count(5);
          success_num = $send(dices, 'count', [], function $$2(val){
            
            if (val == null) val = nil;
            return $rb_ge(val, 4);});
          com_text = self.$format("(%d%d%d)", success_num, counts_6, counts_5);
          output = "(" + (num_dices) + "KR) ＞ " + (dice_text) + " ＞ " + (com_text);
          if ($truthy($rb_gt(success_num, 0))) {
            return $$('Result').$success(output)
          } else {
            return $$('Result').$failure(output)
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
