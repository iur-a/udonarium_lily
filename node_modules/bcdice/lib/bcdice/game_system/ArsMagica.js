require('./../opal');

Opal.modules["bcdice/normalize"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base) {
      var self = $module($base, 'Normalize');

      
      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq(/<=|=</, ($ret_or_1 = op))) {
          return "<="
        } else if ($eqeqeq(/>=|=>/, $ret_or_1)) {
          return ">="
        } else if ($eqeqeq(/<>|!=|=!/, $ret_or_1)) {
          return "!="
        } else if ($eqeqeq(/</, $ret_or_1)) {
          return "<"
        } else if ($eqeqeq(/>/, $ret_or_1)) {
          return ">"
        } else if ($eqeqeq(/=/, $ret_or_1)) {
          return "=="
        } else {
          return nil
        }
      });
      return $def(self, '$target_number', function $$target_number(val) {
        
        if ($eqeq(val, "?")) {
          return val
        } else {
          return val.$to_i()
        }
      });
    })($nesting[0])
  })($nesting[0], $nesting)
};

Opal.modules["bcdice/format"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'Format');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq("==", ($ret_or_1 = op))) {
          return "="
        } else if ($eqeqeq("!=", $ret_or_1)) {
          return "<>"
        } else if ($eqeqeq($$('Symbol'), $ret_or_1)) {
          return op.$to_s()
        } else {
          return nil
        }
      });
      return $def(self, '$modifier', function $$modifier(number) {
        
        if ($truthy(number['$nil?']())) {
          return nil
        } else if ($eqeq(number, 0)) {
          return ""
        } else if ($truthy($rb_gt(number, 0))) {
          return "+" + (number)
        } else {
          return number.$to_s()
        }
      });
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $rb_minus = Opal.rb_minus, $eqeq = Opal.eqeq, $send = Opal.send, $rb_plus = Opal.rb_plus, $neqeq = Opal.neqeq, $rb_gt = Opal.rb_gt, $rb_lt = Opal.rb_lt, $rb_times = Opal.rb_times, $rb_ge = Opal.rb_ge, $def = Opal.def, $thrower = Opal.thrower, self = Opal.top, $nesting = [], nil = Opal.nil;

  
  self.$require("bcdice/normalize");
  self.$require("bcdice/format");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ArsMagica');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.target_numner = $proto.botch = $proto.modify_number = $proto.cmp_op = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "ArsMagica");
        $const_set($nesting[0], 'NAME', "アルスマギカ");
        $const_set($nesting[0], 'SORT_KEY', "あるすまきか");
        $const_set($nesting[0], 'HELP_MESSAGE', "・ストレスダイス　(ArSx+y)\n" + "　\"ArS(ボッチダイス)+(修正)\"です。判定にも使えます。Rコマンド(1R10+y[m])に読替をします。\n" + "　ボッチダイスと修正は省略可能です。(ボッチダイスを省略すると1として扱います)\n" + "　botchダイスの0の数が2以上の時は、数えて表示します。\n" + "　（注意！） botchの判断が発生したときには、そのダイスを含めてロールした全てのダイスを[]の中に並べて表示します。\n" + "　例) (1R10[5]) ＞ 0[0,1,8,0,8,1] ＞ Botch!\n" + "　　最初の0が判断基準で、その右側5つがボッチダイスです。1*2,8*2,0*1なので1botchという訳です。\n");
        self.$register_prefix("ArS", "1R10");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(string) {
          var self = this, diff = nil, $ret_or_1 = nil, botch = nil, bonus = nil, crit_mul = nil, total = nil, cmp_op = nil, die = nil, output = nil, count0 = nil, dice_n = nil, crit_dice = nil;

          
          if (!($truthy(self.$parse_ars(string)) || ($truthy(self.$parse_1r10(string))))) {
            return nil
          };
          diff = ($truthy(($ret_or_1 = self.target_numner)) ? ($ret_or_1) : (0));
          botch = self.botch;
          bonus = self.modify_number;
          crit_mul = 1;
          total = 0;
          cmp_op = self.cmp_op;
          die = $rb_minus(self.randomizer.$roll_once(10), 1);
          output = "(" + (self.$expr()) + ") ＞ ";
          if ($eqeq(die, 0)) {
            
            count0 = 0;
            dice_n = [];
            $send(botch, 'times', [], function $$1(_i){var self = $$1.$$s == null ? this : $$1.$$s, botch_die = nil;
              if (self.randomizer == null) self.randomizer = nil;

              
              if (_i == null) _i = nil;
              botch_die = $rb_minus(self.randomizer.$roll_once(10), 1);
              if ($eqeq(botch_die, 0)) {
                count0 = $rb_plus(count0, 1)
              };
              return dice_n.$push(botch_die);}, {$$s: self});
            output = $rb_plus(output, "0[" + (die) + "," + (dice_n.$join(",")) + "]");
            if ($neqeq(count0, 0)) {
              
              if ($truthy($rb_gt(count0, 1))) {
                output = $rb_plus(output, " ＞ " + (count0) + "Botch!")
              } else {
                output = $rb_plus(output, " ＞ Botch!")
              };
              cmp_op = nil;
            } else {
              
              if ($truthy($rb_gt(bonus, 0))) {
                output = $rb_plus(output, "+" + (bonus) + " ＞ " + (bonus))
              } else if ($truthy($rb_lt(bonus, 0))) {
                output = $rb_plus(output, "" + (bonus) + " ＞ " + (bonus))
              } else {
                output = $rb_plus(output, " ＞ 0")
              };
              total = bonus;
            };
          } else if ($eqeq(die, 1)) {
            
            crit_dice = "";
            while ($eqeq(die, 1)) {
            
              crit_mul = $rb_times(crit_mul, 2);
              die = self.randomizer.$roll_once(10);
              crit_dice = $rb_plus(crit_dice, "" + (die) + ",");
            };
            total = $rb_times(die, crit_mul);
            crit_dice = crit_dice.$sub(/,$/, "");
            output = $rb_plus(output, total.$to_s());
            output = $rb_plus(output, "[1," + (crit_dice) + "]");
            total = $rb_plus(total, bonus);
            if ($truthy($rb_gt(bonus, 0))) {
              output = $rb_plus(output, "+" + (bonus) + " ＞ " + (total))
            } else if ($truthy($rb_lt(bonus, 0))) {
              output = $rb_plus(output, "" + (bonus) + " ＞ " + (total))
            };
          } else {
            
            total = $rb_plus(die, bonus);
            if ($truthy($rb_gt(bonus, 0))) {
              output = $rb_plus(output, "" + (die) + "+" + (bonus) + " ＞ " + (total))
            } else if ($truthy($rb_lt(bonus, 0))) {
              output = $rb_plus(output, "" + (die) + (bonus) + " ＞ " + (total))
            } else {
              output = $rb_plus(output, total.$to_s())
            };
          };
          if ($eqeq(cmp_op, ">=")) {
            output = $rb_plus(output, ($truthy($rb_ge(total, diff)) ? (" ＞ 成功") : (" ＞ 失敗")))
          };
          return output.$to_s();
        });
        self.$private();
        
        $def(self, '$parse_ars', function $$parse_ars(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var $a, $b, self = this, m = nil, $ret_or_1 = nil;

          
          m = /^ArS(\d+)?((?:[+-]-?\d+)+)?(?:([>=]+)(\d+))?$/i.$match(command);
          if (!$truthy(m)) {
            return false
          };
          self.botch = ($truthy(($ret_or_1 = ($a = m['$[]'](1), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (1));
          self.modify_number = $$('ArithmeticEvaluator').$eval(($truthy(($ret_or_1 = m['$[]'](2))) ? ($ret_or_1) : ("")));
          self.cmp_op = $$('Normalize').$comparison_operator(m['$[]'](3));
          self.target_numner = ($b = m['$[]'](4), ($b === nil || $b == null) ? nil : $b.$to_i());
          return true;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$parse_1r10', function $$parse_1r10(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var $a, $b, self = this, m = nil, $ret_or_1 = nil;

          
          m = /^1R10((?:[+-]-?\d+)+)?(?:\[(\d+)\])?(?:([>=]+)(\d+))?$/i.$match(command);
          if (!$truthy(m)) {
            return false
          };
          self.modify_number = $$('ArithmeticEvaluator').$eval(($truthy(($ret_or_1 = m['$[]'](1))) ? ($ret_or_1) : ("")));
          self.botch = ($truthy(($ret_or_1 = ($a = m['$[]'](2), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (1));
          self.cmp_op = $$('Normalize').$comparison_operator(m['$[]'](3));
          self.target_numner = ($b = m['$[]'](4), ($b === nil || $b == null) ? nil : $b.$to_i());
          return true;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        return $def(self, '$expr', function $$expr() {
          var self = this, modifier = nil;

          
          modifier = $$('Format').$modifier(self.modify_number);
          return "1R10" + (modifier) + "[" + (self.botch) + "]" + (self.cmp_op) + (self.target_numner);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
