require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $truthy = Opal.truthy, $rb_le = Opal.rb_le, $rb_ge = Opal.rb_ge, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Ainecadette');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Ainecadette");
        $const_set($nesting[0], 'NAME', "エネカデット");
        $const_set($nesting[0], 'SORT_KEY', "えねかてつと");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 判定\n" + "- 先輩 (AI) 10面ダイスを2つ振って判定します。『有利』なら【3AI】、『不利』なら【1AI】を使います。\n" + "- 後輩 (CA) 6面ダイスを2つ振って判定します。『有利』なら【3CA】、『不利』なら【1CA】を使います。\n");
        self.$register_prefix("(\\d+)?AI", "(\\d+)?CA");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$roll_action(command)
        });
        self.$private();
        $const_set($nesting[0], 'SUCCESS_THRESHOLD', 4);
        $const_set($nesting[0], 'SPECIAL_DICE', 6);
        return $def(self, '$roll_action', function $$roll_action(command) {
          var $a, self = this, m = nil, is_senpai = nil, times = nil, $ret_or_1 = nil, sides = nil, dice_list = nil, max = nil, result = nil, me = nil, target = nil;

          
          m = /^(\d+)?(AI|CA)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          is_senpai = m['$[]'](2)['$==']("AI");
          times = ($truthy(($ret_or_1 = ($a = m['$[]'](1), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (2));
          sides = ($truthy(is_senpai) ? (10) : (6));
          if ($truthy($rb_le(times, 0))) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(times, sides);
          max = dice_list.$max();
          result = ($truthy($rb_le(max, 1)) ? ($$('Result').$fumble("ファンブル（もやもやカウンターを2個獲得）")) : ($truthy(dice_list['$include?'](6)) ? (((me = ($truthy(is_senpai) ? ("先輩") : ("後輩"))), (target = ($truthy(is_senpai) ? ("後輩") : ("先輩"))), $$('Result').$critical("スペシャル（絆カウンターを1個獲得し、" + (target) + "は" + (me) + "への感情を1つ獲得）"))) : ($truthy($rb_ge(max, $$('SUCCESS_THRESHOLD'))) ? ($$('Result').$success("成功")) : ($$('Result').$failure("失敗")))));
          result['$text=']("(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "] ＞ " + (result.$text()));
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
