require('./../opal');

Opal.modules["bcdice/arithmetic_evaluator"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $ensure_kwargs = Opal.ensure_kwargs, $truthy = Opal.truthy, $thrower = Opal.thrower, $def = Opal.def, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'ArithmeticEvaluator');

      var $nesting = [self].concat($parent_nesting);

      return (function(self, $parent_nesting) {
        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        return $def(self, '$eval', function $eval$1(expr, $kwargs) {try { var $t_eval_return = $thrower('eval_return'); 
          var round_type, $ret_or_1 = nil;

          
          $kwargs = $ensure_kwargs($kwargs);
          
          round_type = $kwargs.$$smap["round_type"];if (round_type == null) round_type = $$$($$('RoundType'), 'FLOOR');
          if (!$truthy(expr)) {
            return 0
          };
          if ($truthy(($ret_or_1 = $$('Arithmetic').$eval(expr, round_type)))) {
            return $ret_or_1
          } else {
            return 0
          };} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        }, -2)
      })(Opal.get_singleton_class(self), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $rb_ge = Opal.rb_ge, $rb_divide = Opal.rb_divide, $rb_plus = Opal.rb_plus, $rb_minus = Opal.rb_minus, $truthy = Opal.truthy, $send = Opal.send, $rb_lt = Opal.rb_lt, $rb_gt = Opal.rb_gt, $send2 = Opal.send2, $find_super = Opal.find_super, $eqeq = Opal.eqeq, $thrower = Opal.thrower, self = Opal.top, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  
  self.$require("bcdice/arithmetic_evaluator");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'CthulhuTech');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        self.$register_prefix("\\d+D10");
        $const_set($nesting[0], 'ID', "CthulhuTech");
        $const_set($nesting[0], 'NAME', "クトゥルフテック");
        $const_set($nesting[0], 'SORT_KEY', "くとうるふてつく");
        $const_set($nesting[0], 'HELP_MESSAGE', "・行為判定（test）：nD10+m>=d\n" + "　n個のダイスを使用して、修正値m、難易度dで行為判定（test）を行います。\n" + "　修正値mは省略可能、複数指定可能（例：+2-4）です。\n" + "　成功、失敗、クリティカル、ファンブルを自動判定します。\n" + "　例）2D10>=12　4D10+2>=28　5D10+2-4>=32\n" + "\n" + "・対抗判定（contest）：nD10+m>d\n" + "　行為判定と同様ですが、防御側有利のため「>=」ではなく「>」を入力します。\n" + "　ダメージダイスも表示します。\n");
        (function($base, $super, $parent_nesting) {
          var self = $klass($base, $super, 'Test');

          var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

          $proto.num = $proto.modifier = $proto.difficulty = nil;
          
          $const_set($nesting[0], 'COMPARE_OP', ">=");
          
          $def(self, '$initialize', function $$initialize(num, modifier, difficulty) {
            var self = this;

            
            self.num = num;
            self.modifier = modifier;
            return (self.difficulty = difficulty);
          });
          
          $def(self, '$execute', function $$execute(randomizer) {
            var self = this, dice_values = nil, fumble = nil, sorted_dice_values = nil, roll_result = nil, test_value = nil, diff = nil, success = nil, $ret_or_1 = nil, critical = nil, output_parts = nil;

            
            dice_values = randomizer.$roll_barabara(self.num, 10);
            fumble = $rb_ge(dice_values.$count(1), $rb_divide($rb_plus(dice_values.$length(), 1), 2).$to_i());
            sorted_dice_values = dice_values.$sort();
            roll_result = self.$calculate_roll_result(sorted_dice_values);
            test_value = $rb_plus(roll_result, self.modifier);
            diff = $rb_minus(test_value, self.difficulty);
            success = ($truthy(($ret_or_1 = fumble['$!']())) ? (diff.$send($$$(self.$class(), 'COMPARE_OP'), 0)) : ($ret_or_1));
            critical = $rb_ge(diff, 10);
            output_parts = ["(" + (self.$expression()) + ")", self.$test_value_expression(sorted_dice_values, roll_result), test_value, self.$result_str(success, fumble, critical, diff)];
            return output_parts.$join(" ＞ ");
          });
          self.$private();
          
          $def(self, '$expression', function $$expression() {
            var self = this, modifier_str = nil;

            
            modifier_str = $$('Format').$modifier(self.modifier);
            return "" + (self.num) + "D10" + (modifier_str) + ($$$(self.$class(), 'COMPARE_OP')) + (self.difficulty);
          });
          
          $def(self, '$test_value_expression', function $$test_value_expression(dice_values, roll_result) {
            var self = this, dice_str = nil, modifier_str = nil;

            
            dice_str = dice_values.$join(",");
            modifier_str = $$('Format').$modifier(self.modifier);
            return "" + (roll_result) + "[" + (dice_str) + "]" + (modifier_str);
          });
          
          $def(self, '$result_str', function $$result_str(success, fumble, critical, _diff) {
            
            
            if ($truthy(fumble)) {
              return "ファンブル"
            };
            if ($truthy(critical)) {
              return "クリティカル"
            };
            return ($truthy(success) ? ("成功") : ("失敗"));
          });
          
          $def(self, '$calculate_roll_result', function $$calculate_roll_result(sorted_dice_values) {
            var self = this, highest_single_roll = nil, sum_of_highest_set_of_multiples = nil, candidates = nil;

            
            highest_single_roll = sorted_dice_values.$last();
            sum_of_highest_set_of_multiples = $send($send(sorted_dice_values, 'group_by', [], "itself".$to_proc()).$values(), 'map', [], "sum".$to_proc()).$max();
            candidates = [highest_single_roll, sum_of_highest_set_of_multiples, self.$sum_of_largest_straight(sorted_dice_values)];
            return candidates.$max();
          });
          return $def(self, '$sum_of_largest_straight', function $$sum_of_largest_straight(sorted_dice_values) {
            var max_sum = nil, n_consecutive_values = nil, sum = nil, last = nil;

            
            if ($truthy($rb_lt(sorted_dice_values.$length(), 3))) {
              return 0
            };
            max_sum = 0;
            n_consecutive_values = 0;
            sum = 0;
            last = -1;
            $send(sorted_dice_values.$uniq(), 'each', [], function $$1(value){
              
              if (value == null) value = nil;
              if ($truthy($rb_gt($rb_minus(value, last), 1))) {
                
                n_consecutive_values = 1;
                sum = value;
                last = value;
                return nil;
              };
              n_consecutive_values = $rb_plus(n_consecutive_values, 1);
              sum = $rb_plus(sum, value);
              last = value;
              if (($truthy($rb_ge(n_consecutive_values, 3)) && ($truthy($rb_gt(sum, max_sum))))) {
                return (max_sum = sum)
              } else {
                return nil
              };});
            return max_sum;
          });
        })($nesting[0], null, $nesting);
        (function($base, $super, $parent_nesting) {
          var self = $klass($base, $super, 'Contest');

          var $nesting = [self].concat($parent_nesting);

          
          $const_set($nesting[0], 'COMPARE_OP', ">");
          return $def(self, '$result_str', function $$result_str(success, _fumble, _critical, diff) {
            var $yield = $$result_str.$$p || nil, self = this, formatted = nil, damage_roll_num = nil, damage_roll = nil;

            $$result_str.$$p = null;
            
            formatted = $send2(self, $find_super(self, 'result_str', $$result_str, false, true), 'result_str', [success, _fumble, _critical, diff], $yield);
            if ($truthy(success)) {
              
              damage_roll_num = $rb_divide(diff, 5.0).$ceil();
              damage_roll = "" + (damage_roll_num) + "D10";
              return "" + (formatted) + "（ダメージ：" + (damage_roll) + "）";
            } else {
              return formatted
            };
          });
        })($nesting[0], $$('Test'), $nesting);
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, node = nil;

          
          node = self.$parse(command);
          if (!$truthy(node)) {
            return nil
          };
          return node.$execute(self.randomizer);
        });
        self.$private();
        $const_set($nesting[0], 'TEST_RE', /^(\d+)D10((?:[-+]\d+)+)?(>=?)(\d+)$/.$freeze());
        return $def(self, '$parse', function $$parse(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var m = nil, num = nil, modifier = nil, node_class = nil, difficulty = nil;

          
          m = $$('TEST_RE').$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num = m['$[]'](1).$to_i();
          modifier = ($truthy(m['$[]'](2)) ? ($$('ArithmeticEvaluator').$eval(m['$[]'](2))) : (0));
          node_class = ($eqeq(m['$[]'](3), ">") ? ($$('Contest')) : ($$('Test')));
          difficulty = m['$[]'](4).$to_i();
          return node_class.$new(num, modifier, difficulty);} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
