require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $rb_ge = Opal.rb_ge, $eqeq = Opal.eqeq, $rb_lt = Opal.rb_lt, $send = Opal.send, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'StrangerOfSwordCity');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "StrangerOfSwordCity");
        $const_set($nesting[0], 'NAME', "剣の街の異邦人TRPG");
        $const_set($nesting[0], 'SORT_KEY', "つるきのまちのいほうしんTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定　xSR or xSRy or xSR+y or xSR-y or xSR+y>=z\n" + "　x=ダイス数、y=修正値(省略可、±省略時は＋として扱う)、z=難易度(省略可)\n" + "　判定時はクリティカル、ファンブルの自動判定を行います。\n" + "・通常のnD6ではクリティカル、ファンブルの自動判定は行いません。\n" + "・D66ダイスあり\n");
        self.$register_prefix("\\d+SR");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          self.d66_sort_type = $$$($$('D66SortType'), 'NO_SORT');
          return (self.round_type = $$$($$('RoundType'), 'FLOOR'));
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, result = nil;

          
          self.$debug("eval_game_system_specific_command command", command);
          command = command.$upcase();
          result = self.$checkRoll(command);
          if ($truthy(result['$instance_of?']($$('Result')))) {
            return result
          };
          if (!$truthy(result['$empty?']())) {
            return result
          };
          return result;
        });
        
        $def(self, '$checkRoll', function $$checkRoll(command) {
          var self = this, result = nil, diceCount = nil, modify = nil, difficulty = nil, diceList = nil, dice = nil, totalValue = nil, modifyText = nil, criticalResult = nil;

          
          self.$debug("checkRoll begin command", command);
          result = $$('Result').$new();
          result['$text=']("");
          if (!$truthy(command['$=~'](/^(\d+)SR([+-]?\d+)?(>=(\d+))?$/i))) {
            return result
          };
          diceCount = $$('Regexp').$last_match(1).$to_i();
          modify = $$('Regexp').$last_match(2).$to_i();
          if ($truthy($$('Regexp').$last_match(4))) {
            difficulty = $$('Regexp').$last_match(4).$to_i()
          };
          diceList = self.randomizer.$roll_barabara(diceCount, 6).$sort();
          dice = diceList.$sum();
          totalValue = $rb_plus(dice, modify);
          modifyText = self.$getModifyText(modify);
          result['$text=']($rb_plus(result.$text(), "(" + (command) + ") ＞ " + (dice) + "[" + (diceList.$join(",")) + "]" + (modifyText) + " ＞ " + (totalValue)));
          criticalResult = self.$getCriticalResult(diceList);
          if (!$truthy(criticalResult['$nil?']())) {
            
            result['$critical='](true);
            result['$success='](true);
            result['$text=']($rb_plus(result.$text(), " ＞ クリティカル(+" + (criticalResult) + "D6)"));
            return result;
          };
          if ($truthy(self.$isFumble(diceList, diceCount))) {
            
            result['$fumble='](true);
            result['$failure='](true);
            result['$text=']($rb_plus(result.$text(), " ＞ ファンブル"));
            return result;
          };
          if (!$truthy(difficulty['$nil?']())) {
            if ($truthy($rb_ge(totalValue, difficulty))) {
              
              result['$success='](true);
              result['$text=']($rb_plus(result.$text(), " ＞ 成功"));
            } else {
              
              result['$failure='](true);
              result['$text=']($rb_plus(result.$text(), " ＞ 失敗"));
            }
          };
          return result;
        });
        
        $def(self, '$getModifyText', function $$getModifyText(modify) {
          
          
          if ($eqeq(modify, 0)) {
            return ""
          };
          if ($truthy($rb_lt(modify, 0))) {
            return modify.$to_s()
          };
          return "+" + (modify);
        });
        
        $def(self, '$getCriticalResult', function $$getCriticalResult(diceList) {
          var dice6Count = nil;

          
          dice6Count = $send(diceList, 'select', [], function $$1(i){
            
            if (i == null) i = nil;
            return i['$=='](6);}).$size();
          if ($truthy($rb_ge(dice6Count, 2))) {
            return dice6Count.$to_s()
          };
          return nil;
        });
        return $def(self, '$isFumble', function $$isFumble(diceList, diceCount) {
          
          return $rb_ge($send(diceList, 'select', [], function $$2(i){
            
            if (i == null) i = nil;
            return i['$=='](1);}).$size(), diceCount)
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
