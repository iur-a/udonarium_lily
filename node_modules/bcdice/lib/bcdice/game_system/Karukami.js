require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $hash2 = Opal.hash2, $truthy = Opal.truthy, $rb_le = Opal.rb_le, $rb_gt = Opal.rb_gt, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_plus = Opal.rb_plus, $to_a = Opal.to_a, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Karukami');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Karukami");
        $const_set($nesting[0], 'NAME', "カルカミ");
        $const_set($nesting[0], 'SORT_KEY', "かるかみ");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 行為判定、ダメージ算出 (xUB+y@c>=t)\n" + "  6面ダイスをx個ダイスロールし、クリティカル値以上の出目が出たら振り足して合計値を算出します。\n" + "  x: ダイス数\n" + "  y: 修正値（省略可）\n" + "  c: クリティカル値（省略可）\n" + "  t: 目標値値（省略可）\n" + "  例）2UB, 2UB>=7, 3UB+1@5, 3UB+1@5<10\n");
        self.$register_prefix("\\d+UB");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$roll_ub(command)
        });
        return $def(self, '$roll_ub', function $$roll_ub(command) {
          var self = this, parser = nil, parsed = nil, critical = nil, $ret_or_1 = nil, list_list = nil, criticals = nil, stack = nil, dice_list = nil, total = nil, result = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("UB", $hash2(["round_type"], {"round_type": self.round_type})).$has_prefix_number().$enable_critical();
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          critical = ($truthy(($ret_or_1 = parsed.$critical())) ? ($ret_or_1) : (6));
          if ($truthy($rb_le(critical, 1))) {
            return "(" + (parsed) + ") ＞ クリティカル値は2以上としてください"
          };
          list_list = [];
          criticals = 0;
          stack = parsed.$prefix_number();
          while ($truthy($rb_gt(stack, 0))) {
          
            dice_list = self.randomizer.$roll_barabara(stack, 6);
            list_list.$push(dice_list);
            stack = $send(dice_list, 'count', [], function $$1(x){
              
              if (x == null) x = nil;
              return $rb_ge(x, critical);});
            criticals = $rb_plus(criticals, stack);
          };
          total = $rb_plus(list_list.$flatten().$sum(), parsed.$modify_number());
          result = ($truthy(list_list.$first()['$all?'](1)) ? (((total = 0), $$('Result').$fumble("ファンブル"))) : ($truthy(parsed.$cmp_op()['$nil?']()) ? ($$('Result').$new()) : ($truthy(total.$send(parsed.$cmp_op(), parsed.$target_number())) ? ($$('Result').$success("成功")) : ($$('Result').$failure("失敗")))));
          result['$critical=']($rb_gt(criticals, 0));
          sequence = ["(" + (parsed) + ")"].concat($to_a($send(list_list, 'map', [], function $$2(list){
            
            if (list == null) list = nil;
            return "[" + (list.$join(",")) + "]";}))).concat([total, ($truthy(result['$critical?']()) ? ("" + (criticals) + "クリティカル") : nil), result.$text()]).$compact();
          result['$text='](sequence.$join(" ＞ "));
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
