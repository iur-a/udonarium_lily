require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $send = Opal.send, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'OrgaRain');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "OrgaRain");
        $const_set($nesting[0], 'NAME', "在りて遍くオルガレイン");
        $const_set($nesting[0], 'SORT_KEY', "ありてあまねくおるかれいん");
        $const_set($nesting[0], 'HELP_MESSAGE', "判定：[n]OR(count)\n" + "\n" + "[]内のコマンドは省略可能。\n" + "「n」でダイス数を指定。省略時は「1」。\n" + "(count)で命数を指定。「3111」のように記述。最大6つ。順不同可。\n" + "\n" + "【書式例】\n" + "・5OR6042 → 5dで命数「0,2,4,6」の判定\n" + "・6OR33333 → 6dで命数「3,3,3,3,3」の判定。\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        self.$register_prefix("(\\d+)?OR(\\d{0,6})?");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, m = nil, dice_count = nil, $ret_or_1 = nil, count_no = nil;

          
          m = command.$match(/(\d+)?OR(\d{0,6})$/i);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = ($truthy(($ret_or_1 = m['$[]'](1))) ? ($ret_or_1) : (1)).$to_i();
          count_no = $send(($truthy(($ret_or_1 = m['$[]'](2))) ? ($ret_or_1) : ("")).$each_char(), 'map', [], "to_i".$to_proc()).$sort();
          return self.$check_roll(dice_count, count_no);
        });
        return $def(self, '$check_roll', function $$check_roll(dice_count, count_no) {
          var self = this, dice_array = nil, dice_text = nil, result_array = nil, success = nil, count_text = nil, result_text = nil;

          
          dice_array = self.randomizer.$roll_barabara(dice_count, 10).$sort();
          dice_text = dice_array.$join(",");
          result_array = [];
          success = 0;
          $send($send(dice_array, 'map', [], function $$1(x){
            
            if (x == null) x = nil;
            if ($eqeq(x, 10)) {
              return 0
            } else {
              return x
            };}), 'each', [], function $$2(i){var multiple = nil;

            
            if (i == null) i = nil;
            multiple = count_no.$count(i);
            if ($truthy($rb_gt(multiple, 0))) {
              
              result_array.$push("" + (i) + "(x" + (multiple) + ")");
              return (success = $rb_plus(success, multiple));
            } else {
              return result_array.$push("×")
            };});
          count_text = count_no.$join(",");
          result_text = result_array.$join(",");
          return "" + (dice_count) + "D10(命数：" + (count_text) + ") ＞ " + (dice_text) + " ＞ " + (result_text) + " ＞ 成功数：" + (success);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
