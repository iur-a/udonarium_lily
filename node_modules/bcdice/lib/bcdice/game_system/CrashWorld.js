require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $not = Opal.not, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'CrashWorld');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "CrashWorld");
        $const_set($nesting[0], 'NAME', "墜落世界");
        $const_set($nesting[0], 'SORT_KEY', "ついらくせかい");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定 CWn\n" + "初期目標値n (必須)\n" + "例・CW8\n");
        self.$register_prefix("CW");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, result = nil, $ret_or_1 = nil;

          
          result = nil;
          if ($eqeqeq(/CW(\d+)/i, ($ret_or_1 = command))) {
            result = self.$getCrashWorldRoll($$('Regexp').$last_match(1).$to_i())
          } else {
            nil
          };
          return result;
        });
        return $def(self, '$getCrashWorldRoll', function $$getCrashWorldRoll(target) {
          var self = this, output = nil, isEnd = nil, successness = nil, num = nil;

          
          self.$debug("target", target);
          output = "(";
          isEnd = false;
          successness = 0;
          num = 0;
          while ($not(isEnd)) {
          
            num = self.randomizer.$roll_once(12);
            if ($eqeq(output, "(")) {
              output = "(" + (num)
            } else {
              output = "" + (output) + ", " + (num)
            };
            if (($truthy($rb_le(num, target)) || ($eqeq(num, 11)))) {
              
              target = num;
              successness = $rb_plus(successness, 1);
            } else if ($eqeq(num, 12)) {
              isEnd = true
            } else {
              isEnd = true
            };
          };
          if ($eqeq(num, 12)) {
            successness = 0
          };
          output = "" + (output) + ")  成功度 : " + (successness);
          if ($eqeq(num, 12)) {
            output = "" + (output) + " ファンブル"
          };
          return output;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
