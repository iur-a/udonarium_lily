require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $send = Opal.send, $rb_plus = Opal.rb_plus, $hash2 = Opal.hash2, $eqeqeq = Opal.eqeqeq, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_minus = Opal.rb_minus, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Fiasco');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Fiasco");
        $const_set($nesting[0], 'NAME', "フィアスコ");
        $const_set($nesting[0], 'SORT_KEY', "ふいあすこ");
        $const_set($nesting[0], 'HELP_MESSAGE', "  ・判定コマンド(FSx, WxBx)\n" + "    相関図・転落要素用(FSx)：相関図や転落要素のためにx個ダイスを振り、出目ごとに分類する\n" + "    黒白差分判定用(WxBx)  ：転落、残響のために白ダイス(W指定)と黒ダイス(B指定)で差分を求める\n" + "      ※ WとBは片方指定(Bx, Wx)、入替指定(WxBx,BxWx)可能\n");
        self.$register_prefix("FS", "W", "B");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$roll_fs(command))) ? ($ret_or_2) : (self.$roll_white_black(command)))))) {
            return $ret_or_1
          } else {
            return self.$roll_white_black_single(command)
          }
        });
        self.$private();
        
        $def(self, '$roll_fs', function $$roll_fs(command) {
          var self = this, m = nil, dice_count = nil, dice_list = nil, bucket = nil;

          
          m = /^FS(\d+)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = m['$[]'](1).$to_i();
          dice_list = self.randomizer.$roll_barabara(dice_count, 6);
          bucket = [nil, 0, 0, 0, 0, 0, 0];
          $send(dice_list, 'each', [], function $$1(val){var $a;

            
            if (val == null) val = nil;
            return ($a = [val, $rb_plus(bucket['$[]'](val), 1)], $send(bucket, '[]=', $a), $a[$a.length - 1]);});
          $send(bucket, 'map!', [], function $$2(count){var self = $$2.$$s == null ? this : $$2.$$s;

            
            if (count == null) count = nil;
            return self.$translate("Fiasco.fs.count", $hash2(["count"], {"count": count}));}, {$$s: self});
          return "1 => " + (bucket['$[]'](1)) + ", 2 => " + (bucket['$[]'](2)) + ", 3 => " + (bucket['$[]'](3)) + ", 4 => " + (bucket['$[]'](4)) + ", 5 => " + (bucket['$[]'](5)) + ", 6 => " + (bucket['$[]'](6));
        });
        
        $def(self, '$roll_white_black_single', function $$roll_white_black_single(command) {
          var self = this, m = nil, a = nil, result = nil;

          
          m = /^([WB])(\d+)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          a = $$('Side').$new(self.$color(m['$[]'](1)), m['$[]'](2).$to_i());
          result = a.$roll(self.randomizer);
          return "" + (result) + " ＞ " + (a.$color()) + (a.$total());
        });
        
        $def(self, '$roll_white_black', function $$roll_white_black(command) {
          var self = this, m = nil, $ret_or_1 = nil, a = nil, result_a = nil, b = nil, result_b = nil;

          
          m = /^([WB])(\d+)([WB])(\d+)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          if ($eqeqeq(/^W\d+W\d+$/, ($ret_or_1 = command))) {
            return "" + (command) + "：" + (self.$translate("Fiasco.wb.duplicate_error.white"))
          } else if ($eqeqeq(/^B\d+B\d+$/, $ret_or_1)) {
            return "" + (command) + "：" + (self.$translate("Fiasco.wb.duplicate_error.black"))
          } else {
            nil
          };
          a = $$('Side').$new(self.$color(m['$[]'](1)), m['$[]'](2).$to_i());
          result_a = a.$roll(self.randomizer);
          b = $$('Side').$new(self.$color(m['$[]'](3)), m['$[]'](4).$to_i());
          result_b = b.$roll(self.randomizer);
          return "" + (result_a) + " " + (result_b) + " ＞ " + (a.$diff(b));
        });
        
        $def(self, '$color', function $$color(c) {
          var self = this;

          if ($eqeq(c, "W")) {
            return self.$translate("Fiasco.white")
          } else {
            return self.$translate("Fiasco.black")
          }
        });
        return (function($base, $super) {
          var self = $klass($base, $super, 'Side');

          var $proto = self.$$prototype;

          $proto.count = $proto.dice_list = $proto.color = $proto.total = nil;
          
          
          $def(self, '$initialize', function $$initialize(color, count) {
            var self = this;

            
            self.color = color;
            return (self.count = count);
          });
          
          $def(self, '$roll', function $$roll(randomizer) {
            var self = this;

            
            self.dice_list = ($eqeq(self.count, 0) ? ([0]) : (randomizer.$roll_barabara(self.count, 6)));
            self.total = self.dice_list.$sum();
            return "" + (self.color) + (self.total) + "[" + (self.dice_list.$join(",")) + "]";
          });
          
          $def(self, '$diff', function $$diff(other) {
            var self = this;

            if ($eqeq(self.total, other.$total())) {
              return "0"
            } else if ($truthy($rb_gt(self.total, other.$total()))) {
              return "" + (self.color) + ($rb_minus(self.total, other.$total()))
            } else {
              return "" + (other.$color()) + ($rb_minus(other.$total(), self.total))
            }
          });
          return self.$attr_reader("color", "total");
        })($nesting[0], null);
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
