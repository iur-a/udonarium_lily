require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $send = Opal.send, $rb_le = Opal.rb_le, $rb_minus = Opal.rb_minus, $rb_plus = Opal.rb_plus, $hash2 = Opal.hash2, $truthy = Opal.truthy, $rb_lt = Opal.rb_lt, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Emoklore');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Emoklore");
        $const_set($nesting[0], 'NAME', "エモクロアTRPG");
        $const_set($nesting[0], 'SORT_KEY', "えもくろあTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・技能値判定（xDM<=y）\n" + "  \"(個数)DM<=(判定値)\"で指定します。\n" + "  ダイスの個数は省略可能で、省略した場合1個になります。\n" + "  例）2DM<=5 DM<=8\n" + "・技能値判定（sDAa+z)\n" + "  \"(技能レベル)DA(能力値)+(ダイスボーナス)\"で指定します。\n" + "  ダイスボーナスの個数は省略可能で、省略した場合0になります。\n" + "  技能レベルは1～3の数値、またはベース技能の場合\"b\"が入ります。\n" + "  ダイスの個数は技能レベルとダイスボーナスの個数により決定し、s+z個のダイスを振ります。（s=\"b\"の場合はs=1）\n" + "  判定値はs+aとなります。（s=\"b\"の場合はs=0）\n");
        self.$register_prefix("\\d*DM<=", "(B|\\d*)DA");
        $const_set($nesting[0], 'CRITICAL_VALUE', 1);
        $const_set($nesting[0], 'FUMBLE_VALUE', 10);
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($eqeqeq(/^\d*DM<=\d/, ($ret_or_1 = command))) {
            return self.$roll_dm(command)
          } else if ($eqeqeq(/^(B|\d*)DA\d+(\+)?\d*/, $ret_or_1)) {
            return self.$roll_da(command)
          } else {
            return nil
          }
        });
        self.$private();
        
        $def(self, '$dice_roll', function $$dice_roll(num_dice, success_threshold) {
          var self = this, values = nil, critical = nil, success = nil, fumble = nil, success_value = nil, result = nil;

          
          values = self.randomizer.$roll_barabara(num_dice, 10);
          critical = values.$count($$('CRITICAL_VALUE'));
          success = $send(values, 'count', [], function $$1(num){
            
            if (num == null) num = nil;
            return $rb_le(num, success_threshold);});
          fumble = values.$count($$('FUMBLE_VALUE'));
          success_value = $rb_minus($rb_plus(critical, success), fumble);
          result = self.$compare_result(success_value);
          result['$text=']("[" + (values.$join(", ")) + "] ＞ " + (success_value) + " ＞ " + (self.$translate("Emoklore.success_count", $hash2(["count"], {"count": success_value}))) + " " + (result.$text()));
          return result;
        });
        
        $def(self, '$compare_result', function $$compare_result(success) {
          var self = this;

          if ($truthy($rb_lt(success, 0))) {
            return $$('Result').$fumble(self.$translate("fumble"))
          } else if ($eqeq(success, 0)) {
            return $$('Result').$failure(self.$translate("failure"))
          } else if ($eqeq(success, 1)) {
            return $$('Result').$success(self.$translate("success"))
          } else if ($eqeq(success, 2)) {
            return $$('Result').$critical(self.$translate("Emoklore.double"))
          } else if ($eqeq(success, 3)) {
            return $$('Result').$critical(self.$translate("Emoklore.triple"))
          } else if ($truthy($rb_le(success, 9))) {
            return $$('Result').$critical(self.$translate("Emoklore.miracle"))
          } else {
            return $$('Result').$critical(self.$translate("Emoklore.catastrophe"))
          }
        });
        
        $def(self, '$roll_dm', function $$roll_dm(command) {
          var $a, self = this, m = nil, num_dice = nil, $ret_or_1 = nil, success_threshold = nil, result = nil;

          
          m = /^(\d+)?DM<=(\d+)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num_dice = ($truthy(($ret_or_1 = ($a = m['$[]'](1), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (1));
          success_threshold = m['$[]'](2).$to_i();
          if ($truthy($rb_le(num_dice, 0))) {
            return nil
          };
          result = self.$dice_roll(num_dice, success_threshold);
          result['$text=']("(" + (num_dice) + "DM<=" + (success_threshold) + ") ＞ " + (result.$text()));
          return result;
        });
        return $def(self, '$roll_da', function $$roll_da(command) {
          var $a, self = this, m = nil, bonus = nil, num_dice = nil, $ret_or_1 = nil, success_threshold = nil, result = nil;

          
          m = /^(B|\d+)?DA(\d+)(\+\d+)?$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          bonus = m['$[]'](3).$to_i();
          num_dice = $rb_plus(($eqeq(m['$[]'](1), "B") ? (1) : (($truthy(($ret_or_1 = ($a = m['$[]'](1), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (1)))), bonus);
          success_threshold = $rb_plus(m['$[]'](1).$to_i(), m['$[]'](2).$to_i());
          result = self.$dice_roll(num_dice, success_threshold);
          result['$text=']("(" + (command) + ") ＞ (" + (num_dice) + "DM<=" + (success_threshold) + ") ＞ " + (result.$text()));
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
