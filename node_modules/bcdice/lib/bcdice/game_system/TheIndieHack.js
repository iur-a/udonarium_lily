require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $rb_minus = Opal.rb_minus, $rb_lt = Opal.rb_lt, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $send = Opal.send, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'TheIndieHack');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "TheIndieHack");
        $const_set($nesting[0], 'NAME', "The Indie Hack");
        $const_set($nesting[0], 'SORT_KEY', "しいんていはつく");
        $const_set($nesting[0], 'HELP_MESSAGE', "■判定　cIH+a        c:CL  a:能力値\n" + "\n" + "例)IH: ライトダイスとダークダイスを1個ずつ振って、その結果を表示\n");
        self.$register_prefix("([+-]?\\d)?IH");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$resolute_action(command)
        });
        self.$private();
        
        $def(self, '$resolute_action', function $$resolute_action(command) {
          var self = this, m = nil, cl = nil, abilities = nil, dices = nil, dice_text = nil, dice_text2 = nil, diff = nil, side = nil, output = nil;

          
          m = /([+-]?\d)?IH([+-]\d)?/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          cl = m['$[]'](1).$to_i();
          abilities = m['$[]'](2).$to_i();
          dices = self.randomizer.$roll_barabara(2, 6);
          dice_text = dices.$join(",");
          dices['$[]='](0, $rb_plus(dices['$[]'](0), cl));
          dices['$[]='](1, $rb_plus(dices['$[]'](1), abilities));
          dice_text2 = dices.$join(",");
          diff = $rb_minus(dices['$[]'](1), dices['$[]'](0));
          side = ($truthy($rb_lt(diff, 0)) ? ("ライト") : ("ダーク"));
          if ($eqeq(diff, 0)) {
            side = "両"
          };
          if ($eqeq(dice_text, dice_text2)) {
            output = "(IH) ＞ " + (dice_text) + " ＞ " + (side) + (self.$get_success_level(diff))
          } else {
            output = "(" + (m['$[]'](1)) + "IH" + (m['$[]'](2)) + ") ＞ [" + (dice_text) + "] ＞ " + (dice_text2) + " ＞ " + (side) + (self.$get_success_level(diff))
          };
          if ($truthy($rb_gt(diff, 0))) {
            return $$('Result').$success(output)
          } else if ($truthy($rb_lt(diff, 0))) {
            return $$('Result').$failure(output)
          } else {
            return $send($$('Result').$new(), 'tap', [], function $$1(result){var $a;

              
              if (result == null) result = nil;
              return ($a = [output], $send(result, 'text=', $a), $a[$a.length - 1]);})
          };
        });
        return $def(self, '$get_success_level', function $$get_success_level(die_difference) {
          
          
          switch (die_difference.$abs().valueOf()) {
            case 0:
              return "陣営がそれぞれ確定描写を1つ追加します"
            case 1:
              return "陣営が確定描写を1つ追加しますが、味方によって追加されたネガティブな確定描写を1つ受けます"
            case 2:
              return "陣営が確定描写を1つ追加します"
            case 3:
              return "陣営が確定描写を1つ追加し、さらに場面描写を1つ追加します"
            case 4:
              return "陣営が確定描写を1つ追加し、さらにその陣営の味方ひとりも確定描写を1つ追加します"
            default:
              return "陣営が確定描写を2つ追加します"
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
