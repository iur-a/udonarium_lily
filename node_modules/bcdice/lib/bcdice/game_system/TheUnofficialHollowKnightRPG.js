require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_lt = Opal.rb_lt, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_minus = Opal.rb_minus, $thrower = Opal.thrower, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'TheUnofficialHollowKnightRPG');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "TheUnofficialHollowKnightRPG");
        $const_set($nesting[0], 'NAME', "The Unofficial Hollow Knight RPG");
        $const_set($nesting[0], 'SORT_KEY', "しあんおふいしやるほろうないとRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・能力値判定　[n]AD[+b][#r][>=t]\n" + "　n: 能力値。小数可。省略不可。\n" + "　b: ボーナス、ペナルティダイス。省略可。\n" + "　r: 追加リロールダイス数。省略可。\n" + "　t: 目標値。>=含めて省略可。\n" + "　成功数を判定。\n" + "　例）1AD, 2.5AD, 1.5AD+1, 2AD#1, 2.5AD+2#2>=4\n" + "\n" + "・イニシアチブ　[n]INTI[+b][#r]\n" + "　n: イニシアチブに使う能力値。省略不可。\n" + "  b: ボーナス、ペナルティダイス。省略可。\n" + "  r: 追加リロールダイス数。省略可。\n" + "　振り直しを行ったうえでイニシアチブ値を計算。\n" + "　例）1INTI, 2.5INTI, 1.5INTI+1, 2INTI#1, 2.5INTI+2#2\n");
        self.$register_prefix("(\\d+\\.?\\d*)?AD([+-](\\d+))?(#(\\d+))?(>=(\\d+))?", "(\\d+\\.?\\d*)?(INTI|inti)([+-](\\d+))?(#(\\d+))?");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = false);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = self.$ability_roll(command)))) {
            return $ret_or_1
          } else {
            return self.$initiative_roll(command)
          }
        });
        
        $def(self, '$number_with_sign_from_int', function $$number_with_sign_from_int(number) {
          
          if ($eqeq(number, 0)) {
            return ""
          } else if ($truthy($rb_gt(number, 0))) {
            return "+" + (number.$abs())
          } else if ($truthy($rb_lt(number, 0))) {
            return "-" + (number.$abs())
          } else {
            return number.$to_s()
          }
        });
        
        $def(self, '$number_with_reroll_from_int', function $$number_with_reroll_from_int(number) {
          
          if ($eqeq(number, 0)) {
            return ""
          } else if ($truthy($rb_gt(number, 0))) {
            return "#" + (number)
          } else {
            return number.$to_s()
          }
        });
        
        $def(self, '$ability_roll', function $$ability_roll(command) {
          var self = this, m = nil, num_of_die = nil, bonus = nil, reroll = nil, difficulty = nil, dice_command = nil, values = nil, result = nil, failed_roll = nil, rolled_text = nil, reroll_values = nil, reroll_result = nil;

          
          m = /^(\d+\.?\d*)?AD([+-](\d+))?(#(\d*))?(>=(\d+))?/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          num_of_die = m['$[]'](1).$to_f();
          bonus = m['$[]'](3).$to_i();
          reroll = m['$[]'](5).$to_i();
          difficulty = m['$[]'](7).$to_i();
          if ($truthy(/\.[1-9]+/['$=~'](num_of_die.$to_s()))) {
            
            dice_command = "" + (num_of_die) + "AD" + (self.$number_with_sign_from_int(bonus)) + (self.$number_with_reroll_from_int(reroll));
            reroll = $rb_plus(reroll, 1);
          } else {
            dice_command = "" + (num_of_die.$to_i()) + "AD" + (self.$number_with_sign_from_int(bonus)) + (self.$number_with_reroll_from_int(reroll))
          };
          if ($eqeq(difficulty, 0)) {
            difficulty = 5
          } else {
            dice_command = $rb_plus(dice_command, ">=" + (difficulty))
          };
          values = self.randomizer.$roll_barabara($rb_plus(num_of_die.$to_i(), bonus), 6);
          result = $send(values, 'count', [], function $$1(num){
            
            if (num == null) num = nil;
            return $rb_ge(num, difficulty);});
          failed_roll = $rb_minus(num_of_die.$to_i(), result);
          rolled_text = $rb_plus($rb_plus("[", values.$join(",")), "]");
          reroll_values = [];
          if ($eqeq(reroll, 1)) {
            reroll_values.$push(self.randomizer.$roll_once(6))
          } else if ($truthy($rb_gt(reroll, 1))) {
            reroll_values = $rb_plus(reroll_values, self.randomizer.$roll_barabara(reroll, 6))
          };
          reroll_result = $send(reroll_values, 'count', [], function $$2(num){
            
            if (num == null) num = nil;
            return $rb_ge(num, difficulty);});
          if ($truthy($rb_lt(failed_roll, reroll_result))) {
            reroll_result = failed_roll
          };
          result = $rb_plus(result, reroll_result);
          if (!$truthy(reroll_values['$empty?']())) {
            rolled_text = $rb_plus(rolled_text, $rb_plus($rb_plus(" Reroll [", reroll_values.$join(",")), "]"))
          };
          return "(" + (dice_command) + ") > " + (rolled_text) + " > " + (result) + "成功";
        });
        return $def(self, '$initiative_roll', function $$initiative_roll(command) {
          var self = this, m = nil, grace = nil, bonus = nil, reroll = nil, dice_command = nil, values = nil, revalue = nil, result = nil, res_text = nil;

          
          m = /^(\d+\.?\d*)?(INTI|inti)([+-](\d+))?(#(\d+))?/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          grace = m['$[]'](1).$to_f();
          bonus = m['$[]'](3).$to_i();
          reroll = m['$[]'](6).$to_i();
          if ($truthy(/\.[1-9]+/['$=~'](grace.$to_s()))) {
            
            dice_command = "(" + (grace) + "INTI" + (self.$number_with_sign_from_int(bonus)) + (self.$number_with_reroll_from_int(reroll)) + ")";
            reroll = $rb_plus(reroll, 1);
          } else {
            dice_command = "(" + (grace.$to_i()) + "INTI" + (self.$number_with_sign_from_int(bonus)) + (self.$number_with_reroll_from_int(reroll)) + ")"
          };
          values = self.randomizer.$roll_barabara($rb_plus(grace, bonus), 6);
          revalue = [];
          if (!$eqeq(reroll, 0)) {
            revalue = self.randomizer.$roll_barabara(reroll, 6)
          };
          revalue = revalue.$sort();
          result = 0;
          res_text = "[";
          $send(values, 'each', [], function $$3(value){var is_min = nil, index = nil;

            
            if (value == null) value = nil;
            if ($truthy(revalue['$empty?']())) {
              
              res_text = $rb_plus(res_text, value.$to_s());
              result = $rb_plus(result, value);
            } else {
              
              is_min = false;
              index = -1;
              (function(){try { var $t_break = $thrower('break'); return $send(revalue, 'each', [], function $$4(re){
                
                if (re == null) re = nil;
                index = $rb_plus(index, 1);
                if (!$truthy($rb_gt(re, value))) {
                  return nil
                };
                res_text = $rb_plus(res_text, "" + (value) + "<<" + (re));
                result = $rb_plus(result, re);
                revalue.$delete_at(index);
                is_min = true;
                $t_break.$throw();})} catch($e) {
                if ($e === $t_break) return $e.$v;
                throw $e;
              }})();
              if (!$truthy(is_min)) {
                
                res_text = $rb_plus(res_text, value.$to_s());
                result = $rb_plus(result, value);
              };
            };
            return (res_text = $rb_plus(res_text, ","));});
          res_text = res_text.$chop();
          res_text = $rb_plus(res_text, "]");
          return "" + (dice_command) + " > " + (res_text) + " > " + (result);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
