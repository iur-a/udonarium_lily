require('./../opal');

Opal.modules["bcdice/game_system/Revulture"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_gt = Opal.rb_gt, $thrower = Opal.thrower, $hash2 = Opal.hash2, $rb_ge = Opal.rb_ge, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Revulture');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Revulture");
        $const_set($nesting[0], 'NAME', "光砕のリヴァルチャー");
        $const_set($nesting[0], 'SORT_KEY', "こうさいのりうあるちやあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "■アタック判定（ xAT, xATK, xATTACK ）\n" + "x: ダイス数（加算 + と除算 / を使用可能）\n" + "例） 3AT, 4ATK, 5+6ATTACK, 15/2AT\n" + "\n" + "□アタック判定　目標値つき（ xAT<=y, xATK<=y, xATTACK<=y ）\n" + "x: ダイス数（加算 + と除算 / を使用可能）\n" + "y: 目標値（ 1 以上 6 以下。加算 + を使用可能）\n" + "例） 3AT<=4, 3AT<=2+1\n" + "\n" + "□アタック判定　目標値＆追加ダメージつき（ xAT<=y[>=a:+b], xATK<=y[>=a:+b], xATTACK<=y[z] ）\n" + "x: ダイス数（加算 + と除算 / を使用可能）\n" + "y: 目標値（ 1 以上 6 以下。加算 + を使用可能）\n" + "z: 追加ダメージの規則（詳細は後述）（※複数同時に指定可能）\n" + "\n" + "▽追加ダメージの規則 [a:+b]\n" + "a: ヒット数が a なら\n" + "　=a　（ヒット数が a に等しい）\n" + "　>=a　（ヒット数が a 以上）\n" + "b: ダメージを b 点追加\n" + "\n" + "例） 3AT<=4[>=2:+3] #ルールブックp056「グレングラントAR」\n" + "例） 2AT<=4[=1:+5][>=2:+8] #ルールブックp067「ファーボル・ドラゴンブレス」\n");
        $const_set($nesting[0], 'ATTACK_ROLL_REG', /^(\d+([+\/]\d+)*)?AT(TACK|K)?(<=([1-6](\+\d)*))?((\[>?=\d+:\+\d+\])+)?/i.$freeze());
        self.$register_prefix("\\d+([+\\/]\\d+)*AT");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, m = nil;

          if ($truthy((m = $$('ATTACK_ROLL_REG').$match(command)))) {
            return self.$roll_attack(m['$[]'](1), m['$[]'](5), m['$[]'](7))
          } else {
            return nil
          }
        });
        self.$private();
        
        $def(self, '$roll_attack', function $$roll_attack(dice_count_expression, border_expression, additional_damage_rules) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, dice_count = nil, border = nil, command = nil, dices = nil, critical_hit_count = nil, hit_count = nil, damage = nil, message_elements = nil;

          
          dice_count = $$('Arithmetic').$eval(dice_count_expression, $$$($$('RoundType'), 'FLOOR'));
          if ($truthy(border_expression)) {
            border = $$('Arithmetic').$eval(border_expression, $$$($$('RoundType'), 'FLOOR')).$clamp(1, 6)
          };
          command = self.$make_command_text(dice_count, border, additional_damage_rules);
          if ($truthy($rb_le(dice_count, 0))) {
            return "" + (command) + " ＞ ダイス数が 0 です"
          } else if (($truthy(border['$nil?']()) && ($truthy(additional_damage_rules)))) {
            return "" + (command) + " ＞ 目標値が指定されていないため、追加ダメージを算出できません"
          };
          dices = self.randomizer.$roll_barabara(dice_count, 6).$sort();
          critical_hit_count = dices.$count(1);
          if ($truthy(border)) {
            hit_count = $rb_plus($send(dices, 'count', [], function $$1(dice){
              
              if (dice == null) dice = nil;
              return $rb_le(dice, border);}), critical_hit_count)
          };
          damage = self.$calc_damage(hit_count, additional_damage_rules);
          message_elements = [];
          message_elements['$<<'](command);
          message_elements['$<<'](dices.$join(","));
          if ($truthy($rb_gt(critical_hit_count, 0))) {
            message_elements['$<<']("クリティカル " + (critical_hit_count))
          };
          if ($truthy(hit_count)) {
            message_elements['$<<']("ヒット数 " + (hit_count))
          };
          if ($truthy(damage)) {
            message_elements['$<<']("ダメージ " + (damage))
          };
          return $send($$('Result').$new(message_elements.$join(" ＞ ")), 'tap', [], function $$2(r){var $a;

            
            if (r == null) r = nil;
            if ($truthy(hit_count)) {
              r['$condition=']($rb_gt(hit_count, 0))
            };
            return ($a = [$rb_gt(critical_hit_count, 0)], $send(r, 'critical=', $a), $a[$a.length - 1]);});} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$make_command_text', function $$make_command_text(dice_count, border, additional_damage_rules) {
          var command = nil;

          
          command = "" + (dice_count) + "attack";
          if ($truthy(border)) {
            command = $rb_plus(command, "<=" + (border))
          };
          if ($truthy(additional_damage_rules)) {
            command = $rb_plus(command, additional_damage_rules)
          };
          return "(" + (command) + ")";
        });
        
        $def(self, '$calc_damage', function $$calc_damage(hit_count, additional_damage_rules) {
          var self = this, damage = nil;

          
          if (!$truthy(additional_damage_rules)) {
            return nil
          };
          damage = hit_count;
          $send(self.$parse_additional_damage_rules(additional_damage_rules), 'each', [], function $$3(rule){
            
            if (rule == null) rule = nil;
            if ($truthy(rule['$[]']("condition").$call(hit_count))) {
              return (damage = $rb_plus(damage, rule['$[]']("additinal_damage")))
            } else {
              return nil
            };});
          return damage;
        });
        
        $def(self, '$parse_additional_damage_rules', function $$parse_additional_damage_rules(source) {
          var self = this;

          return $send(source.$scan(/\[(>?=)(\d+):\+(\d+)\]/), 'map', [], function $$4(matched){var self = $$4.$$s == null ? this : $$4.$$s;

            
            if (matched == null) matched = nil;
            return $hash2(["condition", "additinal_damage"], {"condition": self.$make_additional_damage_condition(matched['$[]'](0), matched['$[]'](1).$to_i()), "additinal_damage": matched['$[]'](2).$to_i()});}, {$$s: self})
        });
        return $def(self, '$make_additional_damage_condition', function $$make_additional_damage_condition(comparer, comparing_target) {
          var self = this;

          
          switch (comparer.valueOf()) {
            case "=":
              return $send(self, 'lambda', [], function $$5(hit_count){
                
                if (hit_count == null) hit_count = nil;
                return hit_count['$=='](comparing_target);})
            case ">=":
              return $send(self, 'lambda', [], function $$6(hit_count){
                
                if (hit_count == null) hit_count = nil;
                return $rb_ge(hit_count, comparing_target);})
            default:
              return nil
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_gt = Opal.rb_gt, $thrower = Opal.thrower, $hash2 = Opal.hash2, $rb_ge = Opal.rb_ge, self = Opal.top, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  
  self.$require("bcdice/game_system/Revulture");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Revulture_Korean');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Revulture:Korean");
        $const_set($nesting[0], 'NAME', "광쇄의 리벌처");
        $const_set($nesting[0], 'SORT_KEY', "国際化:Korean:광쇄의 리벌처");
        $const_set($nesting[0], 'HELP_MESSAGE', "■공격 판정（ xAT, xATK, xATTACK ）\n" + "x: 주사위 수（덧셈 + 과 나눗셈 / 사용 가능）\n" + "예시） 3AT, 4ATK, 5+6ATTACK, 15/2AT\n" + "\n" + "□공격 판정 목표값 포함（ xAT<=y, xATK<=y, xATTACK<=y ）\n" + "x: 주사위 수（덧셈 + 과 나눗셈 / 사용 가능）\n" + "y: 목표값（ 1 이상 6 이하. 덧셈 + 사용 가능）\n" + "예시） 3AT<=4, 3AT<=2+1\n" + "\n" + "□공격 판정　목표값＆추가 대미지 포함（ xAT<=y[>=a:+b], xATK<=y[>=a:+b], xATTACK<=y[z] ）\n" + "x: 주사위 수（덧셈 + 과 나눗셈 / 사용 가능）\n" + "y: 목표값（ 1 이상 6 이하. 덧셈 + 사용 가능）\n" + "z: 추가 대미지 규칙（자세한 내용은 후술）（※여러 개를 동시에 지정 가능）\n" + "\n" + "▽추가 대미지 규칙 [a:+b]\n" + "a: 히트 수가 a 라면\n" + "　=a　（히트 수가 a와 동일）\n" + "　>=a　（히트 수가 a 이상）\n" + "b: 대미지를 b 점 추가\n" + "\n" + "예시） 3AT<=4[>=2:+3] #ルールブックp056「グレングラントAR」\n" + "예시） 2AT<=4[=1:+5][>=2:+8] #ルールブックp067「ファーボル・ドラゴンブレス」\n");
        $const_set($nesting[0], 'ATTACK_ROLL_REG', /^(\d+([+\/]\d+)*)?AT(TACK|K)?(<=([1-6](\+\d)*))?((\[>?=\d+:\+\d+\])+)?/i.$freeze());
        self.$register_prefix("\\d+([+\\/]\\d+)*AT");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, m = nil;

          if ($truthy((m = $$('ATTACK_ROLL_REG').$match(command)))) {
            return self.$roll_attack(m['$[]'](1), m['$[]'](5), m['$[]'](7))
          } else {
            return nil
          }
        });
        self.$private();
        
        $def(self, '$roll_attack', function $$roll_attack(dice_count_expression, border_expression, additional_damage_rules) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, dice_count = nil, border = nil, command = nil, dices = nil, critical_hit_count = nil, hit_count = nil, damage = nil, message_elements = nil;

          
          dice_count = $$('Arithmetic').$eval(dice_count_expression, $$$($$('RoundType'), 'FLOOR'));
          if ($truthy(border_expression)) {
            border = $$('Arithmetic').$eval(border_expression, $$$($$('RoundType'), 'FLOOR')).$clamp(1, 6)
          };
          command = self.$make_command_text(dice_count, border, additional_damage_rules);
          if ($truthy($rb_le(dice_count, 0))) {
            return "" + (command) + " ＞ 주사위가 0개 입니다."
          } else if (($truthy(border['$nil?']()) && ($truthy(additional_damage_rules)))) {
            return "" + (command) + " ＞ 목표값이 지정되지 않아 추가 대미지를 계산할 수 없습니다."
          };
          dices = self.randomizer.$roll_barabara(dice_count, 6).$sort();
          critical_hit_count = dices.$count(1);
          if ($truthy(border)) {
            hit_count = $rb_plus($send(dices, 'count', [], function $$1(dice){
              
              if (dice == null) dice = nil;
              return $rb_le(dice, border);}), critical_hit_count)
          };
          damage = self.$calc_damage(hit_count, additional_damage_rules);
          message_elements = [];
          message_elements['$<<'](command);
          message_elements['$<<'](dices.$join(","));
          if ($truthy($rb_gt(critical_hit_count, 0))) {
            message_elements['$<<']("크리티컬 " + (critical_hit_count))
          };
          if ($truthy(hit_count)) {
            message_elements['$<<']("히트 수 " + (hit_count))
          };
          if ($truthy(damage)) {
            message_elements['$<<']("대미지 " + (damage))
          };
          return $send($$('Result').$new(message_elements.$join(" ＞ ")), 'tap', [], function $$2(r){var $a;

            
            if (r == null) r = nil;
            if ($truthy(hit_count)) {
              r['$condition=']($rb_gt(hit_count, 0))
            };
            return ($a = [$rb_gt(critical_hit_count, 0)], $send(r, 'critical=', $a), $a[$a.length - 1]);});} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$make_command_text', function $$make_command_text(dice_count, border, additional_damage_rules) {
          var command = nil;

          
          command = "" + (dice_count) + "attack";
          if ($truthy(border)) {
            command = $rb_plus(command, "<=" + (border))
          };
          if ($truthy(additional_damage_rules)) {
            command = $rb_plus(command, additional_damage_rules)
          };
          return "(" + (command) + ")";
        });
        
        $def(self, '$calc_damage', function $$calc_damage(hit_count, additional_damage_rules) {
          var self = this, damage = nil;

          
          if (!$truthy(additional_damage_rules)) {
            return nil
          };
          damage = hit_count;
          $send(self.$parse_additional_damage_rules(additional_damage_rules), 'each', [], function $$3(rule){
            
            if (rule == null) rule = nil;
            if ($truthy(rule['$[]']("condition").$call(hit_count))) {
              return (damage = $rb_plus(damage, rule['$[]']("additinal_damage")))
            } else {
              return nil
            };});
          return damage;
        });
        
        $def(self, '$parse_additional_damage_rules', function $$parse_additional_damage_rules(source) {
          var self = this;

          return $send(source.$scan(/\[(>?=)(\d+):\+(\d+)\]/), 'map', [], function $$4(matched){var self = $$4.$$s == null ? this : $$4.$$s;

            
            if (matched == null) matched = nil;
            return $hash2(["condition", "additinal_damage"], {"condition": self.$make_additional_damage_condition(matched['$[]'](0), matched['$[]'](1).$to_i()), "additinal_damage": matched['$[]'](2).$to_i()});}, {$$s: self})
        });
        return $def(self, '$make_additional_damage_condition', function $$make_additional_damage_condition(comparer, comparing_target) {
          var self = this;

          
          switch (comparer.valueOf()) {
            case "=":
              return $send(self, 'lambda', [], function $$5(hit_count){
                
                if (hit_count == null) hit_count = nil;
                return hit_count['$=='](comparing_target);})
            case ">=":
              return $send(self, 'lambda', [], function $$6(hit_count){
                
                if (hit_count == null) hit_count = nil;
                return $rb_ge(hit_count, comparing_target);})
            default:
              return nil
          }
        });
      })($nesting[0], $$('Revulture'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
