require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $hash2 = Opal.hash2, $not = Opal.not, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_divide = Opal.rb_divide, $rb_ge = Opal.rb_ge, $rb_le = Opal.rb_le, $def = Opal.def, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'RecordOfLodossWar');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "RecordOfLodossWar");
        $const_set($nesting[0], 'NAME', "ロードス島戦記RPG");
        $const_set($nesting[0], 'SORT_KEY', "ろおとすとうせんきRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "●判定\n" + "　LW<=(目標値)で判定。\n" + "　達成値が目標値の1/10(端数切り上げ)以下であれば大成功。1～10であれば自動成功。\n" + "　91～100であれば自動失敗となります。\n" + "\n" + "●回避判定\n" + "　LWD<=(目標値)で回避判定。この時出目が51以上で自動失敗となります。\n" + "\n" + "　判定と回避判定は、どちらもコマンドだけの場合、出目の表示と自動成功と自動失敗の判定のみを行います。\n");
        self.$register_prefix("LW");
        return $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, parser = nil, cmd = nil, auto_failure = nil, critical = nil, dice_value = nil, result = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("LWD", "LW", $hash2(["round_type"], {"round_type": self.$round_type()})).$restrict_cmp_op_to(nil, "<=");
          cmd = parser.$parse(command);
          if (($truthy(cmd['$nil?']()) || ($not([nil, "<="]['$include?'](cmd.$cmp_op()))))) {
            return nil
          };
          auto_failure = ($eqeq(cmd.$command(), "LWD") ? (51) : (91));
          critical = $rb_divide(cmd.$target_number().$to_f(), 10).$ceil();
          dice_value = self.randomizer.$roll_once(100);
          result = ($truthy($rb_ge(dice_value, auto_failure)) ? ("自動失敗(" + (auto_failure) + ")") : ($truthy($rb_le(dice_value, critical)) ? ("大成功(" + (critical) + ")") : ($truthy($rb_le(dice_value, 10)) ? ("自動成功") : ($truthy(cmd.$cmp_op()) ? (($truthy($rb_le(dice_value, cmd.$target_number())) ? ("成功") : ("失敗"))) : nil))));
          sequence = ["(1D100" + (cmd.$cmp_op()) + (cmd.$target_number()) + ")", dice_value.$to_s(), result].$compact();
          return sequence.$join(" ＞ ");
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
