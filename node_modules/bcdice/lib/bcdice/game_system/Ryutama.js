require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $eqeq = Opal.eqeq, $rb_plus = Opal.rb_plus, $thrower = Opal.thrower, $neqeq = Opal.neqeq, $rb_divide = Opal.rb_divide, $rb_ge = Opal.rb_ge, $rb_gt = Opal.rb_gt, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Ryutama');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.validDiceTypes = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Ryutama");
        $const_set($nesting[0], 'NAME', "りゅうたま");
        $const_set($nesting[0], 'SORT_KEY', "りゆうたま");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定\n" + "　Rx,y>=t（x,y：使用する能力値、t：目標値）\n" + "　1ゾロ、クリティカルも含めて判定結果を表示します\n" + "　能力値１つでの判定は Rx>=t で行えます\n" + "例）R8,6>=13\n");
        self.$register_prefix("R");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.validDiceTypes = [20, 12, 10, 8, 6, 4, 2]);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var $a, $b, self = this, dice1 = nil, dice2 = nil, modifyString = nil, difficulty = nil, $ret_or_1 = nil, modify = nil, value1 = nil, value2 = nil, total = nil, result = nil, value1Text = nil, value2Text = nil, modifyText = nil, baseText = nil, output = nil;

          
          self.$debug("eval_game_system_specific_command begin");
          if (!$truthy(command['$=~'](/^R(\d+)(,(\d+))?([+\-\d]+)?(>=(\d+))?/))) {
            
            self.$debug("unmatched!");
            return "";
          };
          self.$debug("matched");
          dice1 = $$('Regexp').$last_match(1).$to_i();
          dice2 = $$('Regexp').$last_match(3).$to_i();
          modifyString = $$('Regexp').$last_match(4);
          difficulty = $$('Regexp').$last_match(6);
          $b = self.$getDiceType(dice1, dice2), $a = $to_ary($b), (dice1 = ($a[0] == null ? nil : $a[0])), (dice2 = ($a[1] == null ? nil : $a[1])), $b;
          if ($eqeq(dice1, 0)) {
            return ""
          };
          modifyString = ($truthy(($ret_or_1 = modifyString)) ? ($ret_or_1) : (""));
          modify = $$('ArithmeticEvaluator').$eval(modifyString);
          difficulty = self.$getDiffculty(difficulty);
          value1 = self.$getRollValue(dice1);
          value2 = self.$getRollValue(dice2);
          total = $rb_plus($rb_plus(value1, value2), modify);
          result = self.$getResultText(value1, value2, dice1, dice2, difficulty, total);
          if (!$truthy(result['$empty?']())) {
            result = " ＞ " + (result)
          };
          value1Text = "" + (value1) + "(" + (dice1) + ")";
          value2Text = ($eqeq(value2, 0) ? ("") : ("+" + (value2) + "(" + (dice2) + ")"));
          modifyText = self.$getModifyString(modify);
          baseText = self.$getBaseText(dice1, dice2, modify, difficulty);
          output = "(" + (baseText) + ") ＞ " + (value1Text) + (value2Text) + (modifyText) + " ＞ " + (total) + (result);
          return output;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$getDiceType', function $$getDiceType(dice1, dice2) {
          var self = this, diceBase = nil;

          
          self.$debug("getDiceType begin");
          if ($neqeq(dice2, 0)) {
            if ($truthy(self.$isValidDiceOne(dice1))) {
              return [dice1, dice2]
            } else {
              return [0, 0]
            }
          };
          if ($truthy(self.$isValidDice(dice1, dice2))) {
            return [dice1, dice2]
          };
          diceBase = dice1;
          dice1 = $rb_divide(diceBase, 10).$to_i();
          dice2 = diceBase['$%'](10);
          if ($truthy(self.$isValidDice(dice1, dice2))) {
            return [dice1, dice2]
          };
          dice1 = $rb_divide(diceBase, 100).$to_i();
          dice2 = diceBase['$%'](100);
          if ($truthy(self.$isValidDice(dice1, dice2))) {
            return [dice1, dice2]
          };
          if ($truthy(self.$isValidDiceOne(diceBase))) {
            return [diceBase, 0]
          };
          return [0, 0];
        });
        
        $def(self, '$isValidDice', function $$isValidDice(dice1, dice2) {
          var self = this, $ret_or_1 = nil;

          return ($truthy(($ret_or_1 = self.$isValidDiceOne(dice1))) ? (self.$isValidDiceOne(dice2)) : ($ret_or_1))
        });
        
        $def(self, '$isValidDiceOne', function $$isValidDiceOne(dice) {
          var self = this;

          return self.validDiceTypes['$include?'](dice)
        });
        
        $def(self, '$getDiffculty', function $$getDiffculty(difficulty) {
          
          
          if (!$truthy(difficulty['$nil?']())) {
            difficulty = difficulty.$to_i()
          };
          return difficulty;
        });
        
        $def(self, '$getRollValue', function $$getRollValue(dice) {
          var self = this, value = nil;

          
          if ($eqeq(dice, 0)) {
            return 0
          };
          value = self.randomizer.$roll_once(dice);
          return value;
        });
        
        $def(self, '$getResultText', function $$getResultText(value1, value2, dice1, dice2, difficulty, total) {
          var self = this;

          
          if ($truthy(self.$isFamble(value1, value2))) {
            return "１ゾロ【１ゾロポイント＋１】"
          };
          if ($truthy(self.$isCritical(value1, value2, dice1, dice2))) {
            return "クリティカル成功"
          };
          if ($truthy(difficulty['$nil?']())) {
            return ""
          };
          if ($truthy($rb_ge(total, difficulty))) {
            return "成功"
          };
          return "失敗";
        });
        
        $def(self, '$isFamble', function $$isFamble(value1, value2) {
          var $ret_or_1 = nil;

          return ($truthy(($ret_or_1 = value1['$=='](1))) ? (value2['$=='](1)) : ($ret_or_1))
        });
        
        $def(self, '$isCritical', function $$isCritical(value1, value2, dice1, dice2) {
          
          
          if ($eqeq(value2, 0)) {
            return false
          };
          if (($eqeq(value1, 6) && ($eqeq(value2, 6)))) {
            return true
          };
          if (($eqeq(value1, dice1) && ($eqeq(value2, dice2)))) {
            return true
          };
          return false;
        });
        
        $def(self, '$getBaseText', function $$getBaseText(dice1, dice2, modify, difficulty) {
          var self = this, baseText = nil;

          
          baseText = "R" + (dice1);
          if ($neqeq(dice2, 0)) {
            baseText = $rb_plus(baseText, "," + (dice2))
          };
          baseText = $rb_plus(baseText, self.$getModifyString(modify));
          if (!$truthy(difficulty['$nil?']())) {
            baseText = $rb_plus(baseText, ">=" + (difficulty))
          };
          return baseText;
        });
        return $def(self, '$getModifyString', function $$getModifyString(modify) {
          
          
          if ($truthy($rb_gt(modify, 0))) {
            return $rb_plus("+", modify.$to_s())
          } else if ($truthy($rb_lt(modify, 0))) {
            return modify.$to_s()
          };
          return "";
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
