require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $send = Opal.send, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $rb_plus = Opal.rb_plus, $rb_gt = Opal.rb_gt, $eqeq = Opal.eqeq, $neqeq = Opal.neqeq, $rb_minus = Opal.rb_minus, $rb_times = Opal.rb_times, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'BarnaKronika');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.isBattleMode = nil;
        
        $const_set($nesting[0], 'ID', "BarnaKronika");
        $const_set($nesting[0], 'NAME', "バルナ・クロニカ");
        $const_set($nesting[0], 'SORT_KEY', "はるなくろにか");
        $const_set($nesting[0], 'HELP_MESSAGE', "・通常判定　nBK\n" + "　ダイス数nで判定ロールを行います。\n" + "　セット数が1以上の時はセット数も表示します。\n" + "・攻撃判定　nBA\n" + "　ダイス数nで判定ロールを行い、攻撃値と命中部位も表示します。\n" + "・クリティカルコール　nBKCt　nBACt\n" + "　判定コマンドの後ろに「Ct」を付けるとクリティカルコールです。\n" + "　ダイス数n,コール数tで判定ロールを行います。\n" + "　ダイス数nで判定ロールを行います。\n" + "　セット数が1以上の時はセット数も表示し、攻撃判定の場合は命中部位も表示します。\n");
        self.$register_prefix("\\d+BK", "\\d+BA", "\\d+BKC", "\\d+BAC", "\\d+R6");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$replace_text', function $$replace_text(string) {
          
          
          string = $send(string, 'gsub', [/(\d+)BKC(\d)/], function $$1(){
            return "" + ($$('Regexp').$last_match(1)) + "R6[0," + ($$('Regexp').$last_match(2)) + "]"});
          string = $send(string, 'gsub', [/(\d+)BAC(\d)/], function $$2(){
            return "" + ($$('Regexp').$last_match(1)) + "R6[1," + ($$('Regexp').$last_match(2)) + "]"});
          string = $send(string, 'gsub', [/(\d+)BK/], function $$3(){
            return "" + ($$('Regexp').$last_match(1)) + "R6[0,0]"});
          string = $send(string, 'gsub', [/(\d+)BA/], function $$4(){
            return "" + ($$('Regexp').$last_match(1)) + "R6[1,0]"});
          return string;
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(string) {
          var $a, $b, self = this, option = nil, dice_n = nil, $ret_or_1 = nil, criticalCallDice = nil, battleModeText = nil, dice_str = nil, suc = nil, set = nil, at_str = nil, output = nil;

          
          string = self.$replace_text(string);
          if (!$truthy(/(^|\s)S?((\d+)[rR]6(\[([,\d]+)\])?)(\s|$)/i['$=~'](string))) {
            return nil
          };
          string = $$('Regexp').$last_match(2);
          option = $$('Regexp').$last_match(5);
          dice_n = $$('Regexp').$last_match(3);
          dice_n = ($truthy(($ret_or_1 = dice_n)) ? ($ret_or_1) : (1));
          self.isBattleMode = false;
          criticalCallDice = 0;
          if ($truthy(option)) {
            
            $b = $send(option.$split(","), 'map', [], "to_i".$to_proc()), $a = $to_ary($b), (battleModeText = ($a[0] == null ? nil : $a[0])), (criticalCallDice = ($a[1] == null ? nil : $a[1])), $b;
            self.isBattleMode = battleModeText['$=='](1);
          };
          self.$debug("@isBattleMode", self.isBattleMode);
          $b = self.$roll_barna_kronika(dice_n, criticalCallDice), $a = $to_ary($b), (dice_str = ($a[0] == null ? nil : $a[0])), (suc = ($a[1] == null ? nil : $a[1])), (set = ($a[2] == null ? nil : $a[2])), (at_str = ($a[3] == null ? nil : $a[3])), $b;
          output = "(" + (string) + ") ＞ [" + (dice_str) + "] ＞ ";
          if ($truthy(self.isBattleMode)) {
            output = $rb_plus(output, at_str)
          } else {
            
            self.$debug("suc", suc);
            if ($truthy($rb_gt(suc, 1))) {
              output = $rb_plus(output, "成功数" + (suc))
            } else {
              output = $rb_plus(output, "失敗")
            };
            self.$debug("set", set);
            if ($truthy($rb_gt(set, 0))) {
              output = $rb_plus(output, ",セット" + (set))
            };
          };
          return output;
        });
        
        $def(self, '$roll_barna_kronika', function $$roll_barna_kronika(dice_n, criticalCallDice) {
          var self = this, output = nil, suc = nil, set = nil, at_str = nil, diceCountList = nil, c_cnt = nil;

          
          dice_n = dice_n.$to_i();
          output = "";
          suc = 0;
          set = 0;
          at_str = "";
          diceCountList = [0, 0, 0, 0, 0, 0];
          $send(dice_n, 'times', [], function $$5(_i){var self = $$5.$$s == null ? this : $$5.$$s, index = nil;
            if (self.randomizer == null) self.randomizer = nil;

            
            if (_i == null) _i = nil;
            index = self.randomizer.$roll_index(6);
            diceCountList['$[]='](index, $rb_plus(diceCountList['$[]'](index), 1));
            if ($truthy($rb_gt(diceCountList['$[]'](index), suc))) {
              return (suc = diceCountList['$[]'](index))
            } else {
              return nil
            };}, {$$s: self});
          $send((6), 'times', [], function $$6(i){var self = $$6.$$s == null ? this : $$6.$$s, diceCount = nil;

            
            if (i == null) i = nil;
            diceCount = diceCountList['$[]'](i);
            if ($eqeq(diceCount, 0)) {
              return nil
            };
            $send(diceCount, 'times', [], function $$7(_j){
              
              if (_j == null) _j = nil;
              return (output = $rb_plus(output, "" + ($rb_plus(i, 1)) + ","));});
            if ($truthy(self.$isCriticalCall(i, criticalCallDice))) {
              
              self.$debug("isCriticalCall");
              at_str = $rb_plus(at_str, self.$getAttackStringWhenCriticalCall(i, diceCount));
            } else if ($truthy(self.$isNomalAttack(criticalCallDice, diceCount))) {
              
              self.$debug("isNomalAttack");
              at_str = $rb_plus(at_str, self.$getAttackStringWhenNomal(i, diceCount));
            };
            if ($truthy($rb_gt(diceCount, 1))) {
              return (set = $rb_plus(set, 1))
            } else {
              return nil
            };}, {$$s: self});
          if ($neqeq(criticalCallDice, 0)) {
            
            c_cnt = diceCountList['$[]']($rb_minus(criticalCallDice, 1));
            suc = $rb_times(c_cnt, 2);
            if ($neqeq(c_cnt, 0)) {
              set = 1
            } else {
              set = 0
            };
          };
          if (($truthy(self.isBattleMode) && ($truthy($rb_lt(suc, 2))))) {
            at_str = "失敗"
          };
          output = output.$sub(/,$/, "");
          at_str = at_str.$sub(/,$/, "");
          return [output, suc, set, at_str];
        });
        
        $def(self, '$isCriticalCall', function $$isCriticalCall(index, criticalCallDice) {
          var self = this;

          
          if (!$truthy(self.isBattleMode)) {
            return false
          };
          if ($eqeq(criticalCallDice, 0)) {
            return false
          };
          return criticalCallDice['$==']($rb_plus(index, 1));
        });
        
        $def(self, '$isNomalAttack', function $$isNomalAttack(criticalCallDice, diceCount) {
          var self = this;

          
          if (!$truthy(self.isBattleMode)) {
            return false
          };
          if ($neqeq(criticalCallDice, 0)) {
            return false
          };
          return $rb_gt(diceCount, 1);
        });
        
        $def(self, '$getAttackStringWhenCriticalCall', function $$getAttackStringWhenCriticalCall(index, diceCount) {
          var self = this, hitLocation = nil, attackValue = nil, result = nil;

          
          hitLocation = self.$getAttackHitLocation($rb_plus(index, 1));
          attackValue = $rb_times(diceCount, 2);
          result = $rb_plus(hitLocation, ":攻撃値" + (attackValue) + ",");
          return result;
        });
        
        $def(self, '$getAttackStringWhenNomal', function $$getAttackStringWhenNomal(index, diceCount) {
          var self = this, hitLocation = nil, attackValue = nil, result = nil;

          
          hitLocation = self.$getAttackHitLocation($rb_plus(index, 1));
          attackValue = diceCount;
          result = $rb_plus(hitLocation, ":攻撃値" + (attackValue) + ",");
          return result;
        });
        return $def(self, '$getAttackHitLocation', function $$getAttackHitLocation(num) {
          var self = this, table = nil;

          
          table = [[1, "頭部"], [2, "右腕"], [3, "左腕"], [4, "右脚"], [5, "左脚"], [6, "胴体"]];
          return self.$get_table_by_number(num, table);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
