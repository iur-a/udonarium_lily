require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $send = Opal.send, $eqeq = Opal.eqeq, $thrower = Opal.thrower, $hash2 = Opal.hash2, $rb_ge = Opal.rb_ge, $rb_minus = Opal.rb_minus, $rb_times = Opal.rb_times, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'SajinsenkiAGuS');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "SajinsenkiAGuS");
        $const_set($nesting[0], 'NAME', "砂塵戦機アーガス");
        $const_set($nesting[0], 'SORT_KEY', "さしんせんきああかす");
        $const_set($nesting[0], 'HELP_MESSAGE', "・一般判定Lv（チャンス出目0→判定0） nAG+x\n" + "　　　nは習得レベル、Lv0の場合nの省略可能。xは判定値修正（数式による修正可）、省略した場合はレベル修正0\n" + "　　　例）AG:習得レベル0の一般技能、1AG+1:習得レベル1・判定値修正+1の技能、AG+2-1：習得レベル0・判定値修正2-1の技能、(1-1)AG：習得レベル1・レベル修正-1の技能\n" + "\n" + "・適正距離での命中判定（チャンス出目0→判定0、HR算出）OM+y@z\n" + "　　　yは命中補正値（数式可）、zはクリティカル値。クリティカル値省略時は0\n" + "　　　HRの算出時には、HRが大きくなる場合に出目0を10に読み替えます。\n" + "　　　例）OM+18-6@2:命中補正値+18-6でクリティカル値2、適正距離の判定\n" + "\n" + "・非適正距離での命中判定（チャンス出目0→判定0、HR算出）NM+y@z\n" + "　　　yは命中補正値（数式可）、zはクリティカル値。クリティカル値省略時は0\n" + "　　　HRの算出時には、HRが大きくなる場合に出目0を10に読み替えます。\n" + "　　　例）NM+4-3:命中補正値+4-3で非適正距離の判定\n" + "\n" + "・クリティカル表 CR\n" + "\n" + "※通常の1D10などの10面ダイスにおいて出目10の読み替えはしません。コマンドのみです。\n" + "\n");
        self.$register_prefix("-?\\d*AG", "OM", "NM", "CR");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$roll_ippan(command))) ? ($ret_or_2) : (self.$roll_hit_check(command)))))) {
            return $ret_or_1
          } else {
            return self.$roll_tables(command, $$('TABLES'))
          }
        });
        self.$private();
        
        $def(self, '$roll_ippan', function $$roll_ippan(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, level = nil, x = nil, $ret_or_1 = nil, target = nil, dice_list = nil, total = nil, success_level = nil;

          
          m = /^(-?\d+)?AG((?:[-+]\d+)*)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          level = m['$[]'](1).$to_i();
          x = ($truthy(($ret_or_1 = $$('Arithmetic').$eval(m['$[]'](2), self.round_type))) ? ($ret_or_1) : (0));
          target = ($truthy($rb_le(level, 0)) ? ($rb_plus(7, x)) : ($rb_plus($rb_plus(10, level), x)));
          dice_list = $send(self.randomizer.$roll_barabara(2, 10), 'map', [], function $$1(d){
            
            if (d == null) d = nil;
            if ($eqeq(d, 10)) {
              return 0
            } else {
              return d
            };});
          total = dice_list.$sum();
          success_level = $rb_plus(1, $send(dice_list, 'count', [], function $$2(val){
            
            if (val == null) val = nil;
            return $rb_le(val, level);}));
          return $send($$('Result').$new(), 'tap', [], function $$3(result){var $a, sequence = nil;

            
            if (result == null) result = nil;
            result['$condition=']($rb_le(total, target));
            result['$rands='](dice_list);
            sequence = ["(2D10<=" + (target) + ")", "" + (total) + "[" + (dice_list.$join(",")) + "]", ($truthy(dice_list['$include?'](0)) ? ("チャンス") : nil), ($truthy(result['$success?']()) ? ("成功(+" + (success_level) + ")") : ("失敗"))].$compact();
            return ($a = [sequence.$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);});} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$roll_hit_check', function $$roll_hit_check(command) {
          var self = this, parser = nil, parsed = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("OM", "NM", $hash2(["round_type"], {"round_type": self.round_type})).$enable_critical();
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          if ($eqeq(parsed.$command(), "OM")) {
            return self.$roll_om(parsed)
          } else if ($eqeq(parsed.$command(), "NM")) {
            return self.$roll_nm(parsed)
          } else {
            return nil
          };
        });
        
        $def(self, '$roll_om', function $$roll_om(parsed) {
          var self = this, target = nil, critical = nil, $ret_or_1 = nil, dice_list = nil, total = nil, criticals = nil, hr = nil;

          
          target = parsed.$modify_number();
          critical = ($truthy(($ret_or_1 = parsed.$critical())) ? ($ret_or_1) : (0));
          dice_list = $send(self.randomizer.$roll_barabara(2, 10), 'map', [], function $$4(v){
            
            if (v == null) v = nil;
            if ($eqeq(v, 10)) {
              return 0
            } else {
              return v
            };}).$sort().$reverse();
          total = dice_list.$sum();
          criticals = $send(dice_list, 'count', [], function $$5(v){
            
            if (v == null) v = nil;
            return $rb_le(v, critical);});
          hr = self.$calc_hr(target, dice_list);
          return $send($$('Result').$new(), 'tap', [], function $$6(r){var $a;

            
            if (r == null) r = nil;
            r['$condition=']($rb_le(total, target));
            r['$rands='](dice_list);
            r['$critical=']($rb_ge(criticals, 1));
            return ($a = [["(2D10<=" + (target) + ")", "" + (total) + "[" + (dice_list.$join(",")) + "]", ($truthy(dice_list['$include?'](0)) ? ("チャンス") : nil), ($truthy(r['$success?']()) ? ("成功（HR=" + (hr) + "、クリティカル" + (criticals) + "）") : ("失敗"))].$compact().$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);});
        });
        
        $def(self, '$roll_nm', function $$roll_nm(parsed) {
          var self = this, target = nil, critical = nil, $ret_or_1 = nil, dice_list = nil, chosen_dice_list = nil, total = nil, criticals = nil, hr = nil;

          
          target = parsed.$modify_number();
          critical = ($truthy(($ret_or_1 = parsed.$critical())) ? ($ret_or_1) : (0));
          dice_list = $send(self.randomizer.$roll_barabara(3, 10), 'map', [], function $$7(v){
            
            if (v == null) v = nil;
            if ($eqeq(v, 10)) {
              return 0
            } else {
              return v
            };}).$sort().$reverse();
          chosen_dice_list = dice_list.$take(2);
          total = chosen_dice_list.$sum();
          criticals = $send(chosen_dice_list, 'count', [], function $$8(v){
            
            if (v == null) v = nil;
            return $rb_le(v, critical);});
          hr = self.$calc_hr(target, chosen_dice_list);
          return $send($$('Result').$new(), 'tap', [], function $$9(r){var $a;

            
            if (r == null) r = nil;
            r['$condition=']($rb_le(total, target));
            r['$rands='](dice_list);
            r['$critical=']($rb_ge(criticals, 1));
            return ($a = [["(3D10<=" + (target) + ")", "" + (total) + "[" + (dice_list['$[]'](0)) + "," + (dice_list['$[]'](1)) + "&" + (dice_list['$[]'](2)) + "]", ($truthy(chosen_dice_list['$include?'](0)) ? ("チャンス") : nil), ($truthy(r['$success?']()) ? ("成功（HR=" + (hr) + "、クリティカル" + (criticals) + "）") : ("失敗"))].$compact().$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);});
        });
        
        $def(self, '$calc_hr', function $$calc_hr(target, chosen_dice_list) {
          var total = nil, a = nil, b = nil;

          
          total = chosen_dice_list.$sum();
          a = $rb_minus(target, total).$abs();
          b = $rb_minus($rb_minus(target, total), $rb_times(chosen_dice_list.$count(0), 10)).$abs();
          return [a, b].$max();
        });
        return $const_set($nesting[0], 'TABLES', $hash2(["CR"], {"CR": $$$($$('DiceTable'), 'Table').$new("クリティカル表", "1D10", ["1：「小破」ダメージ+［5］。耐久値-［1］", "2：「小破」ダメージ+［5］。耐久値-［1］", "3：「小破」ダメージ+［5］。耐久値-［1］", "4：「小破」ダメージ+［5］。耐久値-［1］", "5：「兵装」損壊を受けるごとに［1D10］を振り、出目に応じた部位の兵装とオプションが《脱落》", "6：「上体」攻撃系能力［白兵/ 火器/ 索敵］は各［- 損壊Lv］", "7：「脚部」行動系・防御系能力［Iv 値（イニシア値）/ 最大MP/ 回避］は各［- 損壊Lv］", "8：「搭乗者」搭乗者の〈最大HP〉および〈HP〉は［-（4 ×損壊Lv）］", "9：「搭乗者」搭乗者の〈最大HP〉および〈HP〉は［-（4 ×損壊Lv）］", "0：「小破」ダメージ+［5］。耐久値-［1］"])}).$freeze());
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
