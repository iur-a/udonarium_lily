require('./../opal');

Opal.modules["bcdice/format"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'Format');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq("==", ($ret_or_1 = op))) {
          return "="
        } else if ($eqeqeq("!=", $ret_or_1)) {
          return "<>"
        } else if ($eqeqeq($$('Symbol'), $ret_or_1)) {
          return op.$to_s()
        } else {
          return nil
        }
      });
      return $def(self, '$modifier', function $$modifier(number) {
        
        if ($truthy(number['$nil?']())) {
          return nil
        } else if ($eqeq(number, 0)) {
          return ""
        } else if ($truthy($rb_gt(number, 0))) {
          return "+" + (number)
        } else {
          return number.$to_s()
        }
      });
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $send = Opal.send, $rb_minus = Opal.rb_minus, $rb_times = Opal.rb_times, $rb_plus = Opal.rb_plus, $hash2 = Opal.hash2, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $eqeq = Opal.eqeq, $neqeq = Opal.neqeq, $rb_ge = Opal.rb_ge, $rb_divide = Opal.rb_divide, $rb_gt = Opal.rb_gt, self = Opal.top, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  
  self.$require("bcdice/format");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'NightmareHunterDeep');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "NightmareHunterDeep");
        $const_set($nesting[0], 'NAME', "ナイトメアハンター=ディープ");
        $const_set($nesting[0], 'SORT_KEY', "ないとめあはんたあていいふ");
        $const_set($nesting[0], 'HELP_MESSAGE', "判定（xD6+y>=a, xD6+y, xD6)\n" + "  出目6の個数をカウントして、その4倍を合計値に加算します。\n" + "  また、宿命を獲得したか表示します。\n" + "\n" + "  Lv目標値 (xD6+y>=LVn, xD6+y>=NLn)\n" + "    レベルで目標値を指定することができます。\n" + "    LVn -> n*5+1, NLn -> n*5+5 に変換されます。\n" + "  目標値'?' (xD6+y>=?)\n" + "    目標値を '?' にすると何Lv成功か、何NL成功かを表示します。\n" + "\n" + "※判定コマンドは xD6 から始まる必要があります。また xD6 が複数あると反応しません。\n");
        self.$register_prefix("\\d+D6");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, $b, self = this, parser = nil, cmd = nil, times = nil, dice_list = nil, dice_total = nil, total = nil, suffix = nil, revision = nil, target = nil, result = nil, sequence = nil;

          
          command = $send($send(command, 'sub', [/Lv(\d+)/i], function $$1(){
            return $rb_minus($rb_times($$('Regexp').$last_match(1).$to_i(), 5), 1).$to_s()}), 'sub', [/NL(\d+)/i], function $$2(){
            return $rb_plus($rb_times($$('Regexp').$last_match(1).$to_i(), 5), 5).$to_s()});
          parser = $$$($$('Command'), 'Parser').$new(/\d+D6/, $hash2(["round_type"], {"round_type": self.$round_type()})).$restrict_cmp_op_to(nil, ">=").$enable_question_target();
          cmd = parser.$parse(command);
          if (!$truthy(cmd)) {
            return nil
          };
          times = cmd.$command().$to_i();
          dice_list = self.randomizer.$roll_barabara(times, 6).$sort();
          dice_total = dice_list.$sum();
          total = $rb_plus(dice_total, cmd.$modify_number());
          $b = self.$dice_revision(dice_list), $a = $to_ary($b), (suffix = ($a[0] == null ? nil : $a[0])), (revision = ($a[1] == null ? nil : $a[1])), $b;
          total = $rb_plus(total, revision);
          target = ($truthy(cmd['$question_target?']()) ? ("?") : (cmd.$target_number()));
          result = self.$result_text(total, cmd.$cmp_op(), target);
          sequence = ["(" + (cmd) + ")", self.$interim_expr(cmd, dice_total, dice_list), self.$expr_with_revision($rb_plus(dice_total, cmd.$modify_number()), suffix), total, result, self.$fate(dice_list)].$compact();
          return sequence.$join(" ＞ ");
        });
        
        $def(self, '$result_text', function $$result_text(total, cmp_op, target) {
          var success_lv = nil, success_nl = nil;

          
          if (!$eqeq(cmp_op, ">=")) {
            return nil
          };
          if ($neqeq(target, "?")) {
            return ($truthy($rb_ge(total, target)) ? ("成功") : ("失敗"))
          };
          success_lv = $rb_divide($rb_plus(total, 1), 5).$to_i();
          success_nl = $rb_divide($rb_minus(total, 5), 5).$to_i();
          return ($truthy($rb_gt(success_lv, 0)) ? ("Lv" + (success_lv) + "/NL" + (success_nl) + "成功") : ("失敗"));
        });
        
        $def(self, '$fate', function $$fate(dice_list) {
          
          if ($truthy($rb_gt(dice_list.$count(1), 0))) {
            return "宿命獲得"
          } else {
            return nil
          }
        });
        
        $def(self, '$interim_expr', function $$interim_expr(cmd, dice_total, dice_list) {
          var modifier = nil;

          if (($truthy($rb_gt(dice_list.$size(), 1)) || ($neqeq(cmd.$modify_number(), 0)))) {
            
            modifier = $$('Format').$modifier(cmd.$modify_number());
            return "" + (dice_total) + "[" + (dice_list.$join(",")) + "]" + (modifier);
          } else {
            return nil
          }
        });
        
        $def(self, '$expr_with_revision', function $$expr_with_revision(total, suffix) {
          
          if ($truthy(suffix)) {
            return "" + (total) + (suffix)
          } else {
            return nil
          }
        });
        return $def(self, '$dice_revision', function $$dice_revision(dice_list) {
          var count6 = nil;

          
          count6 = dice_list.$count(6);
          if ($truthy($rb_gt(count6, 0))) {
            return ["+" + (count6) + "*4", $rb_times(count6, 4)]
          } else {
            return [nil, 0]
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
