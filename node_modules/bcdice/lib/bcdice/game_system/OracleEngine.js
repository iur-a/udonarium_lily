require('./../opal');

Opal.modules["bcdice/format"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'Format');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      
      self.$module_function();
      
      $def(self, '$comparison_operator', function $$comparison_operator(op) {
        var $ret_or_1 = nil;

        if ($eqeqeq("==", ($ret_or_1 = op))) {
          return "="
        } else if ($eqeqeq("!=", $ret_or_1)) {
          return "<>"
        } else if ($eqeqeq($$('Symbol'), $ret_or_1)) {
          return op.$to_s()
        } else {
          return nil
        }
      });
      return $def(self, '$modifier', function $$modifier(number) {
        
        if ($truthy(number['$nil?']())) {
          return nil
        } else if ($eqeq(number, 0)) {
          return ""
        } else if ($truthy($rb_gt(number, 0))) {
          return "+" + (number)
        } else {
          return number.$to_s()
        }
      });
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $eqeqeq = Opal.eqeqeq, $hash2 = Opal.hash2, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $send = Opal.send, $eqeq = Opal.eqeq, $rb_plus = Opal.rb_plus, $neqeq = Opal.neqeq, $rb_ge = Opal.rb_ge, $rb_minus = Opal.rb_minus, $rb_lt = Opal.rb_lt, $rb_gt = Opal.rb_gt, $rb_le = Opal.rb_le, self = Opal.top, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  
  self.$require("bcdice/format");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'OracleEngine');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.cmd = $proto.max_shift = $proto.times = $proto.randomizer = $proto["break"] = $proto.critical = $proto.fumble = nil;
        
        $const_set($nesting[0], 'ID', "OracleEngine");
        $const_set($nesting[0], 'NAME', "オラクルエンジン");
        $const_set($nesting[0], 'SORT_KEY', "おらくるえんしん");
        $const_set($nesting[0], 'HELP_MESSAGE', "  ・クラッチロール （xCL+y>=z)\n" + "  ダイスをx個振り、1個以上目標シフトzに到達したか判定します。修正yは全てのダイスにかかります。\n" + "  成功した時は目標シフトを、失敗した時はダイスの最大値-1シフトを返します\n" + "  zが指定されないときは、ダイスをx個を振り、それに修正yしたものを返します。\n" + "  通常、最低シフトは1、最大シフトは6です。目標シフトもそろえられます。\n" + "  また、CLの後に7を入れ、(xCL7+y>=z)と入力すると最大シフトが7になります。\n" + " ・判定 (xR6+y@c#f$b>=z)\n" + "  ダイスをx個振り、大きいもの2つだけを見て達成値を算出し、成否を判定します。修正yは達成値にかかります。\n" + "  ダイスブレイクとしてbを、クリティカル値としてcを、ファンブル値としてfを指定できます。\n" + "  それぞれ指定されない時、0,12,2になります。\n" + "  クリティカル値の上限はなし、下限は2。ファンブル値の上限は12、下限は0。\n" + "  zが指定されないとき、達成値の算出のみ行います。\n" + " ・ダメージロールのダイスブレイク (xD6+y$b)\n" + "  ダイスをx個振り、合計値を出します。修正yは合計値にかかります。\n" + "  ダイスブレイクとしてbを指定します。合計値は0未満になりません。\n");
        self.$register_prefix("\\d+CL", "\\d+R6", "\\d+D6.*\\$[\\+\\-]?\\d+");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($eqeqeq(/\d+CL.*/i, ($ret_or_1 = command))) {
            return self.$clutch_roll(command)
          } else if ($eqeqeq(/\d+D6.*\$[+-]?\d.*/, $ret_or_1)) {
            return self.$damage_roll(command)
          } else if ($eqeqeq(/\d+R6/, $ret_or_1)) {
            return self.$r_roll(command)
          } else {
            return nil
          }
        });
        
        $def(self, '$clutch_roll', function $$clutch_roll(string) {
          var $a, $b, self = this, parser = nil, $ret_or_1 = nil, dice_list = nil, sequence = nil;

          
          self.$debug("clutch_roll begin", string);
          parser = $$$($$('Command'), 'Parser').$new(/\d+CL[67]?/, $hash2(["round_type"], {"round_type": self.$round_type()})).$restrict_cmp_op_to(nil, ">=");
          self.cmd = parser.$parse(string);
          if (!$truthy(self.cmd)) {
            return nil
          };
          $b = $send(self.cmd.$command().$split("CL"), 'map', [], "to_i".$to_proc()), $a = $to_ary($b), (self.times = ($a[0] == null ? nil : $a[0])), (self.max_shift = ($a[1] == null ? nil : $a[1])), $b;
          self.max_shift = ($truthy(($ret_or_1 = self.max_shift)) ? ($ret_or_1) : (6));
          if ($truthy(self.cmd.$cmp_op())) {
            self.cmd['$target_number='](self.$clamp(self.cmd.$target_number(), 1, self.max_shift))
          };
          if ($eqeq(self.times, 0)) {
            return nil
          };
          dice_list = $send(self.randomizer.$roll_barabara(self.times, 6), 'map', [], function $$1(x){var self = $$1.$$s == null ? this : $$1.$$s;
            if (self.cmd == null) self.cmd = nil;
            if (self.max_shift == null) self.max_shift = nil;

            
            if (x == null) x = nil;
            return self.$clamp($rb_plus(x, self.cmd.$modify_number()), 1, self.max_shift);}, {$$s: self}).$sort();
          sequence = [self.$expr_clutch(), "[" + (dice_list.$join(", ")) + "]", self.$result_clutch(dice_list.$last())];
          return sequence.$join(" ＞ ");
        });
        
        $def(self, '$expr_clutch', function $$expr_clutch() {
          var self = this, max_shift = nil, cmp_op = nil, modify_number = nil;

          
          max_shift = ($eqeq(self.max_shift, 7) ? (7) : (nil));
          cmp_op = $$('Format').$comparison_operator(self.cmd.$cmp_op());
          modify_number = $$('Format').$modifier(self.cmd.$modify_number());
          return "(" + (self.times) + "CL" + (max_shift) + (modify_number) + (cmp_op) + (self.cmd.$target_number()) + ")";
        });
        
        $def(self, '$result_clutch', function $$result_clutch(after_shift) {
          var self = this;

          if ($neqeq(self.cmd.$cmp_op(), ">=")) {
            return "シフト" + (after_shift)
          } else if ($truthy($rb_ge(after_shift, self.cmd.$target_number()))) {
            return "成功 シフト" + (self.cmd.$target_number())
          } else {
            
            after_shift = $rb_minus(after_shift, 1);
            if ($truthy($rb_lt(after_shift, 1))) {
              after_shift = 1
            };
            return "失敗 シフト" + (after_shift);
          }
        });
        
        $def(self, '$clamp', function $$clamp(i, min, max) {
          
          if ($truthy($rb_lt(i, min))) {
            return min
          } else if ($truthy($rb_gt(i, max))) {
            return max
          } else {
            return i
          }
        });
        
        $def(self, '$r_roll', function $$r_roll(string) {
          var self = this, parser = nil, $ret_or_1 = nil, dice_list = nil, dice_broken = nil, dice_total = nil, total = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/\d+R6/, $hash2(["round_type"], {"round_type": self.$round_type()})).$restrict_cmp_op_to(nil, ">=").$enable_critical().$enable_fumble().$enable_dollar();
          self.cmd = parser.$parse(string);
          if (!$truthy(self.cmd)) {
            return nil
          };
          self.times = self.cmd.$command().$to_i();
          if ($eqeq(self.times, 0)) {
            return nil
          };
          self.critical = self.$normalize_critical(($truthy(($ret_or_1 = self.cmd.$critical())) ? ($ret_or_1) : (12)), string);
          self.fumble = self.$normalize_fumble(($truthy(($ret_or_1 = self.cmd.$fumble())) ? ($ret_or_1) : (2)), string);
          self["break"] = ($truthy(($ret_or_1 = self.cmd.$dollar())) ? ($ret_or_1) : (0)).$abs();
          dice_list = self.randomizer.$roll_barabara(self.times, 6).$sort();
          dice_broken = dice_list.$pop(self["break"]);
          dice_total = dice_list.$dup().$pop(2).$inject(0, "+");
          total = $rb_plus(dice_total, self.cmd.$modify_number());
          sequence = [self.$expr_r(), self.$dice_result_r(dice_total, dice_list, dice_broken), self.$result_r(dice_total, total)];
          return sequence.$join(" ＞ ");
        });
        
        $def(self, '$expr_r', function $$expr_r() {
          var self = this, modify_number = nil, critical = nil, fumble = nil, brak = nil, cmp_op = nil;

          
          modify_number = $$('Format').$modifier(self.cmd.$modify_number());
          critical = ($eqeq(self.critical, 12) ? ("") : ("c[" + (self.critical) + "]"));
          fumble = ($eqeq(self.fumble, 2) ? ("") : ("f[" + (self.fumble) + "]"));
          brak = ($eqeq(self["break"], 0) ? ("") : ("b[" + (self["break"]) + "]"));
          cmp_op = $$('Format').$comparison_operator(self.cmd.$cmp_op());
          return "(" + (self.times) + "R6" + (modify_number) + (critical) + (fumble) + (brak) + (cmp_op) + (self.cmd.$target_number()) + ")";
        });
        
        $def(self, '$dice_result_r', function $$dice_result_r(dice_total, dice_list, break_list) {
          var self = this, modify_number_text = nil;

          
          modify_number_text = $$('Format').$modifier(self.cmd.$modify_number());
          if ($truthy(break_list['$empty?']())) {
            return "" + (dice_total) + "[" + (dice_list.$join(", ")) + "]" + (modify_number_text)
          } else {
            return "" + (dice_total) + "[" + (dice_list.$join(", ")) + "]×[" + (break_list.$join(", ")) + "]" + (modify_number_text)
          };
        });
        
        $def(self, '$result_r', function $$result_r(dice_total, total) {
          var self = this;

          if ($truthy($rb_le(dice_total, self.fumble))) {
            return "ファンブル!"
          } else if ($truthy($rb_ge(dice_total, self.critical))) {
            return "クリティカル!"
          } else if ($eqeq(self.cmd.$cmp_op(), ">=")) {
            if ($truthy($rb_ge(total, self.cmd.$target_number()))) {
              return "" + (total) + " 成功"
            } else {
              return "" + (total) + " 失敗"
            }
          } else {
            return total.$to_s()
          }
        });
        
        $def(self, '$normalize_critical', function $$normalize_critical(critical, string) {
          
          
          if ($truthy(/@[+-]/.$match(string))) {
            critical = $rb_plus(12, critical)
          };
          if ($truthy($rb_lt(critical, 2))) {
            critical = 2
          };
          return critical;
        });
        
        $def(self, '$normalize_fumble', function $$normalize_fumble(fumble, string) {
          var self = this;

          
          if ($truthy(/#[+-]/.$match(string))) {
            fumble = $rb_plus(2, fumble)
          };
          return self.$clamp(fumble, 0, 12);
        });
        
        $def(self, '$damage_roll', function $$damage_roll(string) {
          var self = this, parser = nil, $ret_or_1 = nil, dice_list = nil, dice_broken = nil, total_n = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/\d+D6/, $hash2(["round_type"], {"round_type": self.$round_type()})).$restrict_cmp_op_to(nil).$enable_dollar();
          self.cmd = parser.$parse(string);
          if (!$truthy(self.cmd)) {
            return nil
          };
          self.times = self.cmd.$command().$to_i();
          self["break"] = ($truthy(($ret_or_1 = self.cmd.$dollar())) ? ($ret_or_1) : (0)).$abs();
          if ($eqeq(self.times, 0)) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(self.times, 6).$sort();
          dice_broken = dice_list.$pop(self["break"]);
          total_n = $rb_plus(dice_list.$inject(0, "+"), self.cmd.$modify_number());
          if ($truthy($rb_lt(total_n, 0))) {
            total_n = 0
          };
          sequence = [self.$expr_damage(), self.$result_damage(dice_list, dice_broken), total_n];
          return sequence.$join(" ＞ ");
        });
        
        $def(self, '$expr_damage', function $$expr_damage() {
          var self = this, modify_number = nil, brak = nil;

          
          modify_number = $$('Format').$modifier(self.cmd.$modify_number());
          brak = ($eqeq(self["break"], 0) ? ("") : ("b[" + (self["break"]) + "]"));
          return "(" + (self.times) + "D6" + (modify_number) + (brak) + ")";
        });
        return $def(self, '$result_damage', function $$result_damage(dice_list, break_list) {
          var self = this, dice_total = nil, modify_number_text = nil;

          
          dice_total = dice_list.$inject(0, "+");
          modify_number_text = $$('Format').$modifier(self.cmd.$modify_number());
          if ($truthy(break_list['$empty?']())) {
            return "" + (dice_total) + "[" + (dice_list.$join(", ")) + "]" + (modify_number_text)
          } else {
            return "" + (dice_total) + "[" + (dice_list.$join(", ")) + "]×[" + (break_list.$join(", ")) + "]" + (modify_number_text)
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
