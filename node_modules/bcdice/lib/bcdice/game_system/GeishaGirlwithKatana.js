require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $eqeq = Opal.eqeq, $def = Opal.def, $rb_le = Opal.rb_le, $hash = Opal.hash, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'GeishaGirlwithKatana');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "GeishaGirlwithKatana");
        $const_set($nesting[0], 'NAME', "ゲイシャ・ガール・ウィズ・カタナ");
        $const_set($nesting[0], 'SORT_KEY', "けいしやかあるういすかたな");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定 (GK#n)\n" + "  役やチョムバを含めて1回分のダイスロールを判定します。\n" + "　役は　（通常判定）／（戦闘時）　の順で両方出力されます。\n" + "  GK のみの場合5%の確率でチョムバます。\n" + "  GK#3 の様に #n をつけることによってチョムバの確率をn%にすることができます。\n" + "　例）GK　GK#10\n" + "・隠しコマンド (GL)\n" + "  必ずチョムバします。GMが空気を読んでチョムバさせたいときや、\n" + "  GKコマンドを打ち間違えてチョムバするを想定してます。\n" + "　例）GL\n");
        self.$register_prefix("GK", "GL");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, $b, self = this, output = nil, chomba_counter = nil, diceList = nil, yakuResult = nil, deme = nil, zorome = nil, yp = nil;

          
          output = nil;
          if ($truthy(/^GL$/i['$=~'](command))) {
            return self.$getChombaResultText()
          };
          if (!$truthy(/^GK(#(\d+))?$/i['$=~'](command))) {
            return output
          };
          chomba_counter = $$('Regexp').$last_match(2);
          if ($truthy(self.$isChomba(chomba_counter))) {
            return self.$getChombaResultText()
          };
          diceList = self.randomizer.$roll_barabara(3, 6).$sort();
          yakuResult = self.$getYaku(diceList);
          if (!$truthy(yakuResult['$nil?']())) {
            return self.$getResultTextByDice(diceList, "【役】" + (yakuResult))
          };
          $b = self.$getDemeZorome(diceList), $a = $to_ary($b), (deme = ($a[0] == null ? nil : $a[0])), (zorome = ($a[1] == null ? nil : $a[1])), $b;
          if ($eqeq(deme, 0)) {
            return self.$getResultTextByDice(diceList, "失敗")
          };
          yp = ($eqeq(zorome, 1) ? (" YPが1増加") : (""));
          output = self.$getResultTextByDice(diceList, "達成値" + (deme) + (yp));
          self.$debug("getGGwKResult(command) result", output);
          return output;
        });
        
        $def(self, '$isChomba', function $$isChomba(chomba_counter) {
          var self = this, $ret_or_1 = nil, chomba = nil;

          
          chomba_counter = ($truthy(($ret_or_1 = chomba_counter)) ? ($ret_or_1) : (5));
          chomba_counter = chomba_counter.$to_i();
          chomba = self.randomizer.$roll_once(100);
          return $rb_le(chomba, chomba_counter);
        });
        
        $def(self, '$getChombaResultText', function $$getChombaResultText() {
          var self = this;

          return self.$getResultText("チョムバ！！")
        });
        
        $def(self, '$getYaku', function $$getYaku(diceList) {
          var rule = nil, yaku = nil;

          
          rule = $hash([1, 2, 3], "自動失敗／自分の装甲効果無しでダメージを受けてしまう", [4, 5, 6], "自動成功／敵の装甲を無視してダメージを与える", [1, 1, 1], "10倍成功 YPが10増加／10倍ダメージ YPが10増加", [2, 2, 2], "2倍成功／2倍ダメージ", [3, 3, 3], "3倍成功／3倍ダメージ", [4, 4, 4], "4倍成功／4倍ダメージ", [5, 5, 5], "5倍成功／5倍ダメージ", [6, 6, 6], "6倍成功／6倍ダメージ");
          yaku = rule['$[]'](diceList);
          return yaku;
        });
        
        $def(self, '$getDemeZorome', function $$getDemeZorome(diceList) {
          var deme = nil, zorome = nil;

          
          deme = 0;
          zorome = 0;
          if ($eqeq(diceList['$[]'](0), diceList['$[]'](1))) {
            
            deme = diceList['$[]'](2);
            zorome = diceList['$[]'](0);
          } else if ($eqeq(diceList['$[]'](1), diceList['$[]'](2))) {
            
            deme = diceList['$[]'](0);
            zorome = diceList['$[]'](1);
          };
          return [deme, zorome];
        });
        
        $def(self, '$getResultTextByDice', function $$getResultTextByDice(diceList, result) {
          var self = this;

          return self.$getResultText("" + (diceList.$join(",")) + " ＞ " + (result))
        });
        return $def(self, '$getResultText', function $$getResultText(result) {
          
          return "(3B6) ＞ " + (result)
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
