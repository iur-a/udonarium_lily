require('./../opal');

Opal.modules["bcdice/game_system/ShadowRun4"] = function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $rb_divide = Opal.rb_divide, $rb_times = Opal.rb_times, $truthy = Opal.truthy, $rb_ge = Opal.rb_ge, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ShadowRun4');

        var $nesting = [self].concat($parent_nesting);

        
        $const_set($nesting[0], 'ID', "ShadowRun4");
        $const_set($nesting[0], 'NAME', "シャドウラン 4th Edition");
        $const_set($nesting[0], 'SORT_KEY', "しやとうらん4");
        $const_set($nesting[0], 'HELP_MESSAGE', "個数振り足しロール(xRn)の境界値を6にセット、バラバラロール(xBn)の目標値を5以上にセットします。\n" + "BコマンドとRコマンド時に、グリッチの表示を行います。\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          self.sort_barabara_dice = true;
          self.reroll_dice_reroll_threshold = 6;
          self.default_cmp_op = ">=";
          return (self.default_target_number = 5);
        });
        return $def(self, '$grich_text', function $$grich_text(numberSpot1, dice_cnt_total, successCount) {
          var self = this, dice_cnt_total_half = nil;

          
          dice_cnt_total_half = $rb_divide($rb_times(1.0, dice_cnt_total), 2);
          self.$debug("dice_cnt_total_half", dice_cnt_total_half);
          if (!$truthy($rb_ge(numberSpot1, dice_cnt_total_half))) {
            return nil
          };
          if ($eqeq(successCount, 0)) {
            return "クリティカルグリッチ"
          } else {
            return "グリッチ"
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
};

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $rb_gt = Opal.rb_gt, $rb_minus = Opal.rb_minus, $rb_plus = Opal.rb_plus, $thrower = Opal.thrower, $rb_divide = Opal.rb_divide, $eqeq = Opal.eqeq, self = Opal.top, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  
  self.$require("bcdice/game_system/ShadowRun4");
  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ShadowRun5');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "ShadowRun5");
        $const_set($nesting[0], 'NAME', "シャドウラン 5th Edition");
        $const_set($nesting[0], 'SORT_KEY', "しやとうらん5");
        $const_set($nesting[0], 'HELP_MESSAGE', "個数振り足しロール(xRn)の境界値を6にセット、バラバラロール(xBn)の目標値を5以上にセットします。\n" + "バラバラロール(xBn)のみ、リミットをセットできます。リミットの指定は(xBn@l)のように指定します。(省略可)\n" + "BコマンドとRコマンド時に、グリッチの表示を行います。\n");
        self.$register_prefix("(\\d+)B6@(\\d+)");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          self.sort_barabara_dice = true;
          self.reroll_dice_reroll_threshold = 6;
          self.default_cmp_op = ">=";
          return (self.default_target_number = 5);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, b_dice = nil, limit = nil, output_before_limited = nil, output_after_limited = nil, before_suc_cnt = nil, after_suc_cnt = nil, over_suc_cnt = nil, output = nil;

          
          self.$debug("chatch limit prefix");
          m = /(\d+B6)@(\d+)/.$match(command);
          b_dice = m['$[]'](1);
          limit = m['$[]'](2).$to_i();
          output_before_limited = $$$($$$($$('BCDice'), 'CommonCommand'), 'BarabaraDice').$eval(b_dice, self, self.randomizer).$text();
          m = /成功数(\d+)/.$match(output_before_limited);
          output_after_limited = output_before_limited;
          before_suc_cnt = m['$[]'](1).$to_i();
          if ($truthy($rb_gt(before_suc_cnt, limit))) {
            
            after_suc_cnt = limit;
            over_suc_cnt = $rb_minus(before_suc_cnt, limit);
            output_after_limited = output_before_limited.$gsub(/成功数(\d+)/, "成功数" + (after_suc_cnt));
            output_after_limited = $rb_plus(output_after_limited, "(リミット超過" + (over_suc_cnt) + ")");
          };
          output = output_after_limited;
          output = output.$gsub("B", "B6");
          output = output.$gsub("6>=5", "[6]Limit[" + (limit) + "]>=5");
          self.$debug(output);
          return output;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        return $def(self, '$grich_text', function $$grich_text(numberSpot1, dice_cnt_total, successCount) {
          var self = this, dice_cnt_total_half = nil;

          
          dice_cnt_total_half = $rb_divide(dice_cnt_total.$to_f(), 2);
          self.$debug("dice_cnt_total_half", dice_cnt_total_half);
          if (!$truthy($rb_gt(numberSpot1, dice_cnt_total_half))) {
            return nil
          };
          if ($eqeq(successCount, 0)) {
            return "クリティカルグリッチ"
          } else {
            return "グリッチ"
          };
        });
      })($nesting[0], $$('ShadowRun4'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting);
});
