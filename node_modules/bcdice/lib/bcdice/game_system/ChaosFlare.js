require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_le = Opal.rb_le, $rb_minus = Opal.rb_minus, $neqeq = Opal.neqeq, $rb_ge = Opal.rb_ge, $def = Opal.def, $rb_divide = Opal.rb_divide, $not = Opal.not, $range = Opal.range, $hash2 = Opal.hash2, $rb_lt = Opal.rb_lt, $rb_plus = Opal.rb_plus, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ChaosFlare');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = $proto.cmd = nil;
        
        $const_set($nesting[0], 'ID', "ChaosFlare");
        $const_set($nesting[0], 'NAME', "カオスフレア");
        $const_set($nesting[0], 'SORT_KEY', "かおすふれあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "判定\n" + "CF\n" + "  書式: [ダイスの数]CF[修正値][@クリティカル値][#ファンブル値][>=目標値]\n" + "    CF以外は全て省略可能\n" + "  例:\n" + "  - CF 2D6,クリティカル値12,ファンブル値2で判定\n" + "  - CF+10@10 修正値+10,クリティカル値10で判定\n" + "  - CF+10#3 修正値+10,ファンブル値3で判定\n" + "  - CF+10>=10 目標値を指定した場合、差分値も出力する\n" + "  - 3CF+10@10#3>=10 3D6での判定\n" + "  - CF@9#3+8>=10\n" + "\n" + "2D6\n" + "  ファンブル値2で判定する。クリティカルの判定は行われない。\n" + "  目標値が設定された場合、差分値を出力する。\n" + "  - 2D6+4>=10\n" + "\n" + "各種表\n" + "  FT: 因縁表\n" + "  FTx: 数値を指定すると因果表の値を出力する\n" + "  - FT -> 11から66の間でランダム決定\n" + "  - FT23 -> 23の項目を出力\n" + "  - FT0\n" + "  - FT7\n");
        self.$register_prefix("\\d*CF", "FT");
        
        $def(self, '$result_2d6', function $$result_2d6(total, dice_total, _dice_list, cmp_op, target) {
          var sequence = nil, result = nil;

          
          if (!$eqeq(cmp_op, ">=")) {
            return nil
          };
          sequence = [];
          result = $$('Result').$new();
          if ($truthy($rb_le(dice_total, 2))) {
            
            total = $rb_minus(total, 20);
            sequence.$push("ファンブル(-20)");
            result['$fumble='](true);
          };
          if ($neqeq(target, "?")) {
            
            if ($truthy($rb_ge(total, target))) {
              
              sequence.$push("成功");
              result['$success='](true);
            } else {
              
              sequence.$push("失敗");
              result['$failure='](true);
            };
            if ($neqeq($rb_minus(total, target), 0)) {
              sequence.$push("差分値" + ($rb_minus(total, target)))
            };
          };
          if ($truthy(sequence['$empty?']())) {
            return $$('Result').$nothing()
          };
          result['$text='](sequence.$join(" ＞ "));
          return result;
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          if ($truthy(command['$start_with?']("FT"))) {
            return self.$roll_fate_table(command)
          } else {
            return self.$cf_roll(command)
          }
        });
        self.$private();
        
        $def(self, '$roll_fate_table', function $$roll_fate_table(command) {
          var self = this, m = nil, num = nil, dice1 = nil, dice2 = nil, index1 = nil, index2 = nil;

          
          m = /^FT(\d+)?/.$match(command);
          if ($truthy(m['$[]'](1))) {
            
            num = m['$[]'](1).$to_i();
            if ($truthy([0, 7]['$include?'](num))) {
              return "因果表(" + (num) + ") ＞ " + ($$('FATE_TABLE')['$[]'](num)['$[]'](0))
            };
            dice1 = $rb_divide(num, 10).$to_i();
            dice2 = num['$%'](10);
            if (($not($range(1, 6, false)['$include?'](dice1)) || ($not($range(1, 6, false)['$include?'](dice2))))) {
              return nil
            };
          } else {
            
            dice1 = self.randomizer.$roll_once(6);
            dice2 = self.randomizer.$roll_once(6);
          };
          index1 = dice1;
          index2 = $rb_minus($rb_divide(dice2, 2).$to_i(), 1);
          return "因果表(" + (dice1) + (dice2) + ") ＞ " + ($$('FATE_TABLE')['$[]'](index1)['$[]'](index2));
        });
        
        $def(self, '$cf_roll', function $$cf_roll(command) {
          var self = this, parser = nil, times = nil, critical = nil, $ret_or_1 = nil, fumble = nil, dice_list = nil, dice_total = nil, dice_list_text = nil, is_critical = nil, is_fumble = nil, total = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/\d*CF/, $hash2(["round_type"], {"round_type": self.$round_type()})).$enable_critical().$enable_fumble();
          self.cmd = parser.$parse(command);
          if (!$truthy(self.cmd)) {
            return nil
          };
          times = ($eqeq(self.cmd.$command(), "CF") ? (2) : (self.cmd.$command().$to_i()));
          critical = ($truthy(($ret_or_1 = self.cmd.$critical())) ? ($ret_or_1) : (12));
          fumble = ($truthy(($ret_or_1 = self.cmd.$fumble())) ? ($ret_or_1) : (2));
          self.cmd['$dollar='](nil);
          if (($truthy($rb_lt(times, 0)) || ($not([">=", nil]['$include?'](self.cmd.$cmp_op()))))) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(times, 6);
          dice_total = dice_list.$sum();
          dice_list_text = dice_list.$join(",");
          is_critical = $rb_ge(dice_total, critical);
          is_fumble = $rb_le(dice_total, fumble);
          total = ($truthy(is_critical) ? (30) : ($truthy(is_fumble) ? (-20) : (dice_total)));
          total = $rb_plus(total, self.cmd.$modify_number());
          sequence = ["(" + (self.cmd.$to_s("after_modify_number")) + ")", "" + (dice_total) + "[" + (dice_list_text) + "]", total.$to_s(), ($truthy($rb_lt(total, 0)) ? ("0") : nil), ($truthy(is_critical) ? ("クリティカル") : nil), ($truthy(is_fumble) ? ("ファンブル") : nil), ($truthy(self.cmd.$target_number()) ? ("差分値 " + (self.$difference(total))) : nil)].$compact();
          return sequence.$join(" ＞ ");
        });
        
        $def(self, '$difference', function $$difference(total) {
          var self = this;

          if ($truthy($rb_lt(total, 0))) {
            return self.cmd.$target_number()['$-@']()
          } else {
            return $rb_minus(total, self.cmd.$target_number())
          }
        });
        return $const_set($nesting[0], 'FATE_TABLE', [["腐れ縁"], ["純愛", "親近感", "庇護"], ["信頼", "感服", "共感"], ["友情", "尊敬", "慕情"], ["好敵手", "期待", "借り"], ["興味", "憎悪", "悲しみ"], ["恐怖", "執着", "利用"], ["任意"]].$freeze());
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
