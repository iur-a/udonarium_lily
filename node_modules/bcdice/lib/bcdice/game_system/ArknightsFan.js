require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $hash = Opal.hash, $rb_le = Opal.rb_le, $rb_ge = Opal.rb_ge, $eqeq = Opal.eqeq, $not = Opal.not, $thrower = Opal.thrower, $eqeqeq = Opal.eqeqeq, $to_ary = Opal.to_ary, $rb_minus = Opal.rb_minus, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_gt = Opal.rb_gt, $neqeq = Opal.neqeq, $rb_divide = Opal.rb_divide, $rb_times = Opal.rb_times, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ArknightsFan');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "ArknightsFan");
        $const_set($nesting[0], 'NAME', "アークナイツTRPG by daaaper");
        $const_set($nesting[0], 'SORT_KEY', "ああくないつTRPGはいてえはあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 能力値判定 (nADm<=x)\n" + "  nDmのダイスロールをして、出目が x 以下であれば成功。\n" + "  出目が91以上でエラー。\n" + "  出目が10以下でクリティカル。\n" + "\n" + "■ 攻撃/防御判定 (nABm<=x)\n" + "  nBmのダイスロールをして、\n" + "    出目が x 以下であれば成功数+1。\n" + "    出目が91以上でエラー。成功数-1。\n" + "    出目が10以下でクリティカル。成功数+1。\n" + "  上記による成功数をカウント。\n" + "\n" + "■ 役職効果付き攻撃判定 (nABm<=x--役職名h)\n" + "  h: 健康状態(0: 健康、1: 中等症、2: 重症)\n" + "  nBmのダイスロールをして、\n" + "    出目が x 以下であれば成功数+1。\n" + "    出目が91以上でエラー。成功数-1。\n" + "    出目が10以下でクリティカル。成功数+1。\n" + "  上記による成功数をカウントした上で、以下の役職名による成功数増加効果を適応。\n" + "    狙撃（SNI）: 健康(h=0)かつ成功数1以上のとき、成功数+1。\n" + "  健康状態hを省略した場合、健康(h=0)として扱われる。\n" + "\n" + "■ 鉱石病判定 (ORPx@y+Dd+Tt)\n" + "  x: 生理的耐性、y: 上昇後侵食度、d: ダイス補正、t: 判定値補正\n" + "  生理的耐性xのOPが侵食度yに上昇した際の鉱石病判定を、ダイス数補正d、判定値補正tで行う。\n" + "  ダイス数補正と判定値補正は省略可能。例えば ORP60@25 は ORP60@25+D0+T0 と同義。\n" + "  また、ダイス数補正と判定値補正は逆順でも可。例えば ORP60@25+T10+D2 も可。\n" + "\n" + "■ 増悪判定（--WORSENING）\n" + "  症状を「末梢神経障害」「内臓機能不全」「精神症状」からランダムに選択。\n" + "  継続ラウンド数を1d6+1で判定。\n" + "\n" + "■ 中毒判定（--ADDICTION）\n" + "  症状を「脳神経障害」「多臓器不全」「急性精神反応」からランダムに選択。\n" + "\n" + "■ 判定の省略表記\n" + "  nADm、nABm、nABmにおいて、\n" + "    n（ダイス個数）を省略した場合、1として扱われる。\n" + "    m（ダイス種類）を省略した場合、100として扱われる。\n" + "  例えば、AD<=90は1AD100<=90として解釈される。\n");
        self.$register_prefix("[-+*/\\d]*AD\\d*", "[-+*/\\d]*AB\\d*", "ORP", "--WORSENING", "--ADDICTION");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          self.sort_barabara_dice = true;
          return (self.sides_implicit_d = 100);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil, $ret_or_3 = nil, $ret_or_4 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = ($truthy(($ret_or_3 = ($truthy(($ret_or_4 = self.$eval_ad(command))) ? ($ret_or_4) : (self.$eval_ab(command))))) ? ($ret_or_3) : (self.$eval_orp(command))))) ? ($ret_or_2) : (self.$eval_worsening(command)))))) {
            return $ret_or_1
          } else {
            return self.$eval_addiction(command)
          }
        });
        self.$private();
        (function($base, $parent_nesting) {
          var self = $module($base, 'Status');

          var $nesting = [self].concat($parent_nesting);

          
          $const_set($nesting[0], 'CRITICAL', 1);
          $const_set($nesting[0], 'SUCCESS', 2);
          $const_set($nesting[0], 'FAILURE', 3);
          return $const_set($nesting[0], 'ERROR', 4);
        })($nesting[0], $nesting);
        $const_set($nesting[0], 'STATUS_NAME', $hash($$$($$('Status'), 'CRITICAL'), "クリティカル！", $$$($$('Status'), 'SUCCESS'), "成功", $$$($$('Status'), 'FAILURE'), "失敗", $$$($$('Status'), 'ERROR'), "エラー").$freeze());
        
        $def(self, '$check_roll', function $$check_roll(roll_result, target) {
          var success = nil, crierror = nil, result = nil;

          
          success = $rb_le(roll_result, target);
          crierror = ($truthy($rb_le(roll_result, 10)) ? ("Critical") : ($truthy($rb_ge(roll_result, 91)) ? ("Error") : ("Neutral")));
          result = (($truthy(success) && ($eqeq(crierror, "Critical"))) ? ($$$($$('Status'), 'CRITICAL')) : (($truthy(success) && ($eqeq(crierror, "Neutral"))) ? ($$$($$('Status'), 'SUCCESS')) : (($truthy(success) && ($eqeq(crierror, "Error"))) ? ($$$($$('Status'), 'SUCCESS')) : (($not(success) && ($eqeq(crierror, "Critical"))) ? ($$$($$('Status'), 'FAILURE')) : (($not(success) && ($eqeq(crierror, "Neutral"))) ? ($$$($$('Status'), 'FAILURE')) : (($not(success) && ($eqeq(crierror, "Error"))) ? ($$$($$('Status'), 'ERROR')) : nil))))));
          return result;
        });
        
        $def(self, '$eval_ad', function $$eval_ad(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, times = nil, sides = nil, target = nil;

          
          m = /^([-+*\/\d]*)AD(\d*)<=([-+*\/\d]+)$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          times = m['$[]'](1);
          sides = m['$[]'](2);
          target = $$('Arithmetic').$eval(m['$[]'](3), self.round_type);
          times = ($not(times['$empty?']()) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (1));
          sides = ($not(sides['$empty?']()) ? (sides.$to_i()) : (100));
          return self.$roll_ad(command, times, sides, target);} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$eval_ab', function $$eval_ab(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, times = nil, sides = nil, target = nil, type = nil, type_status = nil;

          
          m = /^([-+*\/\d]*)AB(\d*)<=([-+*\/\d]+)(?:--([^\d\s]+)([0-2])?)?$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          times = m['$[]'](1);
          sides = m['$[]'](2);
          target = $$('Arithmetic').$eval(m['$[]'](3), self.round_type);
          type = m['$[]'](4);
          type_status = m['$[]'](5);
          times = ($not(times['$empty?']()) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (1));
          sides = ($not(sides['$empty?']()) ? (sides.$to_i()) : (100));
          if ($not(type_status['$nil?']())) {
            type_status = type_status.$to_i()
          } else if ($eqeq(type, "SNIPER")) {
            type_status = 1
          } else {
            type_status = 0
          };
          if ($truthy(type['$nil?']())) {
            return self.$roll_ab(command, times, sides, target)
          } else {
            return self.$roll_ab_withtype(command, times, sides, target, type, type_status)
          };} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$eval_orp', function $$eval_orp(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, $ret_or_1 = nil, endurance = nil, oripathy = nil, times_mod = nil, target_mod = nil;

          
          m = /^ORP(?<END>[-+*\/\d]+)@(?<ORP>[-+*\/\d]+)(?:\+D(?<DICE>[-+*\/\d]+))?(?:\+T(?<TGT>[-+*\/\d]+))?$/.$match(command);
          m = ($truthy(($ret_or_1 = m)) ? ($ret_or_1) : (/^ORP(?<END>[-+*\/\d]+)@(?<ORP>[-+*\/\d]+)(?:\+T(?<TGT>[-+*\/\d]+))?(?:\+D(?<DICE>[-+*\/\d]+))?$/.$match(command)));
          if (!$truthy(m)) {
            return nil
          };
          endurance = $$('Arithmetic').$eval(m['$[]']("END"), self.round_type);
          oripathy = $$('Arithmetic').$eval(m['$[]']("ORP"), self.round_type);
          times_mod = ($not(m['$[]'](3)['$nil?']()) ? ($$('Arithmetic').$eval(m['$[]']("DICE"), self.round_type)) : (0));
          target_mod = ($not(m['$[]'](4)['$nil?']()) ? ($$('Arithmetic').$eval(m['$[]']("TGT"), self.round_type)) : (0));
          return self.$roll_orp(command, endurance, oripathy, times_mod, target_mod);} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$roll_ad', function $$roll_ad(command, times, sides, target) {
          var self = this, dice_list = nil, total = nil, result = nil, result_text = nil, $ret_or_1 = nil;

          
          dice_list = self.randomizer.$roll_barabara(times, sides).$sort();
          total = dice_list.$sum();
          result = self.$check_roll(total, target);
          if ($eqeq(times, 1)) {
            result_text = "(" + (command) + ") ＞ " + (dice_list.$join(",")) + " ＞ " + ($$('STATUS_NAME')['$[]'](result))
          } else {
            result_text = "(" + (command) + ") ＞ " + (total) + "[" + (dice_list.$join(",")) + "] ＞ " + ($$('STATUS_NAME')['$[]'](result))
          };
          if ($eqeqeq($$$($$('Status'), 'CRITICAL'), ($ret_or_1 = result))) {
            return $$('Result').$critical(result_text)
          } else if ($eqeqeq($$$($$('Status'), 'SUCCESS'), $ret_or_1)) {
            return $$('Result').$success(result_text)
          } else if ($eqeqeq($$$($$('Status'), 'FAILURE'), $ret_or_1)) {
            return $$('Result').$failure(result_text)
          } else if ($eqeqeq($$$($$('Status'), 'ERROR'), $ret_or_1)) {
            return $$('Result').$fumble(result_text)
          } else {
            return nil
          };
        });
        
        $def(self, '$roll_ab', function $$roll_ab(command, times, sides, target) {
          var $a, $b, self = this, dice_list = nil, success_count = nil, critical_count = nil, error_count = nil, result_count = nil, result_text = nil;

          
          dice_list = self.randomizer.$roll_barabara(times, sides).$sort();
          $b = self.$process_ab(dice_list, target), $a = $to_ary($b), (success_count = ($a[0] == null ? nil : $a[0])), (critical_count = ($a[1] == null ? nil : $a[1])), (error_count = ($a[2] == null ? nil : $a[2])), $b;
          result_count = $rb_minus($rb_plus(success_count, critical_count), error_count);
          result_text = "(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "] ＞ " + (success_count) + "+" + (critical_count) + "C-" + (error_count) + "E ＞ 成功数" + (result_count);
          return $send($$('Result').$new(), 'tap', [], function $$1(r){var $c;

            
            if (r == null) r = nil;
            r['$text='](result_text);
            r['$condition=']($rb_gt(result_count, 0));
            r['$critical=']($rb_gt(critical_count, 0));
            return ($c = [$rb_gt(error_count, 0)], $send(r, 'fumble=', $c), $c[$c.length - 1]);});
        });
        
        $def(self, '$roll_ab_withtype', function $$roll_ab_withtype(command, times, sides, target, type, type_status) {
          var $a, $b, self = this, dice_list = nil, success_count = nil, critical_count = nil, error_count = nil, result_count = nil, result_mod = nil, result_text = nil;

          
          dice_list = self.randomizer.$roll_barabara(times, sides).$sort();
          $b = self.$process_ab(dice_list, target), $a = $to_ary($b), (success_count = ($a[0] == null ? nil : $a[0])), (critical_count = ($a[1] == null ? nil : $a[1])), (error_count = ($a[2] == null ? nil : $a[2])), $b;
          result_count = $rb_minus($rb_plus(success_count, critical_count), error_count);
          
          switch (type.valueOf()) {
            case "SNI":
              if (($eqeq(type_status, 0) && ($truthy($rb_gt(result_count, 0))))) {
                result_mod = 1
              } else {
                result_mod = 0
              }
              break;
            case "SNIPER":
              if (($neqeq(type_status, 0) && ($truthy($rb_gt(result_count, 0))))) {
                result_mod = 1
              } else {
                result_mod = 0
              }
              break;
            default:
              nil
          };
          if ($not(result_mod['$nil?']())) {
            
            result_count = $rb_plus(result_count, result_mod);
            result_text = "(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "] ＞ " + (success_count) + "+" + (critical_count) + "C-" + (error_count) + "E+" + (result_mod) + "(" + (type) + ") ＞ 成功数" + (result_count);
          } else {
            result_text = "(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "] ＞ " + (success_count) + "+" + (critical_count) + "C-" + (error_count) + "E ＞ 成功数" + (result_count)
          };
          return $send($$('Result').$new(), 'tap', [], function $$2(r){var $c;

            
            if (r == null) r = nil;
            r['$text='](result_text);
            r['$condition=']($rb_gt(result_count, 0));
            r['$critical=']($rb_gt(critical_count, 0));
            return ($c = [$rb_gt(error_count, 0)], $send(r, 'fumble=', $c), $c[$c.length - 1]);});
        });
        $const_set($nesting[0], 'ENDURANCE_LEVEL_TABLE', [20, 40, 70, 90, $$$($$('Float'), 'INFINITY')].$freeze());
        $const_set($nesting[0], 'ORP_TIMES_TABLE', [1, 2, 2, 3, 4].$freeze());
        
        $def(self, '$roll_orp', function $$roll_orp(command, endurance, oripathy, times_mod, target_mod) {
          var self = this, sides = nil, endurance_level = nil, original_times = nil, times = nil, oripathy_stage = nil, original_target = nil, target = nil, dice_and_target_text = nil, result_texts = nil, dice_list = nil, success_count = nil;

          
          sides = 100;
          endurance_level = $send($$('ENDURANCE_LEVEL_TABLE'), 'find_index', [], function $$3(n){
            
            if (n == null) n = nil;
            return $rb_le(endurance, n);});
          original_times = $$('ORP_TIMES_TABLE')['$[]'](endurance_level);
          times = $rb_plus(original_times, times_mod);
          if ($truthy($rb_le(oripathy, 20))) {
            return $$('Result').$new("(" + (command) + ") ＞ 鉱石病判定が発生しない侵食度です。侵食度は21以上を指定してください。")
          };
          oripathy_stage = $rb_minus($rb_divide(oripathy, 20.0).$ceil(), 1);
          original_target = $rb_minus($rb_minus(80, $rb_times(oripathy_stage, 20)), $rb_times($rb_minus(oripathy, $rb_times(oripathy_stage, 20)), 5));
          target = $rb_plus(original_target, target_mod);
          dice_and_target_text = $rb_plus($rb_plus($rb_plus("ダイス数" + (original_times), ($truthy($rb_gt(times_mod, 0)) ? ("+" + (times_mod)) : (""))), "、判定値" + (original_target)), ($truthy($rb_gt(target_mod, 0)) ? ("+" + (target_mod)) : ("")));
          result_texts = ["(" + (command) + ")", dice_and_target_text, "" + (times) + "B100<=" + (target)];
          if ($truthy($rb_le(target, 0))) {
            
            result_texts = $rb_plus(result_texts, ["自動失敗！"]);
            return $$('Result').$failure(result_texts.$join(" ＞ "));
          };
          dice_list = self.randomizer.$roll_barabara(times, sides).$sort();
          success_count = $send(dice_list, 'count', [], function $$4(n){
            
            if (n == null) n = nil;
            return $rb_le(n, target);});
          if ($truthy($rb_gt(success_count, 0))) {
            
            result_texts = $rb_plus(result_texts, ["[" + (dice_list.$join(",")) + "]", "成功数" + (success_count), "成功"]);
            return $$('Result').$success(result_texts.$join(" ＞ "));
          } else {
            
            result_texts = $rb_plus(result_texts, ["[" + (dice_list.$join(",")) + "]", "成功数" + (success_count), "失敗"]);
            return $$('Result').$failure(result_texts.$join(" ＞ "));
          };
        });
        
        $def(self, '$process_ab', function $$process_ab(dice_list, target) {
          var self = this, success_count = nil, critical_count = nil, error_count = nil;

          
          success_count = 0;
          critical_count = 0;
          error_count = 0;
          $send(dice_list, 'each', [], function $$5(value){var self = $$5.$$s == null ? this : $$5.$$s, $ret_or_1 = nil;

            
            if (value == null) value = nil;
            if ($eqeqeq($$$($$('Status'), 'CRITICAL'), ($ret_or_1 = self.$check_roll(value, target)))) {
              
              critical_count = $rb_plus(critical_count, 1);
              return (success_count = $rb_plus(success_count, 1));
            } else if ($eqeqeq($$$($$('Status'), 'SUCCESS'), $ret_or_1)) {
              return (success_count = $rb_plus(success_count, 1))
            } else if ($eqeqeq($$$($$('Status'), 'FAILURE'), $ret_or_1)) {
              return nil
            } else if ($eqeqeq($$$($$('Status'), 'ERROR'), $ret_or_1)) {
              return (error_count = $rb_plus(error_count, 1))
            } else {
              return nil
            };}, {$$s: self});
          return [success_count, critical_count, error_count];
        });
        $const_set($nesting[0], 'WORSENING_TABLE', ["末梢神経障害", "内臓機能不全", "精神症状"].$freeze());
        
        $def(self, '$eval_worsening', function $$eval_worsening(command) {
          var self = this, value = nil, chosen = nil, elapse = nil;

          
          if ($neqeq(command, "--WORSENING")) {
            return nil
          };
          value = self.randomizer.$roll_once(3);
          chosen = $$('WORSENING_TABLE')['$[]']($rb_minus(value, 1));
          elapse = $rb_plus(self.randomizer.$roll_once(6), 1);
          return "--WORSENING ＞ " + (chosen) + ": " + (elapse) + " rounds";
        });
        $const_set($nesting[0], 'ADDICTION_TABLE', ["脳神経障害", "多臓器不全", "急性精神症状"].$freeze());
        return $def(self, '$eval_addiction', function $$eval_addiction(command) {
          var self = this, value = nil, chosen = nil;

          
          if ($neqeq(command, "--ADDICTION")) {
            return nil
          };
          value = self.randomizer.$roll_once(3);
          chosen = $$('ADDICTION_TABLE')['$[]']($rb_minus(value, 1));
          return "--ADDICTION ＞ " + (chosen);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
