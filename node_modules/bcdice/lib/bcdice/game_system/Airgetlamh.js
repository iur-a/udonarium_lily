require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $send = Opal.send, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $rb_times = Opal.rb_times, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Airgetlamh');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Airgetlamh");
        $const_set($nesting[0], 'NAME', "朱の孤塔のエアゲトラム");
        $const_set($nesting[0], 'SORT_KEY', "あけのことうのえあけとらむ");
        $const_set($nesting[0], 'HELP_MESSAGE', "【Reg2.0『THE ANSWERER』～】\n" + "・調査判定（成功数を表示）：[n]AA[m]\n" + "・命中判定（ダメージ表示）：[n]AA[m]*p[+t][Cx]\n" + "【～Reg1.1『昇華』】\n" + "・調査判定（成功数を表示）：[n]AL[m]\n" + "・命中判定（ダメージ表示）：[n]AL[m]*p\n" + "----------------------------------------\n" + "[]内のコマンドは省略可能。\n" + "\n" + "「n」でダイス数（攻撃回数）を指定。省略時は「2」。\n" + "「m」で目標値を指定。省略時は「6」。\n" + "「p」で威力を指定。「*」は「x」で代用可。\n" + "「+t」でクリティカルトリガーを指定。省略可。\n" + "「Cx」でクリティカル値を指定。省略時は「1」、最大値は「3」、「0」でクリティカル無し。\n" + "\n" + "攻撃力指定で命中判定となり、成功数ではなく、ダメージを結果表示します。\n" + "クリティカルヒットの分だけ、自動で振り足し処理を行います。\n" + "（ALコマンドではクリティカル処理を行いません）\n" + "\n" + "【書式例】\n" + "・AL → 2d10で目標値6の調査判定。\n" + "・5AA7*12 → 5d10で目標値7、威力12の命中判定。\n" + "・AA7x28+5 → 2d10で目標値7、威力28、クリティカルトリガー5の命中判定。\n" + "・9aa5*10C2 → 9d10で目標値5、威力10、クリティカル値2の命中判定。\n" + "・15AAx4c0 → 15d10で目標値6、威力4、クリティカル無しの命中判定。\n");
        self.$register_prefix("\\d*A[AL]");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, diceCount = nil, $ret_or_1 = nil, target = nil, damage = nil, criticalTrigger = nil, criticalNumber = nil;

          
          if ($truthy(command['$=~'](/(\d+)?A(A|L)(\d+)?((x|\*)(\d+)(\+(\d+))?)?(C(\d+))?$/i))) {
            
            diceCount = ($truthy(($ret_or_1 = $$('Regexp').$last_match(1))) ? ($ret_or_1) : (2)).$to_i();
            target = ($truthy(($ret_or_1 = $$('Regexp').$last_match(3))) ? ($ret_or_1) : (6)).$to_i();
            damage = ($truthy(($ret_or_1 = $$('Regexp').$last_match(6))) ? ($ret_or_1) : (0)).$to_i();
            if ($eqeq($$('Regexp').$last_match(2), "L")) {
              
              criticalTrigger = 0;
              criticalNumber = 0;
            } else {
              
              criticalTrigger = ($truthy(($ret_or_1 = $$('Regexp').$last_match(8))) ? ($ret_or_1) : (0)).$to_i();
              criticalNumber = ($truthy(($ret_or_1 = $$('Regexp').$last_match(10))) ? ($ret_or_1) : (1)).$to_i();
            };
            if ($truthy($rb_gt(criticalNumber, 4))) {
              criticalNumber = 3
            };
            return self.$checkRoll(diceCount, target, damage, criticalTrigger, criticalNumber);
          };
          return nil;
        });
        return $def(self, '$checkRoll', function $$checkRoll(diceCount, target, damage, criticalTrigger, criticalNumber) {
          var self = this, totalSuccessCount = nil, totalCriticalCount = nil, text = nil, rollCount = nil, diceArray = nil, diceText = nil, successCount = nil, criticalCount = nil, result = nil, isDamage = nil, totalDamage = nil;

          
          totalSuccessCount = 0;
          totalCriticalCount = 0;
          text = "";
          rollCount = diceCount;
          while ($truthy($rb_gt(rollCount, 0))) {
          
            diceArray = self.randomizer.$roll_barabara(rollCount, 10).$sort();
            diceText = diceArray.$join(",");
            successCount = $send(diceArray, 'count', [], function $$1(i){
              
              if (i == null) i = nil;
              return $rb_le(i, target);});
            criticalCount = $send(diceArray, 'count', [], function $$2(i){
              
              if (i == null) i = nil;
              return $rb_le(i, criticalNumber);});
            totalSuccessCount = $rb_plus(totalSuccessCount, successCount);
            totalCriticalCount = $rb_plus(totalCriticalCount, criticalCount);
            if (!$truthy(text['$empty?']())) {
              text = $rb_plus(text, "+")
            };
            text = $rb_plus(text, "" + (successCount) + "[" + (diceText) + "]");
            rollCount = criticalCount;
          };
          result = "";
          isDamage = damage['$!='](0);
          if ($truthy(isDamage)) {
            
            totalDamage = $rb_plus($rb_times(totalSuccessCount, damage), $rb_times(totalCriticalCount, criticalTrigger));
            result = $rb_plus(result, "(" + (diceCount) + "D10<=" + (target) + ") ＞ " + (text) + " ＞ Hits：" + (totalSuccessCount) + "*" + (damage));
            if ($truthy($rb_gt(criticalTrigger, 0))) {
              result = $rb_plus(result, " + Trigger：" + (totalCriticalCount) + "*" + (criticalTrigger))
            };
            result = $rb_plus(result, " ＞ " + (totalDamage) + "ダメージ");
          } else {
            result = $rb_plus(result, "(" + (diceCount) + "D10<=" + (target) + ") ＞ " + (text) + " ＞ 成功数：" + (totalSuccessCount))
          };
          if ($truthy($rb_gt(totalCriticalCount, 0))) {
            result = $rb_plus(result, " / " + (totalCriticalCount) + "クリティカル")
          };
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
