require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $truthy = Opal.truthy, $send = Opal.send, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $rb_ge = Opal.rb_ge, $rb_le = Opal.rb_le, $rb_gt = Opal.rb_gt, $rb_times = Opal.rb_times, $neqeq = Opal.neqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Aionia');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Aionia");
        $const_set($nesting[0], 'NAME', "慈悲なきアイオニア");
        $const_set($nesting[0], 'SORT_KEY', "しひなきあいおにあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "- 技能判定（クリティカル・ファンブルなし）\n" + "AB{n}>={dif} n=10面ダイスの数、dif=難易度\n" + "- 技能判定（クリティカル・ファンブルあり）\n" + "ABT{n}>={dif} n=10面ダイスの数、dif=難易度\n" + "\n" + "例:AB2>=5        （一般技能を活用して難易度5の技能判定。 クリファンなし。）\n" + "例:ABT3>=15      （専門技能を活用して難易度15の技能判定。クリファンあり。）\n" + "例:AB1+2>=8      （一般技能を活用せず難易度8の技能判定。 ボーナスとして+2点の補正あり。  クリファンなし。）\n" + "例:ABT3-3>=10    （専門技能を活用して難易度10の技能判定。ペナルティとして-3点の補正あり。クリファンあり。）\n" + "例:ABT2>=4/8/12  （一般技能を活用して難易度4/8/12の段階的な技能判定。クリファンあり。）\n");
        self.$register_prefix("ABT?\\d+([\\+\\-]\\d+)?>=\\d+(\\/\\d+)*");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$roll_skills(command)
        });
        return $def(self, '$roll_skills', function $$roll_skills(command) {
          var self = this, m = nil, use_cf = nil, times = nil, bonus = nil, targets = nil, target = nil, min_target = nil, max_target = nil, dice_list = nil, dice_total = nil, total = nil, is_success = nil, has_critical = nil, has_fumble = nil, result = nil, times_suc = nil, bonus_text = nil, bonus_result = nil;

          
          m = /AB(T?)(\d+)([+-]\d+)?>=((\d+)((\/\d+)*))/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          use_cf = m['$[]'](1)['$!=']("");
          times = m['$[]'](2).$to_i();
          bonus = m['$[]'](3).$to_i();
          targets = $send(m['$[]'](4).$split("/"), 'map', [], "to_i".$to_proc());
          target = targets['$[]'](0);
          min_target = targets.$min();
          max_target = targets.$max();
          dice_list = self.randomizer.$roll_barabara(times, 10);
          dice_total = dice_list.$sum();
          total = $rb_plus(dice_total, bonus);
          is_success = false;
          has_critical = false;
          has_fumble = false;
          if ($eqeq(targets.$count(), 1)) {
            if ($truthy($rb_ge(total, target))) {
              
              is_success = true;
              if (($truthy($rb_ge(total, $rb_plus(target, 20))) && ($truthy(use_cf)))) {
                
                result = "クリティカル";
                has_critical = true;
              } else if ($truthy($rb_le(target, times))) {
                result = "自動成功"
              } else {
                result = "成功"
              };
            } else {
              
              is_success = false;
              if (($eqeq(dice_list.$count(1), times) && ($truthy(use_cf)))) {
                
                result = "ファンブル";
                has_fumble = true;
              } else if ($truthy($rb_gt(target, $rb_times(10, times)))) {
                result = "自動失敗"
              } else {
                result = "失敗"
              };
            }
          } else if ($truthy($rb_ge(total, min_target))) {
            
            is_success = true;
            if (($truthy($rb_ge(total, $rb_plus(max_target, 20))) && ($truthy(use_cf)))) {
              
              result = "クリティカル";
              has_critical = true;
            } else if ($truthy($rb_le(max_target, times))) {
              result = "自動成功"
            } else if ($truthy($rb_ge(total, max_target))) {
              result = "全成功"
            } else {
              
              times_suc = $send(targets, 'count', [], function $$1(x){
                
                if (x == null) x = nil;
                return $rb_le(x, total);});
              result = "" + (times_suc) + "段階成功";
            };
          } else {
            
            is_success = false;
            if (($eqeq(dice_list.$count(1), times) && ($truthy(use_cf)))) {
              
              result = "ファンブル";
              has_fumble = true;
            } else if ($truthy($rb_gt(min_target, $rb_times(10, times)))) {
              result = "自動失敗"
            } else {
              result = "失敗"
            };
          };
          bonus_text = "";
          bonus_result = "";
          if ($neqeq(bonus, 0)) {
            
            if ($truthy($rb_gt(bonus, 0))) {
              bonus_text = "+"
            };
            bonus_text = $rb_plus(bonus_text, bonus.$to_s());
            bonus_result = "" + (total) + " ＞ ";
          };
          return $send($$('Result').$new(), 'tap', [], function $$2(r){var $a;

            
            if (r == null) r = nil;
            r['$text=']("(" + (command) + ") ＞ " + (dice_total) + "[" + (dice_list.$join(",")) + "]" + (bonus_text) + " ＞ " + (bonus_result) + (result));
            r['$critical='](has_critical);
            r['$fumble='](has_fumble);
            r['$success='](is_success);
            return ($a = [is_success['$!']()], $send(r, 'failure=', $a), $a[$a.length - 1]);});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
