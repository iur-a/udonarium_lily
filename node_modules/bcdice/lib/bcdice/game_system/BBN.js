require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $send = Opal.send, $to_a = Opal.to_a, $rb_ge = Opal.rb_ge, $def = Opal.def, $rb_divide = Opal.rb_divide, $rb_gt = Opal.rb_gt, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'BBN');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = $proto.roll_times = $proto.modify = $proto.modify_str = $proto.difficulty = $proto.critical = $proto.fumble = nil;
        
        self.$register_prefix("\\d+BN");
        $const_set($nesting[0], 'ID', "BBN");
        $const_set($nesting[0], 'NAME', "BBNTRPG");
        $const_set($nesting[0], 'SORT_KEY', "ひいひいえぬTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定(xBN±y>=z[c,f])\n" + "　xD6の判定。クリティカル、ファンブルの自動判定を行います。\n" + "　1Dのクリティカル値とファンブル値は1。2Dのクリティカル値とファンブル値は2。\n" + "　nDのクリティカル値とファンブル値は n/2 の切り上げ。\n" + "　クリティカルとファンブルが同時に発生した場合、クリティカルを優先。\n" + "　x：xに振るダイス数を入力。\n" + "　y：yに修正値を入力。省略可能。\n" + "  z：zに目標値を入力。省略可能。\n" + "  c：クリティカルに必要なダイス目「6」の数の増減。省略可能。\n" + "  f：ファンブルに必要なダイス目「1」の数の増減。省略可能。\n" + "　例） 3BN+4　3BN>=8　3BN+1>=10[-1] 3BN+1>=10[,1] 3BN+1>=10[1,1]\n");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, dice_list = nil, dice = nil, dice_str = nil, total = nil, sequence = nil;

          
          if (!$truthy(self.$parse(command))) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(self.roll_times, 6);
          dice = dice_list.$sum();
          dice_str = dice_list.$join(",");
          total = $rb_plus(dice, self.modify);
          sequence = ["(" + (command) + ")", "" + (dice) + "[" + (dice_str) + "]" + (self.modify_str), total];
          if ($truthy(self['$critical_?'](dice_list))) {
            $send(sequence, 'push', ["クリティカル！"].concat($to_a(self.$additional_roll(dice_list.$count(6), total))))
          } else if ($truthy(self['$fumble_?'](dice_list))) {
            sequence.$push("ファンブル！")
          } else if ($truthy(self.difficulty)) {
            sequence.$push(($truthy($rb_ge(total, self.difficulty)) ? ("成功") : ("失敗")))
          };
          return sequence.$join(" ＞ ");
        });
        self.$private();
        
        $def(self, '$parse', function $$parse(command) {
          var $a, self = this, m = nil, $ret_or_1 = nil, base = nil;

          
          m = /^(\d+)BN([+-]\d+)?(>=(([+-]?\d+)))?(\[([+-]?\d+)?(,([+-]?\d+))?\])?/.$match(command);
          if (!$truthy(m)) {
            return false
          };
          self.roll_times = m['$[]'](1).$to_i();
          self.modify_str = ($truthy(($ret_or_1 = m['$[]'](2))) ? ($ret_or_1) : (""));
          self.modify = m['$[]'](2).$to_i();
          self.difficulty = ($a = m['$[]'](4), ($a === nil || $a == null) ? nil : $a.$to_i());
          base = self.$critical_base(self.roll_times);
          self.critical = $rb_plus(base, m['$[]'](7).$to_i());
          self.fumble = $rb_plus(base, m['$[]'](9).$to_i());
          return true;
        });
        
        $def(self, '$critical_base', function $$critical_base(roll_times) {
          
          
          switch (roll_times.valueOf()) {
            case 1:
            case 2:
              return roll_times
            default:
              return $rb_divide(roll_times.$to_f(), 2).$ceil()
          }
        });
        
        $def(self, '$critical_?', function $BBN_critical_$ques$1(dice_list) {
          var self = this;

          return $rb_ge(dice_list.$count(6), self.critical)
        });
        
        $def(self, '$fumble_?', function $BBN_fumble_$ques$2(dice_list) {
          var self = this;

          return $rb_ge(dice_list.$count(1), self.fumble)
        });
        return $def(self, '$additional_roll', function $$additional_roll(additional_dice, total) {
          var self = this, sequence = nil, reroll_count = nil, $ret_or_1 = nil, dice_list = nil, dice_total = nil, dice_str = nil;

          
          sequence = [];
          reroll_count = 0;
          while ($truthy(($truthy(($ret_or_1 = $rb_gt(additional_dice, 0))) ? ($rb_lt(reroll_count, 10)) : ($ret_or_1)))) {
          
            reroll_count = $rb_plus(reroll_count, 1);
            dice_list = self.randomizer.$roll_barabara(additional_dice, 6);
            dice_total = dice_list.$sum();
            dice_str = dice_list.$join(",");
            additional_dice = dice_list.$count(6);
            sequence.$push("" + (total) + "+" + (dice_total) + "[" + (dice_str) + "]");
            if ($truthy($rb_gt(additional_dice, 0))) {
              sequence.$push("追加クリティカル！")
            };
            total = $rb_plus(total, dice_total);
          };
          if ($truthy($rb_gt(additional_dice, 0))) {
            sequence.$push("無限ループ防止のため中断")
          };
          sequence.$push(total);
          if ($truthy(self.difficulty)) {
            sequence.$push(($truthy($rb_ge(total, self.difficulty)) ? ("成功") : ("失敗")))
          };
          return sequence;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
