require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $hash2 = Opal.hash2, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_le = Opal.rb_le, $rb_minus = Opal.rb_minus, $neqeq = Opal.neqeq, $eqeq = Opal.eqeq, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Agnostos');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Agnostos");
        $const_set($nesting[0], 'NAME', "アグノストス");
        $const_set($nesting[0], 'SORT_KEY', "あくのすとす");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 行為判定\n" + "  CDx>=t\n" + "  x: コンディションレベル（A~E もしくは 5~1)\n" + "  t: 目標値\n" + "  の成否とコンディションの変動量を判定します。\n" + "\n" + "■ 心拍のコンディションチェック\n" + "  HCDx\n" + "  x: コンディションレベル（C+, B+, A, B-, C-, もしくは 5~1）\n" + "  酸素の消費量、コンディションの変動量、気絶したかを判定します。\n" + "\n" + "■ 必殺技\n" + "  xSPy\n" + "  x: メインコンディション（A~E もしくは 5~1)\n" + "  y: サブコンディション（A~E もしくは 5~1)\n" + "  必殺技のダメージ量を判定します。\n");
        self.$register_prefix("CD", "HCD", "[A-E1-5]SP[A-E1-5]");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$condition_roll(command))) ? ($ret_or_2) : (self.$heart_condition_roll(command)))))) {
            return $ret_or_1
          } else {
            return self.$special_roll(command)
          }
        });
        self.$private();
        
        $def(self, '$condition_roll', function $$condition_roll(command) {
          var self = this, parser = nil, parsed = nil, condition_level = nil, sides = nil, value = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/CD[A-E1-5]/, $hash2(["round_type"], {"round_type": self.round_type})).$disable_modifier().$restrict_cmp_op_to(">=");
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          condition_level = self.$to_condition_level(parsed.$command()['$[]'](-1));
          sides = self.$to_sides(condition_level);
          value = self.randomizer.$roll_once(sides);
          return $send($$('Result').$new(), 'tap', [], function $$1(r){var $a, self = $$1.$$s == null ? this : $$1.$$s;

            
            if (r == null) r = nil;
            r['$critical='](self['$critical?'](sides, value));
            r['$fumble='](self['$fumble?'](sides, value));
            r['$condition=']($rb_ge(value, parsed.$target_number()));
            return ($a = [["(CD" + (condition_level) + ">=" + (parsed.$target_number()) + ")", "(1D" + (sides) + ">=" + (parsed.$target_number()) + ")", value.$to_s(), ($truthy(r['$success?']()) ? ("成功") : ("失敗")), self.$condition_change(sides, value)].$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);}, {$$s: self});
        });
        
        $def(self, '$to_condition_level', function $$to_condition_level(char$) {
          
          
          switch (char$.valueOf()) {
            case "5":
              return "A"
            case "4":
              return "B"
            case "3":
              return "C"
            case "2":
              return "D"
            case "1":
              return "E"
            default:
              return char$
          }
        });
        
        $def(self, '$to_sides', function $$to_sides(condition) {
          
          
          switch (condition.valueOf()) {
            case "A":
              return 12
            case "B":
              return 10
            case "C":
              return 8
            case "D":
              return 6
            default:
              return 4
          }
        });
        
        $def(self, '$condition_change', function $$condition_change(sides, value) {
          var self = this;

          if ($truthy(self['$critical?'](sides, value))) {
            return "コンディション：2段階上昇（クリティカル）"
          } else if ($truthy(self['$fumble?'](sides, value))) {
            return "コンディション：2段階下降（ファンブル）"
          } else if (($neqeq(sides, 12) && ($truthy($rb_le($rb_minus(sides, value), 1))))) {
            return "コンディション：1段階上昇"
          } else if (($eqeq(sides, 12) && ($truthy($rb_le(value, 6))))) {
            return "コンディション：1段階下降"
          } else if (($eqeq(sides, 10) && ($truthy($rb_le(value, 3))))) {
            return "コンディション：1段階下降"
          } else if (($eqeq(sides, 8) && ($truthy($rb_le(value, 2))))) {
            return "コンディション：1段階下降"
          } else {
            return "コンディション：変動なし"
          }
        });
        
        $def(self, '$critical?', function $Agnostos_critical$ques$2(sides, value) {
          var $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = sides['$!='](12)))) {
            return sides['$=='](value)
          } else {
            return $ret_or_1
          }
        });
        
        $def(self, '$fumble?', function $Agnostos_fumble$ques$3(sides, value) {
          var $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = sides['$!='](4)))) {
            return value['$=='](1)
          } else {
            return $ret_or_1
          }
        });
        
        $def(self, '$heart_condition_roll', function $$heart_condition_roll(command) {
          var self = this, m = nil, suffix = nil, condition_level = nil, sides = nil, value = nil;

          
          m = /^HCD([A1-5]|[BC][+-])$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          suffix = m['$[]'](1);
          condition_level = self.$to_heart_condition_level(suffix);
          sides = self.$to_heart_sides(condition_level);
          value = self.randomizer.$roll_once(sides);
          return $send($$('Result').$new(), 'tap', [], function $$4(r){var $a, self = $$4.$$s == null ? this : $$4.$$s;

            
            if (r == null) r = nil;
            r['$critical='](self['$critical?'](sides, value));
            r['$fumble='](self['$fumble?'](sides, value));
            return ($a = [["(HCD" + (condition_level) + ")", "(1D" + (sides) + ")", value.$to_s(), self.$heart_condition_change(sides, value)].$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);}, {$$s: self});
        });
        
        $def(self, '$to_heart_condition_level', function $$to_heart_condition_level(char$) {
          
          
          switch (char$.valueOf()) {
            case "5":
              return "C+"
            case "4":
              return "B+"
            case "3":
              return "A"
            case "2":
              return "B-"
            case "1":
              return "C-"
            default:
              return char$
          }
        });
        
        $def(self, '$to_heart_sides', function $$to_heart_sides(condition) {
          
          
          switch (condition.valueOf()) {
            case "C+":
              return 12
            case "B+":
              return 10
            case "A":
              return 8
            case "B-":
              return 6
            default:
              return 4
          }
        });
        
        $def(self, '$fainted?', function $Agnostos_fainted$ques$5(sides, value) {
          
          
          switch (sides.valueOf()) {
            case 12:
              return $rb_ge(value, 7)
            case 10:
              return $rb_ge(value, 10)
            case 8:
              return false
            case 6:
              return $rb_le(value, 1)
            default:
              return $rb_le(value, 2)
          }
        });
        
        $def(self, '$heart_condition_change', function $$heart_condition_change(sides, value) {
          var self = this;

          if ($truthy(self['$fainted?'](sides, value))) {
            return "気絶"
          } else {
            return self.$condition_change(sides, value)
          }
        });
        
        $def(self, '$special_roll', function $$special_roll(command) {
          var self = this, m = nil, times_conditon_level = nil, sides_conditon_level = nil, times = nil, sides = nil, dice_list = nil, value = nil;

          
          m = /^([A-E1-5])SP([A-E1-5])$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          times_conditon_level = self.$to_condition_level(m['$[]'](1));
          sides_conditon_level = self.$to_condition_level(m['$[]'](2));
          times = self.$to_times(times_conditon_level);
          sides = self.$to_sides(sides_conditon_level);
          dice_list = self.randomizer.$roll_barabara(times, sides);
          value = dice_list.$sum();
          return ["(" + (times_conditon_level) + "SP" + (sides_conditon_level) + ")", "(" + (times) + "D" + (sides) + ")", "" + (value) + "[" + (dice_list.$join(",")) + "]", value.$to_s()].$join(" ＞ ");
        });
        return $def(self, '$to_times', function $$to_times(condition) {
          
          
          switch (condition.valueOf()) {
            case "A":
              return 5
            case "B":
              return 4
            case "C":
              return 3
            case "D":
              return 2
            default:
              return 1
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
