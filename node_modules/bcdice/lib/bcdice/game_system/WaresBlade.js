require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $def = Opal.def, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'WaresBlade');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "WaresBlade");
        $const_set($nesting[0], 'NAME', "ワースブレイド");
        $const_set($nesting[0], 'SORT_KEY', "わあすふれいと");
        $const_set($nesting[0], 'HELP_MESSAGE', "nD10>=m 方式の判定で成否、完全成功、完全失敗を自動判定します。\n");
        return $def(self, '$result_nd10', function $$result_nd10(_total, _dice_total, dice_list, cmp_op, _target) {
          
          
          if (!$eqeq(cmp_op, ">=")) {
            return nil
          };
          if ($eqeq(dice_list.$count(10), dice_list.$size())) {
            return $$('Result').$critical("完全成功")
          } else if ($eqeq(dice_list.$count(1), dice_list.$size())) {
            return $$('Result').$fumble("絶対失敗")
          } else {
            return nil
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
