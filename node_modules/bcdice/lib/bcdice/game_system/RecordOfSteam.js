require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $rb_ge = Opal.rb_ge, $to_ary = Opal.to_ary, $def = Opal.def, $rb_gt = Opal.rb_gt, $neqeq = Opal.neqeq, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $rb_le = Opal.rb_le, $rb_times = Opal.rb_times, $send = Opal.send, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'RecordOfSteam');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "RecordOfSteam");
        $const_set($nesting[0], 'NAME', "Record of Steam");
        $const_set($nesting[0], 'SORT_KEY', "れこおとおふすちいむ");
        $const_set($nesting[0], 'HELP_MESSAGE', "2S2@1\n" + "RecordOfSteam : (2S2@1) ＞ 1,2,3,4 ＞ 1回転 ＞ 成功数2\n" + "\n" + "4S3@2\n" + "RecordOfSteam : (4S3@2) ＞ 2,1,2,4,4,4,2,3,4,5,6,6 ＞ 4回転 ＞ 成功数5\n");
        self.$register_prefix("\\d+S\\d+");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, $b, self = this, diceCount = nil, targetNumber = nil, criticalValue = nil, $ret_or_1 = nil, specialValue = nil, rollResult = nil, successCount = nil, roundCount = nil, specialCount = nil, fumbleCount = nil, output = nil, roundCountText = nil, successText = nil, specialText = nil, fumbleText = nil, result = nil;

          
          if (!$truthy(/(\d+)[sS](\d+)(@(\d+))?/i['$=~'](command))) {
            return "1"
          };
          diceCount = $$('Regexp').$last_match(1).$to_i();
          targetNumber = $$('Regexp').$last_match(2).$to_i();
          criticalValue = $$('Regexp').$last_match(4);
          criticalValue = ($truthy(($ret_or_1 = criticalValue)) ? ($ret_or_1) : (1));
          criticalValue = criticalValue.$to_i();
          if ($truthy($rb_ge(diceCount, 150))) {
            return "(多分)無限個なので振れません！ ヤメテクダサイ、(プロセスが)死んでしまいますっ"
          };
          if ($truthy($rb_ge(criticalValue, 3))) {
            return "(多分)無限個なので振れません！ ヤメテクダサイ、(プロセスが)死んでしまいますっ"
          };
          specialValue = criticalValue;
          $b = self.$getDiceRollResult(diceCount, targetNumber, criticalValue, specialValue), $a = $to_ary($b), (rollResult = ($a[0] == null ? nil : $a[0])), (successCount = ($a[1] == null ? nil : $a[1])), (roundCount = ($a[2] == null ? nil : $a[2])), (specialCount = ($a[3] == null ? nil : $a[3])), (fumbleCount = ($a[4] == null ? nil : $a[4])), $b;
          output = "(" + (command) + ") ＞ " + (rollResult);
          roundCountText = self.$getRoundCountText(roundCount);
          successText = self.$getSuccessText(successCount);
          specialText = self.$getSpecialText(specialCount);
          fumbleText = self.$getFumbleText(fumbleCount);
          result = "" + (output) + (roundCountText) + (specialText) + (successText) + (fumbleText);
          return result;
        });
        
        $def(self, '$getDiceRollResult', function $$getDiceRollResult(diceCount, targetNumber, criticalValue, specialValue) {
          var self = this, successCount = nil, roundCount = nil, rollResult = nil, specialCount = nil, specialFlag = nil, fumbleCount = nil, fumbleFlag = nil, diceList = nil, diceListText = nil;

          
          successCount = 0;
          roundCount = 0;
          rollResult = "";
          specialCount = 0;
          specialFlag = false;
          fumbleCount = 0;
          fumbleFlag = false;
          while ($truthy($rb_gt(diceCount, 0))) {
          
            diceList = self.randomizer.$roll_barabara(diceCount, 6);
            diceListText = diceList.$join(",");
            if ($neqeq(rollResult, "")) {
              rollResult = $rb_plus(rollResult, ",")
            };
            rollResult = $rb_plus(rollResult, diceListText);
            if (($eqeq(diceList.$uniq().$length(), 1) && ($eqeq(roundCount, 0)))) {
              if ($truthy($rb_le(diceList.$uniq().$first(), specialValue))) {
                specialFlag = true
              } else if ($eqeq(diceList.$uniq().$first(), 6)) {
                fumbleFlag = true
              }
            };
            self.$debug("diceList", diceList);
            if ($truthy(specialFlag)) {
              
              specialCount = 1;
              successCount = $rb_times(diceCount, 3);
              return [rollResult, successCount, roundCount, specialCount, fumbleCount];
            } else if ($truthy(fumbleFlag)) {
              
              fumbleCount = 1;
              return [rollResult, successCount, roundCount, specialCount, fumbleCount];
            };
            diceCount = 0;
            $send(diceList, 'map', [], function $$1(diceValue){var self = $$1.$$s == null ? this : $$1.$$s;

              
              if (diceValue == null) diceValue = nil;
              self.$debug("diceValue", diceValue);
              self.$debug("criticalValue", criticalValue);
              self.$debug("specialValue", specialValue);
              if ($truthy($rb_le(diceValue, criticalValue))) {
                
                diceCount = $rb_plus(diceCount, 2);
                roundCount = $rb_plus(roundCount, 1);
              };
              if ($truthy($rb_le(diceValue, targetNumber))) {
                return (successCount = $rb_plus(successCount, 1))
              } else {
                return nil
              };}, {$$s: self});
          };
          return [rollResult, successCount, roundCount, specialCount, fumbleCount];
        });
        
        $def(self, '$getRoundCountText', function $$getRoundCountText(roundCount) {
          
          
          if ($truthy($rb_le(roundCount, 0))) {
            return ""
          };
          return " ＞ " + (roundCount) + "回転";
        });
        
        $def(self, '$getSuccessText', function $$getSuccessText(successCount) {
          
          
          if ($truthy($rb_gt(successCount, 0))) {
            return " ＞ 成功数" + (successCount)
          };
          return " ＞ 失敗";
        });
        
        $def(self, '$getSpecialText', function $$getSpecialText(specialCount) {
          
          if ($eqeq(specialCount, 1)) {
            return " ＞ スペシャル"
          } else {
            return nil
          }
        });
        return $def(self, '$getFumbleText', function $$getFumbleText(fumbleCount) {
          
          if ($eqeq(fumbleCount, 1)) {
            return " ＞ ファンブル"
          } else {
            return nil
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
