require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $hash2 = Opal.hash2, $rb_ge = Opal.rb_ge, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $rb_minus = Opal.rb_minus, $send = Opal.send, $neqeq = Opal.neqeq, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'BeginningIdol2022');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "BeginningIdol2022");
        $const_set($nesting[0], 'NAME', "ビギニングアイドル（2022年改訂版）");
        $const_set($nesting[0], 'SORT_KEY', "ひきにんくあいとる2022");
        $const_set($nesting[0], 'HELP_MESSAGE', "これは、2022年に大判サイズで発売された『駆け出しアイドルRPG ビギニングアイドル 基本ルールブック』に対応したコマンドです。\n" + "\n" + "・行為判定　BIn@c#f+m>=t\n" + "　nD6をダイスロールし、行為判定に成功したかを出力します。スペシャルとファンブルの判定も行います。\n" + "　　n: ダイス数（省略時 2)\n" + "　　c: スペシャル値（省略時 12)\n" + "　　f: ファンブル値（省略時 2)\n" + "　　m: 修正値（省略可)\n" + "　　t: 目標値\n" + "\n" + "・パフォーマンス判定　PDn+m\n" + "　nD6をダイスロールし、パフォーマンス値を出力します。パーフェクトミラクルとミラクルの判定も行います。\n" + "　　n: ダイス数\n" + "　　m: 修正値（省略可)\n" + "\n" + "・シンフォニー　xxxPDn+m\n" + "　nD6をダイスロールし、場に残っているダイスを加味してパフォーマンス値を出力します。\n" + "　パーフェクトミラクルとミラクルシンクロの判定も行います。\n" + "　　xxx: 場に残っているダイスの出目を列挙したもの\n" + "　　n: ダイス数\n" + "　　m: 修正値（省略可)\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          return (self.d66_sort_type = $$$($$('D66SortType'), 'ASC'));
        });
        self.$register_prefix("BI", "PD", "[1-6]+PD");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$roll_skill_check(command))) ? ($ret_or_2) : (self.$roll_performance_check(command)))))) {
            return $ret_or_1
          } else {
            return self.$roll_symphony_check(command)
          }
        });
        self.$private();
        
        $def(self, '$roll_skill_check', function $$roll_skill_check(command) {
          var self = this, parser = nil, parsed = nil, dice_times = nil, $ret_or_1 = nil, critical = nil, fumble = nil, dice_list = nil, dice_total = nil, is_critical = nil, is_fumble = nil, total = nil, result = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("BI", $hash2(["round_type"], {"round_type": self.round_type})).$enable_suffix_number().$enable_critical().$enable_fumble().$restrict_cmp_op_to(">=");
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          dice_times = ($truthy(($ret_or_1 = parsed.$suffix_number())) ? ($ret_or_1) : (2));
          critical = ($truthy(($ret_or_1 = parsed.$critical())) ? ($ret_or_1) : (12));
          fumble = ($truthy(($ret_or_1 = parsed.$fumble())) ? ($ret_or_1) : (2));
          dice_list = self.randomizer.$roll_barabara(dice_times, 6).$sort();
          dice_total = dice_list.$sum();
          is_critical = $rb_ge(dice_total, critical);
          is_fumble = ($truthy(($ret_or_1 = is_critical['$!']())) ? ($rb_le(dice_total, fumble)) : ($ret_or_1));
          total = $rb_plus(dice_total, parsed.$modify_number());
          result = ($truthy(is_critical) ? ($$('Result').$critical("スペシャル(PCは【思い出】を1つ獲得する)")) : ($truthy(is_fumble) ? ($$('Result').$fumble("ファンブル(【思い出】を1つ獲得し、ファンブル表を振る)")) : ($truthy($rb_ge(total, parsed.$target_number())) ? ($$('Result').$success("成功")) : ($$('Result').$failure("失敗")))));
          result['$text=']("(" + (parsed) + ") ＞ " + (dice_total) + "[" + (dice_list.$join(",")) + "]" + ($$('Format').$modifier(parsed.$modify_number())) + " ＞ " + (total) + " ＞ " + (result.$text()));
          return result;
        });
        
        $def(self, '$roll_performance_check', function $$roll_performance_check(command) {
          var self = this, m = nil, suffix_number = nil, modifier = nil, is_extension = nil, dice_times = nil, extension_bonus = nil, dice_list = nil, uniqed = nil, is_perfect_miracle = nil, is_miracle = nil, result_label = nil;

          
          m = /^PD(\d+)([+-]\d+)?$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          suffix_number = m['$[]'](1).$to_i();
          modifier = m['$[]'](2).$to_i();
          is_extension = $rb_ge(suffix_number, 7);
          dice_times = ($truthy(is_extension) ? (6) : (suffix_number));
          extension_bonus = ($truthy(is_extension) ? ($rb_minus(suffix_number, dice_times)) : (0));
          if ($truthy($rb_le(dice_times, 0))) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(dice_times, 6).$sort();
          uniqed = self.$select_uniqs(dice_list).$sort();
          is_perfect_miracle = uniqed['$==']([1, 2, 3, 4, 5, 6]);
          is_miracle = uniqed['$empty?']();
          result_label = ($truthy(is_perfect_miracle) ? ("【パーフェクトミラクル】" + ($rb_plus($rb_plus(30, extension_bonus), modifier))) : ($truthy(is_miracle) ? ("【ミラクル】" + ($rb_plus($rb_plus(10, extension_bonus), modifier))) : ($rb_plus($rb_plus(uniqed.$sum(), extension_bonus), modifier).$to_s())));
          if ($truthy(is_extension)) {
            result_label = $rb_plus(result_label, " (エクステンション: " + (extension_bonus) + "個まで振りなおし可能)")
          };
          return $send($$('Result').$new(), 'tap', [], function $$1(result){var $a, $ret_or_1 = nil;

            
            if (result == null) result = nil;
            result['$critical='](($truthy(($ret_or_1 = is_perfect_miracle)) ? ($ret_or_1) : (is_miracle)));
            return ($a = [["(" + (command) + ")", "パフォーマンス判定", "[" + (dice_list.$join(",")) + "]" + ($$('Format').$modifier(extension_bonus)) + ($$('Format').$modifier(modifier)), ($neqeq(dice_list.$size(), uniqed.$size()) ? ("[" + (uniqed.$join(",")) + "]" + ($$('Format').$modifier(extension_bonus)) + ($$('Format').$modifier(modifier))) : nil), result_label].$compact().$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);});
        });
        
        $def(self, '$select_uniqs', function $$select_uniqs(array) {
          
          return $send($send($send(array, 'group_by', [], "itself".$to_proc()).$to_a(), 'select', [], function $$2(_, arr){
            
            if (_ == null) _ = nil;
            if (arr == null) arr = nil;
            return arr.$size()['$=='](1);}), 'map', [], function $$3(key, _){
            
            if (key == null) key = nil;
            if (_ == null) _ = nil;
            return key;})
        });
        return $def(self, '$roll_symphony_check', function $$roll_symphony_check(command) {
          var self = this, m = nil, carries = nil, dice_times = nil, modifier = nil, dice_list = nil, uniqed = nil, is_perfect_miracle = nil, is_miracle_synchro = nil, result_label = nil;

          
          m = /^([1-6]+)PD([1-6])([+-]\d+)?$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          carries = $send(m['$[]'](1).$chars(), 'map', [], "to_i".$to_proc()).$sort();
          dice_times = m['$[]'](2).$to_i();
          modifier = m['$[]'](3).$to_i();
          dice_list = self.randomizer.$roll_barabara(dice_times, 6).$sort();
          uniqed = self.$select_uniqs($rb_plus(carries, dice_list)).$sort();
          is_perfect_miracle = uniqed['$==']([1, 2, 3, 4, 5, 6]);
          is_miracle_synchro = uniqed['$empty?']();
          result_label = ($truthy(is_perfect_miracle) ? ("【パーフェクトミラクル】" + ($rb_plus(30, modifier))) : ($truthy(is_miracle_synchro) ? ("【ミラクルシンクロ】" + ($rb_plus(20, modifier))) : ($rb_plus(uniqed.$sum(), modifier).$to_s())));
          return $send($$('Result').$new(), 'tap', [], function $$4(result){var $a, $ret_or_1 = nil;

            
            if (result == null) result = nil;
            result['$critical='](($truthy(($ret_or_1 = is_perfect_miracle)) ? ($ret_or_1) : (is_miracle_synchro)));
            return ($a = [["(" + (command) + ")", "シンフォニー", "[" + (carries.$join(",")) + "],[" + (dice_list.$join(",")) + "]" + ($$('Format').$modifier(modifier)), "[" + (uniqed.$join(",")) + "]" + ($$('Format').$modifier(modifier)), result_label].$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
