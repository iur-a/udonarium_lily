require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $hash2 = Opal.hash2, $truthy = Opal.truthy, $rb_lt = Opal.rb_lt, $eqeq = Opal.eqeq, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_divide = Opal.rb_divide, $rb_ge = Opal.rb_ge, $rb_gt = Opal.rb_gt, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'DemonSpike');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "DemonSpike");
        $const_set($nesting[0], 'NAME', "デモンスパイク");
        $const_set($nesting[0], 'SORT_KEY', "てもんすはいく");
        $const_set($nesting[0], 'HELP_MESSAGE', "・行為判定 xDS+y\n" + "　行為判定を行い、達成値、成否、成功度を出力する。\n" + "　x: ダイス数（省略：2）\n" + "　y: 能力値やスパイク能力による達成値の修正（省略可）\n");
        self.$register_prefix("\\d*DS");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$roll_action(command)
        });
        self.$private();
        
        $def(self, '$roll_action', function $$roll_action(command) {
          var self = this, parser = nil, parsed = nil, $ret_or_1 = nil, step = nil, step_list = nil, is_fumble = nil, total = nil, success_level = nil, is_success = nil, res = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("DS", $hash2(["round_type"], {"round_type": self.round_type})).$enable_prefix_number().$restrict_cmp_op_to(nil);
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          if ($truthy(($ret_or_1 = parsed.$prefix_number()))) {
            $ret_or_1
          } else {
            parsed['$prefix_number='](2)
          };
          if ($truthy($rb_lt(parsed.$prefix_number(), 2))) {
            return nil
          };
          step = self.$roll_step(parsed.$prefix_number());
          step_list = [step];
          while ($eqeq(step['$[]']("dice_sum"), 10)) {
          
            step = self.$roll_step(parsed.$prefix_number());
            step_list.$push(step);
          };
          is_fumble = step_list['$[]'](0)['$[]']("dice_sum")['$=='](2);
          total = ($truthy(is_fumble) ? (0) : ($rb_plus($send(step_list, 'sum', [], function $$1(s){
            
            if (s == null) s = nil;
            return s['$[]']("dice_sum");}), parsed.$modify_number())));
          success_level = $rb_divide(total, 10).$floor();
          is_success = $rb_ge(total, 10);
          res = ($truthy(is_success) ? ("成功, 成功度" + (success_level)) : ($truthy(is_fumble) ? ("自動的失敗") : ("失敗")));
          sequence = ["(" + (parsed) + ")", $send(step_list, 'map', [], function $$2(s){
            
            if (s == null) s = nil;
            return "" + (s['$[]']("dice_sum")) + "[" + (s['$[]']("dice_list").$join(",")) + "]";}), total, res].$flatten();
          return $send($$('Result').$new(), 'tap', [], function $$3(r){var $a;

            
            if (r == null) r = nil;
            r['$condition='](is_success);
            r['$critical=']($rb_gt(step_list.$length(), 1));
            r['$fumble='](is_fumble);
            return ($a = [sequence.$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);});
        });
        return $def(self, '$roll_step', function $$roll_step(times) {
          var self = this, dice_list = nil, dice_sum = nil;

          
          dice_list = self.randomizer.$roll_barabara(times, 6).$sort().$reverse();
          dice_sum = $rb_plus(dice_list['$[]'](0), dice_list['$[]'](1)).$clamp(2, 10);
          return $hash2(["dice_list", "dice_sum"], {"dice_list": dice_list, "dice_sum": dice_sum});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
