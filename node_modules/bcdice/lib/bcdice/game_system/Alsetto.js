require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $send = Opal.send, $rb_le = Opal.rb_le, $rb_plus = Opal.rb_plus, $rb_times = Opal.rb_times, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Alsetto');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Alsetto");
        $const_set($nesting[0], 'NAME', "詩片のアルセット");
        $const_set($nesting[0], 'SORT_KEY', "うたかたのあるせつと");
        $const_set($nesting[0], 'HELP_MESSAGE', "・成功判定：nAL[m]　　　　・トライアンフ無し：nALC[m]\n" + "・命中判定：nAL[m]*p　　　・トライアンフ無し：nALC[m]*p\n" + "・命中判定（ガンスリンガーの根源詩）：nALG[m]*p\n" + "[]内は省略可能。\n" + "\n" + "ALコマンドはトライアンフの分だけ、自動で振り足し処理を行います。\n" + "「n」でダイス数を指定。\n" + "「m」で目標値を指定。省略時は、デフォルトの「3」が使用されます。\n" + "「p」で攻撃力を指定。「*」は「x」でも可。\n" + "攻撃力指定で命中判定となり、成功数ではなく、ダメージを結果表示します。\n" + "\n" + "ALCコマンドはトライアンフ無しで、成功数、ダメージを結果表示します。\n" + "ALGコマンドは「2以下」でトライアンフ処理を行います。\n" + "\n" + "【書式例】\n" + "・5AL → 5d6で目標値3。\n" + "・5ALC → 5d6で目標値3。トライアンフ無し。\n" + "・6AL2 → 6d6で目標値2。\n" + "・4AL*5 → 4d6で目標値3、攻撃力5の命中判定。\n" + "・7AL2x10 → 7d6で目標値2、攻撃力10の命中判定。\n" + "・8ALC4x5 → 8d6で目標値4、攻撃力5、トライアンフ無しの命中判定。\n");
        self.$register_prefix("\\d+AL[CG]?");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, rapid = nil, isCritical = nil, criticalNumber = nil, target = nil, $ret_or_1 = nil, damage = nil;

          
          if ($truthy(command['$=~'](/(\d+)AL(C|G)?(\d+)?((x|\*)(\d+))?$/i))) {
            
            rapid = $$('Regexp').$last_match(1).$to_i();
            isCritical = $$('Regexp').$last_match(2)['$nil?']();
            if ($truthy(isCritical)) {
              criticalNumber = 1
            } else if ($eqeq($$('Regexp').$last_match(2), "G")) {
              
              isCritical = true;
              criticalNumber = 2;
            } else {
              criticalNumber = 0
            };
            target = ($truthy(($ret_or_1 = $$('Regexp').$last_match(3))) ? ($ret_or_1) : (3)).$to_i();
            damage = ($truthy(($ret_or_1 = $$('Regexp').$last_match(6))) ? ($ret_or_1) : (0)).$to_i();
            return self.$checkRoll(rapid, target, damage, isCritical, criticalNumber);
          };
          return nil;
        });
        return $def(self, '$checkRoll', function $$checkRoll(rapid, target, damage, isCritical, criticalNumber) {
          var self = this, totalSuccessCount = nil, totalCriticalCount = nil, text = nil, rollCount = nil, diceArray = nil, diceText = nil, successCount = nil, criticalCount = nil, isDamage = nil, totalDamage = nil, result = nil;

          
          totalSuccessCount = 0;
          totalCriticalCount = 0;
          text = "";
          rollCount = rapid;
          while ($truthy($rb_gt(rollCount, 0))) {
          
            diceArray = self.randomizer.$roll_barabara(rollCount, 6).$sort();
            diceText = diceArray.$join(",");
            successCount = 0;
            criticalCount = 0;
            $send(diceArray, 'each', [], function $$1(i){
              
              if (i == null) i = nil;
              if ($truthy($rb_le(i, target))) {
                successCount = $rb_plus(successCount, 1)
              };
              if ($truthy($rb_le(i, criticalNumber))) {
                return (criticalCount = $rb_plus(criticalCount, 1))
              } else {
                return nil
              };});
            totalSuccessCount = $rb_plus(totalSuccessCount, successCount);
            if (!$eqeq(criticalCount, 0)) {
              totalCriticalCount = $rb_plus(totalCriticalCount, 1)
            };
            if (!$truthy(text['$empty?']())) {
              text = $rb_plus(text, "+")
            };
            text = $rb_plus(text, "" + (successCount) + "[" + (diceText) + "]");
            if (!$truthy(isCritical)) {
              break
            };
            rollCount = criticalCount;
          };
          isDamage = damage['$!='](0);
          if ($truthy(isDamage)) {
            
            totalDamage = $rb_times(totalSuccessCount, damage);
            result = "(" + (rapid) + "D6<=" + (target) + ") ＞ " + (text) + " ＞ Hits：" + (totalSuccessCount) + "*" + (damage) + " ＞ " + (totalDamage) + "ダメージ";
          } else {
            result = "(" + (rapid) + "D6<=" + (target) + ") ＞ " + (text) + " ＞ 成功数：" + (totalSuccessCount)
          };
          if ($truthy(isCritical)) {
            result = $rb_plus(result, " / " + (totalCriticalCount) + "トライアンフ")
          };
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
