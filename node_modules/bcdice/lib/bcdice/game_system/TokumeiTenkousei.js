require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $hash2 = Opal.hash2, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $rb_gt = Opal.rb_gt, $eqeq = Opal.eqeq, $send = Opal.send, $rb_times = Opal.rb_times, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'TokumeiTenkousei');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = $proto.dice_list = nil;
        
        $const_set($nesting[0], 'ID', "TokumeiTenkousei");
        $const_set($nesting[0], 'NAME', "特命転攻生");
        $const_set($nesting[0], 'SORT_KEY', "とくめいてんこうせい");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定 (xD6+y>=n)\n" + "　ゾロ目での自動振り足し\n" + "　1の出目に応じてEPPの獲得量を表示\n" + "　目標値 \"?\" には未対応\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_add_dice = true);
        });
        self.$register_prefix("\\d+D6");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, parser = nil, cmd = nil, times = nil, dice_list = nil, dice_list_flatten = nil, dice_total = nil, count_one = nil, total = nil, result = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/\d+D6/, $hash2(["round_type"], {"round_type": self.$round_type()}));
          cmd = parser.$parse(command);
          if (!$truthy(cmd)) {
            return nil
          };
          times = cmd.$command().$to_i();
          dice_list = self.randomizer.$roll_barabara(times, 6).$sort();
          self.dice_list = [dice_list];
          while ($truthy(self['$same_all_dice?'](dice_list))) {
          
            dice_list = self.randomizer.$roll_barabara(times, 6).$sort();
            self.dice_list.$push(dice_list);
          };
          dice_list_flatten = self.dice_list.$flatten();
          dice_total = dice_list_flatten.$sum();
          count_one = dice_list_flatten.$count(1);
          total = $rb_plus(dice_total, cmd.$modify_number());
          result = ($truthy(cmd.$cmp_op()) ? (($truthy(total.$send(cmd.$cmp_op(), cmd.$target_number())) ? ($$('Result').$success("成功")) : ($$('Result').$failure("失敗")))) : nil);
          sequence = ["(" + (cmd) + ")", self.$interim_expr(cmd, dice_total), total.$to_s(), result.$text(), self.$epp(count_one)].$compact();
          result['$text='](sequence.$join(" ＞ "));
          return result;
        });
        
        $def(self, '$same_all_dice?', function $TokumeiTenkousei_same_all_dice$ques$1(dice_list) {
          var $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = $rb_gt(dice_list.$size(), 1)))) {
            return dice_list.$uniq().$size()['$=='](1)
          } else {
            return $ret_or_1
          }
        });
        
        $def(self, '$interim_expr', function $$interim_expr(cmd, dice_total) {
          var self = this, dice_list = nil, modifier = nil;

          
          if (($eqeq(self.dice_list.$flatten().$size(), 1) && ($eqeq(cmd.$modify_number(), 0)))) {
            return nil
          };
          dice_list = $send(self.dice_list, 'map', [], function $$2(ds){
            
            if (ds == null) ds = nil;
            return "[" + (ds.$join(",")) + "]";}).$join("");
          modifier = $$('Format').$modifier(cmd.$modify_number());
          return [dice_total.$to_s(), dice_list, modifier].$join("");
        });
        return $def(self, '$epp', function $$epp(count_one) {
          
          if ($truthy($rb_gt(count_one, 0))) {
            return "" + ($rb_times(count_one, 5)) + "EPP獲得"
          } else {
            return nil
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
