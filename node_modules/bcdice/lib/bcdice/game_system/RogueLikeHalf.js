require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $rb_gt = Opal.rb_gt, $rb_ge = Opal.rb_ge, $rb_plus = Opal.rb_plus, $thrower = Opal.thrower, $rb_times = Opal.rb_times, $rb_lt = Opal.rb_lt, $rb_minus = Opal.rb_minus, $neqeq = Opal.neqeq, $hash2 = Opal.hash2, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'RogueLikeHalf');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "RogueLikeHalf");
        $const_set($nesting[0], 'NAME', "ローグライクハーフ");
        $const_set($nesting[0], 'SORT_KEY', "ろおくらいくはあふ");
        $const_set($nesting[0], 'HELP_MESSAGE', "■判定　RH+x>=t        x:技量点 t:達成値(威力)\n" + "\n" + "例)RH+1>=5: ダイスを1個振って、技量点1,達成値5の結果を表示(クリティカル・ファンブルも表示)\n" + "\n" + "■D33　D33+x        x:修正値\n" + "\n" + "例)D33: 3面ダイスを2個振って、その結果を表示。\n" + "\n" + "■宝物表　NTT+x     x:修正値\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$resolute_action(command))) ? ($ret_or_2) : (self.$resolute_d33(command)))))) {
            return $ret_or_1
          } else {
            return self.$roll_table_command(command)
          }
        });
        self.$private();
        
        $def(self, '$with_symbol', function $$with_symbol(number) {
          
          if ($eqeq(number, 0)) {
            return "+0"
          } else if ($truthy($rb_gt(number, 0))) {
            return "+" + (number)
          } else {
            return number.$to_s()
          }
        });
        
        $def(self, '$get_result_of_action', function $$get_result_of_action(total, die, target) {
          
          if ($eqeq(die, 6)) {
            return $$('Result').$critical("クリティカル")
          } else if ($eqeq(die, 1)) {
            return $$('Result').$fumble("ファンブル")
          } else if ($truthy($rb_ge(total, target))) {
            return $$('Result').$success("成功")
          } else {
            return $$('Result').$failure("失敗")
          }
        });
        
        $def(self, '$resolute_action', function $$resolute_action(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, modify = nil, target = nil, die = nil, die_text = nil, total = nil, result = nil, command_text = nil, sequence = nil;

          
          m = /RH([+-]\d)*(>=(\d+))?/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = ($truthy(m['$[]'](1)) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (0));
          target = m['$[]'](3).$to_i();
          if ($eqeq(target, 0)) {
            target = 4
          };
          die = self.randomizer.$roll_once(6);
          die_text = die.$to_s();
          total = $rb_plus(die, modify);
          result = self.$get_result_of_action(total, die, target);
          command_text = "(RH" + (self.$with_symbol(modify)) + ">=" + (target) + ")";
          sequence = [command_text, "[" + (die_text) + "]" + (self.$with_symbol(modify)), total, result.$text()].$compact();
          result['$text='](sequence.$join(" ＞ "));
          return result;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$resolute_d33', function $$resolute_d33(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, modify = nil, dice = nil, dice_text = nil, dice_total = nil, p = nil, total = nil, sequence = nil;

          
          m = /D33([+-]\d+)*/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          modify = ($truthy(m['$[]'](1)) ? ($$('Arithmetic').$eval(m['$[]'](1), self.round_type)) : (0));
          dice = self.randomizer.$roll_barabara(2, 3);
          dice_text = dice.$join("");
          dice_total = $rb_plus($rb_plus($rb_times(dice['$[]'](0), 3), dice['$[]'](1)), modify);
          if ($truthy($rb_gt(dice_total, 12))) {
            dice_total = 12
          };
          if ($truthy($rb_lt(dice_total, 4))) {
            dice_total = 4
          };
          p = dice_total.$divmod(3);
          if ($eqeq(p['$[]'](1), 0)) {
            
            p['$[]='](0, $rb_minus(p['$[]'](0), 1));
            p['$[]='](1, 3);
          };
          total = $rb_plus($rb_times(p['$[]'](0), 10), p['$[]'](1));
          sequence = ["(" + (command) + ")", dice_text].$compact();
          if ($neqeq(modify, 0)) {
            sequence = ["(" + (command) + ")", "" + (dice_text) + (self.$with_symbol(modify)), total].$compact()
          };
          return $$('Result').$new(sequence.$join(" ＞ "));} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$roll_table_command', function $$roll_table_command(command) {
          var self = this, result = nil, m = nil, operator = nil, value = nil;

          
          command = command.$upcase();
          result = [];
          m = /([A-Z]+)(([+]|-)(\d+))?/.$match(command);
          if (!$truthy(m)) {
            return result
          };
          command = m['$[]'](1);
          operator = m['$[]'](3);
          value = m['$[]'](4).$to_i();
          return self.$get_another_table_result(command, operator, value);
        });
        
        $def(self, '$get_another_table_result', function $$get_another_table_result(command, operator, value) {
          var self = this, result = nil, table_name = nil, table = nil, index = nil, info = nil;

          
          result = "";
          table_name = command;
          table = $$('TABLES')['$[]'](table_name);
          if ($truthy(table['$nil?']())) {
            return result
          };
          index = self.$get_table_index(operator, value, 1, 6);
          info = table.$choice(index);
          result = "" + (info.$table_name()) + ":" + (info.$value()) + ":" + (info.$body());
          return result;
        });
        
        $def(self, '$get_table_index', function $$get_table_index(operator, value, dice_count, dice_type) {
          var self = this, modify = nil, index = nil;

          
          modify = 0;
          
          switch (operator.valueOf()) {
            case "+":
              modify = value
              break;
            case "-":
              modify = $rb_times(value, -1)
              break;
            default:
              nil
          };
          index = self.randomizer.$roll_sum(dice_count, dice_type);
          index = $rb_plus(index, modify);
          index = [index, $rb_times(dice_count, 1)].$max();
          index = [index, $rb_plus($rb_times(dice_count, dice_type), 1)].$min();
          return index;
        });
        $const_set($nesting[0], 'TABLES', $hash2(["NTT"], {"NTT": $$$($$('DiceTable'), 'Table').$new("宝物表", "1D6", ["金貨１枚", "１ｄ６枚の金貨", "２ｄ６枚の金貨（下限は金貨５枚）", "１個のアクセサリー（１ｄ６×１ｄ６枚の金貨と同等の価値）", "１個の宝石・小（１ｄ６×５枚の金貨と同等の価値。下限は金貨１５枚の価値）", "１個の宝石・大（２ｄ６×５枚の金貨と同等の価値。下限は金貨３０枚の価値）", "【魔法の宝物表】でダイスロールを行うこと。"])}).$freeze());
        return self.$register_prefix("RH", "D33", $$('TABLES').$keys());
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
