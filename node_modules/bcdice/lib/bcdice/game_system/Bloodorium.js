require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $hash2 = Opal.hash2, $truthy = Opal.truthy, $send = Opal.send, $rb_plus = Opal.rb_plus, $rb_times = Opal.rb_times, $rb_gt = Opal.rb_gt, $neqeq = Opal.neqeq, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'Bloodorium');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "Bloodorium");
        $const_set($nesting[0], 'NAME', "ブラドリウム");
        $const_set($nesting[0], 'SORT_KEY', "ふらとりうむ");
        $const_set($nesting[0], 'HELP_MESSAGE', "・ダイスチェック xDC+y\n" + "　【ダイスチェック】を行う。《トライアンフ》を結果に自動反映する。\n" + "　x: ダイス数\n" + "　y: 結果への修正値 （省略可）\n");
        self.$register_prefix("\\d+DC");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$dicecheck(command)
        });
        self.$private();
        
        $def(self, '$dicecheck', function $$dicecheck(command) {
          var self = this, parser = nil, parsed = nil, dice_list = nil, dice_value = nil, values_count = nil, triumph = nil, total = nil, sequence = nil;

          
          parser = $$$($$('Command'), 'Parser').$new("DC", $hash2(["round_type"], {"round_type": self.round_type})).$has_prefix_number().$restrict_cmp_op_to(nil);
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          dice_list = self.randomizer.$roll_barabara(parsed.$prefix_number(), 6).$sort();
          dice_value = dice_list.$max();
          values_count = $send($send(dice_list, 'group_by', [], "itself".$to_proc()), 'transform_values', [], "length".$to_proc());
          triumph = values_count.$values().$max();
          total = $rb_plus($rb_times(dice_value, triumph), parsed.$modify_number());
          sequence = ["(" + (parsed) + ")", "[" + (dice_list.$join(",")) + "]" + ($$('Format').$modifier(parsed.$modify_number())), ($truthy($rb_gt(triumph, 1)) ? ("《トライアンフ》(*" + (triumph) + ")") : nil), ($neqeq(total, dice_value) ? (self.$total_expr(dice_value, triumph, parsed.$modify_number())) : nil), total].$compact();
          return $send($$('Result').$new(), 'tap', [], function $$1(r){var $a;

            
            if (r == null) r = nil;
            r['$critical=']($rb_gt(triumph, 1));
            return ($a = [sequence.$join(" ＞ ")], $send(r, 'text=', $a), $a[$a.length - 1]);});
        });
        return $def(self, '$total_expr', function $$total_expr(dice_value, triumph, modify_number) {
          var formated_triumph = nil;

          
          formated_triumph = ($truthy($rb_gt(triumph, 1)) ? ("*" + (triumph)) : (nil));
          return "" + (dice_value) + (formated_triumph) + ($$('Format').$modifier(modify_number));
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
