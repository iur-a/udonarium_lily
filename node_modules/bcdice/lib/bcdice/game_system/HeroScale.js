require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $truthy = Opal.truthy, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_lt = Opal.rb_lt, $neqeq = Opal.neqeq, $rb_minus = Opal.rb_minus, $rb_times = Opal.rb_times, $eqeq = Opal.eqeq, $rb_le = Opal.rb_le, $rb_divide = Opal.rb_divide, $rb_ge = Opal.rb_ge, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'HeroScale');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "HeroScale");
        $const_set($nesting[0], 'NAME', "英雄の尺度");
        $const_set($nesting[0], 'SORT_KEY', "えいゆうのしやくと");
        $const_set($nesting[0], 'HELP_MESSAGE', "同人TRPGシステム『英雄の尺度』用ダイスボット。\n" + "基本ルールブック＋サプリメント対応。仮称は非対応。\n" + "コマンド一覧は以下の通り。*添え字で内容は[]。†がついていたら添え字必須。\n" + "5hs4 超越\n" + "5hs4,b 肉体の超越\n" + "5hs4,s,* 科学の超越[†達成値への加算値]\n" + "5hs4,p 激情の超越\n" + "4hs6 加護\n" + "4hs6,s 選択の加護\n" + "4hs6,p 安寧の加護\n" + "4hs6,r 逆転の加護\n" + "3hs8,*,* 契約[奉納の出目1,奉納の出目2]\n" + "3hs8,a,*,* 享受の契約[†受諾出目1][†受諾出目2]\n" + "3hs8,e,* 収奪の契約[†取得出目]\n" + "3hs8,b 燃焼の契約\n" + "3hs8,o,*,* 奉納の契約[奉納の出目1,奉納の出目2]\n" + "2hs20 呪い\n" + "2hs20,r 歪曲の呪い\n" + "2hs20,c 崩壊の呪い\n" + "2hs20,d 破滅の呪い\n" + "3hs10 異物\n" + "3hs10,i 模造の異物\n" + "3hs10,m,* 混血の異物[追加振り基準出目（初期値10）]\n" + "3hs10,b,* 彼方の異物[追加振り停止基準値（初期値666）]\n" + "1hs60 報い\n" + "1hs60,d 堕落の報い\n" + "1hs60,o 忘却の報い\n" + "1hs60,s,* 封印の報い[出目への係数]\n" + "12hs2 同化\n" + "12hs2,m,*,*,*,*,*,*,*,* 怪物の同化[*d2,*d4,*d6,*d8,*d10,*d12,*d20,*d60]\n" + "12hs2,t,* [†2の枚数宣言]\n" + "12hs2,c,* 法則の同化[†1の枚数宣言]\n" + "1hs12 下位存在\n" + "2hs12 中位存在\n" + "2hs12,t 変遷の中位存在\n" + "2hs12,c 偶然の中位存在\n" + "2hs12,g,* 萌芽の上位存在[加算値]\n" + "3hs12 上位存在\n" + "3hs12,g 大神の上位存在\n" + "3hs12,h 神性の上位存在\n" + "3hs12,w 魔性の上位存在\n" + "3hs12,m 悪意の上位存在\n" + "3hs12,s,* 大罪の上位存在[†確定する目標値]\n" + "3hs12,d 破壊の上位存在\n" + "3hs12,a 懊悩の上位存在\n" + "3hs12,o 試練の上位存在\n" + "3hs12,c 創造の上位存在\n" + "3hs12,e 元素の上位存在\n" + "*hs* 乗算ロール\n");
        self.$register_prefix("\\d+HS\\d+");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this;

          return self.$select_origin(command)
        });
        self.$private();
        
        $def(self, '$select_origin', function $$select_origin(command) {
          var self = this, order = nil, message = nil, dice = nil, natural_result = nil, total = nil;

          
          order = command.$split(",");
          
          switch (order['$[]'](0).valueOf()) {
            case "5HS4":
              return self.$origin_great(order)
            case "4HS6":
              return self.$origin_protection(order)
            case "3HS8":
              return self.$origin_vow(order)
            case "2HS20":
              return self.$origin_curse(order)
            case "3HS10":
              return self.$origin_stranger(order)
            case "1HS60":
              return self.$origin_karma(order)
            case "12HS2":
              return self.$origin_absorption(order)
            case "1HS12":
              return self.$origin_normal()
            case "2HS12":
              return self.$origin_unique(order)
            case "3HS12":
              return self.$origin_omnipotent(order)
            default:
              
              message = order['$[]'](0);
              dice = order['$[]'](0).$rpartition("HS");
              if ((($truthy($rb_gt(dice.$length(), 2)) && ($truthy(dice['$[]'](0)['$=~'](/^\d+$/)))) && ($truthy(dice['$[]'](2)['$=~'](/^\d+$/))))) {
                
                natural_result = self.randomizer.$roll_barabara(dice['$[]'](0).$to_i(), dice['$[]'](2).$to_i());
                total = self.$results_multiplication(natural_result);
                message = $rb_plus(message, " ＞ " + (total) + "[" + (natural_result.$join(",")) + "]");
                return message;
              } else {
                return nil
              };
          };
        });
        
        $def(self, '$origin_great', function $$origin_great(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(5, 4);
          
          switch (order['$[]'](1).valueOf()) {
            case "P":
              message = self.$fate_passion(natural_result)
              break;
            case "S":
              message = self.$fate_science(natural_result, order)
              break;
            case "B":
              message = self.$fate_body(natural_result)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "超越 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_passion', function $$fate_passion(natural_result) {
          var self = this, modified_result = nil, number_of_1 = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          number_of_1 = natural_result.$count(1);
          $send(natural_result, 'each', [], function $$1(result){
            
            if (result == null) result = nil;
            return modified_result['$<<']($rb_plus(result, number_of_1));});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "激情の超越 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_science', function $$fate_science(natural_result, order) {
          var self = this, subtotal = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            if ($truthy($rb_lt(order['$[]'](2).$to_i(), 1024))) {
              
              total = $rb_plus(subtotal, order['$[]'](2).$to_i());
              message = "科学の超越 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
              if ($truthy($rb_gt(total, 1023))) {
                message = $rb_plus(message, "(科学臨界)")
              };
            } else {
              message = "エラー：科学力が1024を超えています。"
            }
          } else {
            message = "エラー：科学力を設定してください。"
          };
          return message;
        });
        
        $def(self, '$fate_body', function $$fate_body(natural_result) {
          var self = this, modified_result = nil, additional = nil, value = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          additional = natural_result.$count(4);
          while ($truthy($rb_gt(additional, 0))) {
          
            value = self.randomizer.$roll_once(4);
            modified_result.$push(value);
            if ($neqeq(value, 4)) {
              additional = $rb_minus(additional, 1)
            };
          };
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "肉体の超越 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$origin_protection', function $$origin_protection(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(4, 6);
          
          switch (order['$[]'](1).valueOf()) {
            case "R":
              message = self.$fate_reversal(natural_result)
              break;
            case "P":
              message = self.$fate_peace(natural_result)
              break;
            case "S":
              message = self.$fate_choice(natural_result)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "加護 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_reversal', function $$fate_reversal(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          $send(natural_result, 'each', [], function $$2(result){
            
            if (result == null) result = nil;
            if ($truthy($rb_lt(result, 4))) {
              result = $rb_minus(7, result)
            };
            return modified_result['$<<'](result);});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "逆転の加護 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_peace', function $$fate_peace(natural_result) {
          var self = this, subtotal = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_plus(subtotal, 250);
          message = "安寧の加護 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          return message;
        });
        
        $def(self, '$fate_choice', function $$fate_choice(natural_result) {
          var self = this, modified_result = nil, even_total = nil, odd_total = nil, total = nil, subtotal = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result.$concat(self.randomizer.$roll_barabara(3, 6));
          even_total = 1;
          odd_total = 1;
          $send(modified_result, 'each', [], function $$3(result){
            
            if (result == null) result = nil;
            if ($truthy(result['$even?']())) {
              return (even_total = $rb_times(even_total, result))
            } else {
              return (odd_total = $rb_times(odd_total, result))
            };});
          if ($truthy($rb_gt(even_total, odd_total))) {
            total = even_total
          } else {
            total = odd_total
          };
          subtotal = self.$results_multiplication(natural_result);
          message = "選択の加護 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$origin_vow', function $$origin_vow(order) {
          var self = this, natural_result = nil, message = nil, subtotal = nil, modified_result = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(3, 8);
          
          switch (order['$[]'](1).valueOf()) {
            case "O":
              message = self.$fate_offering(natural_result, order)
              break;
            case "B":
              message = self.$fate_burning(natural_result)
              break;
            case "E":
              message = self.$fate_exploitation(natural_result, order)
              break;
            case "A":
              message = self.$fate_acceptance(natural_result, order)
              break;
            default:
              
              subtotal = self.$results_multiplication(natural_result);
              if ((($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](1)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
                
                modified_result = natural_result.$dup();
                modified_result['$<<'](order['$[]'](1).$to_i());
                modified_result['$<<'](order['$[]'](2).$to_i());
                total = self.$results_multiplication(modified_result);
                message = "契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
              } else {
                message = "契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "]"
              };
          };
          return message;
        });
        
        $def(self, '$fate_offering', function $$fate_offering(natural_result, order) {
          var self = this, subtotal = nil, modified_result = nil, total = nil, message = nil, offering_result = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          if ((($truthy($rb_gt(order.$length(), 3)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](3)['$=~'](/^\d+$/))))) {
            
            modified_result = natural_result.$dup();
            modified_result['$<<'](order['$[]'](2).$to_i());
            modified_result['$<<'](order['$[]'](3).$to_i());
            total = self.$results_multiplication(modified_result);
            message = "奉納の契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          } else {
            message = "奉納の契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "]"
          };
          offering_result = natural_result.$dup();
          offering_result['$sort!']()['$reverse!']().$shift(1);
          message = $rb_plus(message, "(奉納：" + (offering_result.$join(",")) + ")");
          return message;
        });
        
        $def(self, '$fate_burning', function $$fate_burning(natural_result) {
          var self = this, subtotal = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_times(subtotal, 6);
          message = "燃焼の契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          return message;
        });
        
        $def(self, '$fate_exploitation', function $$fate_exploitation(natural_result, order) {
          var self = this, subtotal = nil, modified_result = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            
            modified_result = natural_result.$dup();
            modified_result['$[]='](modified_result.$index(modified_result.$min()), order['$[]'](2).$to_i());
            total = self.$results_multiplication(modified_result);
            message = "収奪の契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          } else {
            message = "エラー：収奪数を指定してください。"
          };
          return message;
        });
        
        $def(self, '$fate_acceptance', function $$fate_acceptance(natural_result, order) {
          var self = this, subtotal = nil, change_result = nil, modified_result = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          if ((($truthy($rb_gt(order.$length(), 3)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](3)['$=~'](/^\d+$/))))) {
            
            change_result = natural_result.$sort().$take(2);
            modified_result = natural_result.$dup();
            modified_result['$[]='](modified_result.$index(change_result['$[]'](0)), order['$[]'](2).$to_i());
            modified_result['$[]='](modified_result.$index(change_result['$[]'](1)), order['$[]'](3).$to_i());
            total = self.$results_multiplication(modified_result);
            message = "享受の契約 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          } else {
            message = "エラー：享受数を指定してください。"
          };
          return message;
        });
        
        $def(self, '$origin_curse', function $$origin_curse(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(2, 20);
          
          switch (order['$[]'](1).valueOf()) {
            case "R":
              message = self.$fate_ruin(natural_result)
              break;
            case "C":
              message = self.$fate_collapse(natural_result)
              break;
            case "D":
              message = self.$fate_distortion(natural_result)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "呪い ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_ruin', function $$fate_ruin(natural_result) {
          var self = this, modified_result = nil, total = nil, subtotal = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result.$concat(self.randomizer.$roll_barabara(2, 20));
          total = 1;
          $send(modified_result, 'each', [], function $$4(result){
            
            if (result == null) result = nil;
            if ($truthy($rb_gt(result, 10))) {
              return (total = $rb_times(total, result))
            } else {
              return nil
            };});
          subtotal = self.$results_multiplication(natural_result);
          message = "破滅の呪い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_collapse', function $$fate_collapse(natural_result) {
          var self = this, modified_result = nil, collapse_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          collapse_result = self.$result_raoundup(natural_result['$[]'](natural_result.$index(natural_result.$max())));
          if ($eqeq(modified_result['$[]'](0), modified_result['$[]'](1))) {
            
            modified_result['$[]='](0, collapse_result);
            modified_result['$[]='](1, collapse_result);
            modified_result['$<<'](collapse_result);
            modified_result['$<<'](collapse_result);
          } else {
            
            modified_result['$[]='](natural_result.$index(natural_result.$max()), collapse_result);
            modified_result.$insert(natural_result.$index(natural_result.$max()), collapse_result);
          };
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "崩壊の呪い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_distortion', function $$fate_distortion(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          if ($eqeq(modified_result['$[]'](0), modified_result['$[]'](1))) {
            
            modified_result['$[]='](0, $rb_plus(modified_result['$[]'](0), 13));
            modified_result['$[]='](1, $rb_plus(modified_result['$[]'](1), 13));
          } else {
            modified_result['$[]='](natural_result.$index(natural_result.$min()), $rb_plus(modified_result['$[]'](natural_result.$index(natural_result.$min())), 13))
          };
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "歪曲の呪い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$origin_stranger', function $$origin_stranger(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(3, 10);
          natural_result = self.$stranger_effection(natural_result);
          
          switch (order['$[]'](1).valueOf()) {
            case "I":
              message = self.$fate_imitation(natural_result)
              break;
            case "M":
              message = self.$fate_mixed(natural_result, order)
              break;
            case "B":
              message = self.$fate_beyond(natural_result, order)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "異物 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_imitation', function $$fate_imitation(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result['$sort!']();
          modified_result['$[]='](0, ($eqeq($rb_plus(modified_result['$[]'](0), $rb_times(modified_result['$[]'](1), 10)), 0) ? (100) : ($rb_plus(modified_result['$[]'](0), $rb_times(modified_result['$[]'](1), 10)))));
          modified_result['$[]='](1, modified_result['$[]'](2));
          modified_result.$delete_at(2);
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "模造の異物 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_mixed', function $$fate_mixed(natural_result, order) {
          var self = this, subtotal = nil, mixed_score = nil, modified_result = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            mixed_score = order['$[]'](2).$to_i()
          } else {
            mixed_score = 1
          };
          modified_result = natural_result.$dup();
          if ($truthy($rb_le(mixed_score, natural_result.$min()))) {
            
            modified_result['$<<'](self.randomizer.$roll_once(12));
            total = self.$results_multiplication(modified_result);
            message = "混血の異物 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "](追加振り)";
          } else {
            
            modified_result['$[]='](natural_result.$index(natural_result.$min()), 10);
            total = self.$results_multiplication(modified_result);
            message = "混血の異物 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "](10置換)";
          };
          return message;
        });
        
        $def(self, '$fate_beyond', function $$fate_beyond(natural_result, order) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, beyond_limit = nil, $ret_or_1 = nil, message = nil;

          
          modified_result = natural_result.$dup();
          subtotal = self.$results_multiplication(natural_result);
          total = subtotal;
          beyond_limit = 666;
          if ((($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/)))) && ($truthy($rb_lt(order['$[]'](2).$to_i(), 666))))) {
            beyond_limit = order['$[]'](2).$to_i()
          };
          while ($truthy(($truthy(($ret_or_1 = total['$!='](0))) ? ($rb_le(total, beyond_limit)) : ($ret_or_1)))) {
          
            modified_result['$<<'](self.randomizer.$roll_d9());
            total = self.$results_multiplication(modified_result);
          };
          message = "彼方の異物 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$origin_karma', function $$origin_karma(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(1, 60);
          
          switch (order['$[]'](1).valueOf()) {
            case "D":
              message = self.$fate_depravity(natural_result)
              break;
            case "O":
              message = self.$fate_oblivion(natural_result)
              break;
            case "S":
              message = self.$fate_sealed(natural_result, order)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "報い ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_depravity', function $$fate_depravity(natural_result) {
          var self = this, subtotal = nil, modified_result = nil, depravity_num1 = nil, depravity_num10 = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          modified_result = natural_result.$dup();
          depravity_num1 = natural_result['$[]'](0)['$%'](10);
          depravity_num10 = $rb_divide(natural_result['$[]'](0), 10).$to_i();
          if ($truthy($rb_gt(depravity_num10, 1))) {
            modified_result['$<<'](depravity_num10)
          };
          if ($truthy($rb_gt(depravity_num1, 1))) {
            modified_result['$<<'](depravity_num1)
          };
          total = self.$results_multiplication(modified_result);
          message = "堕落の報い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_oblivion', function $$fate_oblivion(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result['$<<'](self.randomizer.$roll_once(60));
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_divide(self.$results_multiplication(modified_result), 2).$to_i();
          message = "忘却の報い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_sealed', function $$fate_sealed(natural_result, order) {
          var self = this, modified_result = nil, subtotal = nil, sealed_break = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          subtotal = self.$results_multiplication(natural_result);
          sealed_break = 1;
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            sealed_break = order['$[]'](2).$to_i()
          };
          modified_result['$[]='](0, $rb_times(modified_result['$[]'](0), sealed_break));
          total = $rb_times(subtotal, sealed_break);
          message = "封印の報い ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          if ($truthy($rb_le(total, 30))) {
            
            sealed_break = $rb_times(sealed_break, 4);
            message = $rb_plus(message, "(封印解除成功：" + (sealed_break) + ")");
          } else if ($truthy($rb_le(total, 60))) {
            
            sealed_break = $rb_times(sealed_break, 2);
            message = $rb_plus(message, "(封印解除成功：" + (sealed_break) + ")");
          } else {
            message = $rb_plus(message, "(封印解除失敗：" + (sealed_break) + ")")
          };
          return message;
        });
        
        $def(self, '$origin_absorption', function $$origin_absorption(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(12, 2);
          
          switch (order['$[]'](1).valueOf()) {
            case "M":
              message = self.$fate_monster(natural_result, order)
              break;
            case "T":
              message = self.$fate_treasure(natural_result, order)
              break;
            case "C":
              message = self.$fate_concept(natural_result, order)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "同化 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_monster', function $$fate_monster(_natural_result, order) {
          var self = this, modified_result = nil, count_of_10 = nil, total = nil, subtotal = nil, message = nil;

          
          modified_result = [];
          if ((((((((($truthy($rb_gt(order.$length(), 9)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](3)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](4)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](5)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](6)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](7)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](8)['$=~'](/^\d+$/)))) && ($truthy(order['$[]'](9)['$=~'](/^\d+$/))))) {
            
            if ($truthy($rb_gt(order['$[]'](2).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](2).$to_i(), 2))
            };
            if ($truthy($rb_gt(order['$[]'](3).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](3).$to_i(), 4))
            };
            if ($truthy($rb_gt(order['$[]'](4).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](4).$to_i(), 6))
            };
            if ($truthy($rb_gt(order['$[]'](5).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](5).$to_i(), 8))
            };
            count_of_10 = order['$[]'](6).$to_i();
            while ($truthy($rb_gt(count_of_10, 0))) {
            
              modified_result['$<<'](self.randomizer.$roll_d9());
              count_of_10 = $rb_minus(count_of_10, 1);
            };
            if ($truthy($rb_gt(order['$[]'](7).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](7).$to_i(), 12))
            };
            if ($truthy($rb_gt(order['$[]'](8).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](8).$to_i(), 20))
            };
            if ($truthy($rb_gt(order['$[]'](9).$to_i(), 0))) {
              modified_result.$concat(self.randomizer.$roll_barabara(order['$[]'](9).$to_i(), 60))
            };
            total = self.$results_multiplication(modified_result);
            subtotal = modified_result.$sum();
            message = "怪物の同化 ＞ " + (total) + "[" + (modified_result.$join(",")) + "] 浸蝕値：" + (subtotal);
            if ($truthy([2, 4, 6, 8, 10, 12, 20, 60]['$include?'](subtotal))) {
              message = $rb_plus(message, "(変異進行)")
            };
            if ($truthy(modified_result['$include?'](1))) {
              message = $rb_plus(message, "(人間性喪失)")
            };
          } else {
            message = "エラー：変異状態を指定してください。"
          };
          return message;
        });
        
        $def(self, '$fate_treasure', function $$fate_treasure(natural_result, order) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, treasure_point = nil, message = nil;

          
          modified_result = natural_result.$dup();
          subtotal = self.$results_multiplication(natural_result);
          total = subtotal;
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            
            treasure_point = order['$[]'](2).$to_i();
            if ($truthy($rb_ge(modified_result.$count(2), treasure_point))) {
              
              total = $rb_times(total, treasure_point);
              message = "秘宝の同化 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "(同調成功)";
            } else {
              message = "秘宝の同化 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "(同調失敗)"
            };
          } else {
            message = "エラー：解放率を指定してください。"
          };
          return message;
        });
        
        $def(self, '$fate_concept', function $$fate_concept(natural_result, order) {
          var self = this, modified_result = nil, subtotal = nil, existence_scale = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          subtotal = self.$results_multiplication(natural_result);
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            
            existence_scale = order['$[]'](2).$to_i();
            if ($truthy($rb_gt(existence_scale, 12))) {
              existence_scale = 12
            };
            modified_result.$fill(2, 0, existence_scale);
            total = self.$results_multiplication(modified_result);
            message = "概念の同化 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          } else {
            message = "エラー：事象強度を指定してください。"
          };
          return message;
        });
        
        $def(self, '$origin_normal', function $$origin_normal() {
          var self = this, natural_result = nil, total = nil, message = nil;

          
          natural_result = self.randomizer.$roll_barabara(1, 12);
          total = self.$results_multiplication(natural_result);
          message = "下位存在 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$origin_unique', function $$origin_unique(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(2, 12);
          
          switch (order['$[]'](1).valueOf()) {
            case "G":
              message = self.$fate_growth(natural_result, order)
              break;
            case "T":
              message = self.$fate_transition(natural_result)
              break;
            case "C":
              message = self.$fate_chance(natural_result)
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "中位存在 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_growth', function $$fate_growth(natural_result, order) {
          var self = this, subtotal = nil, total = nil, message = nil, growth_level = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = subtotal;
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            
            total = $rb_plus(total, order['$[]'](2).$to_i());
            message = "萌芽の中位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          } else {
            message = "萌芽の中位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "]"
          };
          growth_level = $rb_plus(order['$[]'](2).$to_i(), 50);
          message = $rb_plus(message, "(成長段階：" + (growth_level) + ")");
          return message;
        });
        
        $def(self, '$fate_transition', function $$fate_transition(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result['$<<'](self.randomizer.$roll_d9());
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "変遷の中位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_chance', function $$fate_chance(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          if ($eqeq(modified_result['$[]'](0), modified_result['$[]'](1))) {
            
            total = $rb_times(total, 24);
            message = "偶然の中位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          } else {
            message = "偶然の中位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "]"
          };
          return message;
        });
        
        $def(self, '$origin_omnipotent', function $$origin_omnipotent(order) {
          var self = this, natural_result = nil, message = nil, total = nil;

          
          natural_result = self.randomizer.$roll_barabara(3, 12);
          
          switch (order['$[]'](1).valueOf()) {
            case "G":
              message = self.$fate_god(natural_result)
              break;
            case "H":
              message = self.$fate_holy(natural_result)
              break;
            case "W":
              message = self.$fate_wicked(natural_result)
              break;
            case "M":
              message = self.$fate_malice(natural_result)
              break;
            case "S":
              message = self.$fate_sin(natural_result, order)
              break;
            case "D":
              message = self.$fate_destruction(natural_result)
              break;
            case "A":
              message = self.$fate_anguish(natural_result)
              break;
            case "O":
              message = self.$fate_ordeal(natural_result)
              break;
            case "C":
              message = self.$fate_creation(natural_result)
              break;
            case "E":
              message = self.$fate_element()
              break;
            default:
              
              total = self.$results_multiplication(natural_result);
              message = "上位存在 ＞ " + (total) + "[" + (natural_result.$join(",")) + "]";
          };
          return message;
        });
        
        $def(self, '$fate_god', function $$fate_god(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          $send(natural_result, 'each', [], function $$5(result){
            
            if (result == null) result = nil;
            return modified_result['$<<']($rb_plus(result, 3));});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "大神の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_holy', function $$fate_holy(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          $send(natural_result, 'each', [], function $$6(result){
            
            if (result == null) result = nil;
            return modified_result['$<<']($rb_plus(result, 1));});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "神性の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_wicked', function $$fate_wicked(natural_result) {
          var self = this, subtotal = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_plus(subtotal, 120);
          message = "魔性の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          return message;
        });
        
        $def(self, '$fate_malice', function $$fate_malice(natural_result) {
          var self = this, subtotal = nil, total = nil, message = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_times(subtotal, 2);
          message = "悪意の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total);
          return message;
        });
        
        $def(self, '$fate_sin', function $$fate_sin(natural_result, order) {
          var self = this, subtotal = nil, total = nil, message = nil, sin_weight = nil, sin_count = nil, $ret_or_1 = nil, modified_result = nil;

          
          subtotal = self.$results_multiplication(natural_result);
          total = subtotal;
          if (($truthy($rb_gt(order.$length(), 2)) && ($truthy(order['$[]'](2)['$=~'](/^\d+$/))))) {
            
            message = "大罪の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "]";
            sin_weight = order['$[]'](2).$to_i();
            sin_count = 0;
            while ($truthy(($truthy(($ret_or_1 = $rb_lt(sin_count, 3))) ? ($rb_lt(total, sin_weight)) : ($ret_or_1)))) {
            
              modified_result = self.randomizer.$roll_barabara(3, 12);
              total = self.$results_multiplication(modified_result);
              message = $rb_plus(message, " ＞ " + (total) + "[" + (modified_result.$join(",")) + "]");
              sin_count = $rb_plus(sin_count, 1);
            };
          } else {
            message = "エラー：罪の重さを指定してください。"
          };
          return message;
        });
        
        $def(self, '$fate_destruction', function $$fate_destruction(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          modified_result['$<<'](self.randomizer.$roll_once(12));
          subtotal = self.$results_multiplication(natural_result);
          total = $rb_minus(self.$results_multiplication(modified_result), 300);
          message = "破壊の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_anguish', function $$fate_anguish(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          $send(natural_result, 'each', [], function $$7(result){
            
            if (result == null) result = nil;
            if ($truthy($rb_lt(result, 7))) {
              result = 7
            };
            return modified_result['$<<'](result);});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "懊悩の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_ordeal', function $$fate_ordeal(natural_result) {
          var self = this, modified_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = [];
          $send(natural_result, 'each', [], function $$8(result){
            
            if (result == null) result = nil;
            if ($truthy($rb_lt(result, 9))) {
              result = 9
            };
            return modified_result['$<<'](result);});
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "試練の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_creation', function $$fate_creation(natural_result) {
          var self = this, modified_result = nil, temporary_result = nil, subtotal = nil, total = nil, message = nil;

          
          modified_result = natural_result.$dup();
          temporary_result = natural_result.$dup();
          temporary_result['$sort!']();
          modified_result['$<<'](temporary_result['$[]'](1));
          subtotal = self.$results_multiplication(natural_result);
          total = self.$results_multiplication(modified_result);
          message = "創造の上位存在 ＞ " + (subtotal) + "[" + (natural_result.$join(",")) + "] ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$fate_element', function $$fate_element() {
          var self = this, modified_result = nil, total = nil, message = nil;

          
          modified_result = [];
          modified_result['$<<'](self.randomizer.$roll_once(4));
          modified_result['$<<'](self.randomizer.$roll_once(6));
          modified_result['$<<'](self.randomizer.$roll_once(8));
          modified_result['$<<'](self.randomizer.$roll_once(12));
          modified_result['$<<'](self.randomizer.$roll_once(20));
          total = self.$results_multiplication(modified_result);
          message = "元素の上位存在 ＞ " + (total) + "[" + (modified_result.$join(",")) + "]";
          return message;
        });
        
        $def(self, '$results_multiplication', function $$results_multiplication(result_list) {
          var total = nil;

          
          total = 1;
          $send(result_list, 'each', [], function $$9(result){
            
            if (result == null) result = nil;
            return (total = $rb_times(total, result));});
          return total;
        });
        
        $def(self, '$stranger_effection', function $$stranger_effection(result_list) {
          var stranger_list = nil;

          
          stranger_list = [];
          $send(result_list, 'each', [], function $$10(result){
            
            if (result == null) result = nil;
            return stranger_list['$<<']($rb_minus(result, 1));});
          return stranger_list;
        });
        return $def(self, '$result_raoundup', function $$result_raoundup(result) {
          
          if ($truthy(result['$even?']())) {
            return $rb_divide(result, 2).$to_i()
          } else {
            return $rb_plus($rb_divide(result, 2).$to_i(), 1)
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
