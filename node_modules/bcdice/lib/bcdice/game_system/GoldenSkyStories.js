require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $eqeqeq = Opal.eqeqeq, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'GoldenSkyStories');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "GoldenSkyStories");
        $const_set($nesting[0], 'NAME', "ゆうやけこやけ");
        $const_set($nesting[0], 'SORT_KEY', "ゆうやけこやけ");
        $const_set($nesting[0], 'HELP_MESSAGE', "※「ゆうやけこやけ」はダイスロールを使用しないシステムです。\n" + "※このダイスボットは部屋のシステム名表示用となります。\n" + "\n" + "・下駄占い (GETA)\n" + "  あーしたてんきになーれ\n");
        self.$register_prefix("geta");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.enabled_upcase_input = false);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, result = nil, $ret_or_1 = nil;

          
          self.$debug("eval_game_system_specific_command command", command);
          result = "";
          if ($eqeqeq(/geta/i, ($ret_or_1 = command))) {
            result = self.$getaRoll()
          } else {
            nil
          };
          if ($truthy(result['$empty?']())) {
            return nil
          };
          return "" + (command) + " ＞ " + (result);
        });
        return $def(self, '$getaRoll', function $$getaRoll() {
          var self = this, result = nil, dice = nil, getaString = nil;

          
          result = "";
          dice = self.randomizer.$roll_once(7);
          result = $rb_plus(result, "下駄占い ＞ ");
          getaString = "";
          
          switch (dice.valueOf()) {
            case 1:
              getaString = "裏：あめ"
              break;
            case 2:
              getaString = "表：はれ"
              break;
            case 3:
              getaString = "裏：あめ"
              break;
            case 4:
              getaString = "表：はれ"
              break;
            case 5:
              getaString = "裏：あめ"
              break;
            case 6:
              getaString = "表：はれ"
              break;
            case 7:
              getaString = "横：くもり"
              break;
            default:
              nil
          };
          result = $rb_plus(result, getaString);
          return result;
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
