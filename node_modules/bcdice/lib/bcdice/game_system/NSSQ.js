require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $rb_plus = Opal.rb_plus, $eqeq = Opal.eqeq, $rb_ge = Opal.rb_ge, $rb_lt = Opal.rb_lt, $thrower = Opal.thrower, $rb_minus = Opal.rb_minus, $rb_gt = Opal.rb_gt, $send = Opal.send, $eqeqeq = Opal.eqeqeq, $rb_le = Opal.rb_le, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'NSSQ');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "NSSQ");
        $const_set($nesting[0], 'NAME', "SRSじゃない世界樹の迷宮TRPG");
        $const_set($nesting[0], 'SORT_KEY', "えすああるえすしやないせかいしゆのめいきゆうTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 判定 (xSQ±y>=z)\n" + "  xD6の判定。3つ以上振ったとき、出目の高い2つを表示します。絶対成功、絶対失敗も計算します。\n" + "  2つのサイコロを使用して出目に1があった場合は、FPの獲得も表示します。3つ以上使用した場合は表示しません。\n" + "  ±y: yに修正値を入力。±の計算に対応。省略可能。\n" + "  z: 目標値。省略可能。\n" + "\n" + "■ ダメージロール (xDR(C)(+)y)\n" + "  xD6のダメージロール。クリティカルヒットの自動判定を行います。Cを付けるとクリティカルアップ状態で計算できます。+を付けるとクリティカルヒット時のダイスが8個になります。\n" + "  x: xに振るダイス数を入力。\n" + "  y: yに耐性を入力。\n" + "  例) 5DR3 5DRC4 5DRC+4\n" + "\n" + "■ 回復ロール (xHRy)\n" + "  xD6の回復ロール。クリティカルヒットが発生しません。\n" + "  x: xに振るダイス数を入力。\n" + "  y: yに耐性を入力。省略した場合3。\n" + "  例) 2HR 10HR2\n" + "\n" + "■ 採集ロール (TC±z,SC±z,GC±z)\n" + "  少しだけ(T)、そこそこ(S)、ガッツリ(G)採取採掘伐採を行います。\n" + "  z: zに追加でロールする回数を入力。省略可能。\n" + "  例) TC SC+1 GC-1\n");
        self.$register_prefix("\\d+SQ[\\+\\-\\d]*", "\\d+DR(C)?(\\+)?\\d+", "[TSG]C", "\\d+HR\\d*");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil, $ret_or_3 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = ($truthy(($ret_or_3 = self.$roll_sq(command))) ? ($ret_or_3) : (self.$damage_roll(command))))) ? ($ret_or_2) : (self.$heal_roll(command)))))) {
            return $ret_or_1
          } else {
            return self.$collecting_roll(command)
          }
        });
        self.$private();
        
        $def(self, '$roll_sq', function $$roll_sq(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var $a, self = this, m = nil, dice_count = nil, modifier = nil, target = nil, dice_list = nil, largest_two = nil, total = nil, num_1 = nil, result = nil, fp_result = nil, sequence = nil;

          
          m = /(\d+)SQ([+\-\d]+)?(([>=]+)(\d+))?/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = m['$[]'](1).$to_i();
          modifier = $$('ArithmeticEvaluator').$eval(m['$[]'](2));
          target = ($a = m['$[]'](5), ($a === nil || $a == null) ? nil : $a.$to_i());
          dice_list = self.randomizer.$roll_barabara(dice_count, 6);
          largest_two = dice_list.$sort().$reverse().$take(2);
          total = $rb_plus(largest_two.$sum(), modifier);
          num_1 = dice_list.$count(1);
          result = ($eqeq(largest_two, [6, 6]) ? ($$('Result').$critical(" ＞ 絶対成功！")) : ($eqeq(largest_two, [1, 1]) ? ($$('Result').$fumble(" ＞ 絶対失敗！")) : (($truthy(target) && ($truthy($rb_ge(total, target)))) ? ($$('Result').$success(" ＞ 成功")) : (($truthy(target) && ($truthy($rb_lt(total, target)))) ? ($$('Result').$failure(" ＞ 失敗")) : ($$('Result').$new())))));
          fp_result = (($eqeq(dice_count, 2) && ($truthy($rb_ge(num_1, 1)))) ? (" (【FP】" + (num_1) + "獲得)") : (""));
          sequence = ["(" + (command) + ")", "[" + (dice_list.$join(",")) + "]" + ($$('Format').$modifier(modifier)), "" + (total) + "[" + (largest_two.$join(",")) + "]" + (result.$text()) + (fp_result)];
          result['$text='](sequence.$join(" ＞ "));
          return result;} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$damage_roll', function $$damage_roll(command) {
          var self = this, m = nil, dice_count = nil, critical_up = nil, increase_critical_dice = nil, resist = nil, dice_list = nil, normal_damage = nil, result = nil, critical_target = nil;

          
          m = /(\d+)DR(C)?(\+)?(\d+)/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = m['$[]'](1).$to_i();
          critical_up = m['$[]'](2)['$nil?']()['$!']();
          increase_critical_dice = m['$[]'](3)['$nil?']()['$!']();
          resist = m['$[]'](4).$to_i();
          dice_list = self.randomizer.$roll_barabara(dice_count, 6);
          normal_damage = self.$damage(dice_list, resist);
          result = "(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "]" + (resist);
          critical_target = ($truthy(critical_up) ? (1) : (2));
          if ($truthy($rb_ge($rb_minus(dice_list.$count(6), dice_list.$count(1)), critical_target))) {
            
            result = $rb_plus(result, self.$critical_damage_roll(increase_critical_dice, resist, normal_damage));
            result = $$('Result').$critical(result);
          } else {
            
            result = $rb_plus(result, " ＞ " + (normal_damage) + "ダメージ");
            result = ($truthy($rb_gt(normal_damage, 0)) ? ($$('Result').$success(result)) : ($$('Result').$failure(result)));
          };
          return result;
        });
        
        $def(self, '$critical_damage_roll', function $$critical_damage_roll(increase_critical_dice, resist, normal_damage) {
          var self = this, dice_count = nil, dice_list = nil, critical_damage = nil;

          
          dice_count = ($truthy(increase_critical_dice) ? (8) : (4));
          dice_list = self.randomizer.$roll_barabara(dice_count, 6);
          critical_damage = self.$damage(dice_list, resist);
          return " ＞ クリティカルヒット！ ＞ (" + (dice_count) + "DR" + (resist) + ") ＞ [" + (dice_list.$join(",")) + "]" + (resist) + " ＞ " + ($rb_plus(normal_damage, critical_damage)) + "ダメージ";
        });
        
        $def(self, '$heal_roll', function $$heal_roll(command) {
          var $a, self = this, m = nil, dice_count = nil, resist = nil, $ret_or_1 = nil, dice_list = nil, heal_amount = nil, result_text = nil;

          
          m = /^(\d+)HR(\d+)?$/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dice_count = m['$[]'](1).$to_i();
          resist = ($truthy(($ret_or_1 = ($a = m['$[]'](2), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (3));
          dice_list = self.randomizer.$roll_barabara(dice_count, 6);
          heal_amount = self.$damage(dice_list, resist);
          result_text = "(" + (command) + ") ＞ [" + (dice_list.$join(",")) + "]" + (resist) + " ＞ " + (heal_amount) + "回復";
          return ($truthy($rb_gt(heal_amount, 0)) ? ($$('Result').$success(result_text)) : ($$('Result').$failure(result_text)));
        });
        
        $def(self, '$damage', function $$damage(dice_list, resist) {
          
          return $send(dice_list, 'count', [], function $$1(x){
            
            if (x == null) x = nil;
            return $rb_gt(x, resist);})
        });
        
        $def(self, '$collecting_roll', function $$collecting_roll(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, type = nil, modifier = nil, aatto_param = nil, $ret_or_1 = nil, roll_times = nil, results = nil;

          
          m = /([TSG])C([+\-\d]+)?/i.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          type = m['$[]'](1);
          modifier = $$('ArithmeticEvaluator').$eval(m['$[]'](2));
          aatto_param = ($eqeqeq("T", ($ret_or_1 = type)) ? (3) : ($eqeqeq("S", $ret_or_1) ? (4) : ($eqeqeq("G", $ret_or_1) ? (5) : (nil))));
          roll_times = $rb_plus($rb_minus(aatto_param, 2), modifier);
          if ($truthy($rb_le(roll_times, 0))) {
            return nil
          };
          results = $send($$('Array'), 'new', [roll_times], function $$2(i){var self = $$2.$$s == null ? this : $$2.$$s, dice_list = nil, dice = nil;
            if (self.randomizer == null) self.randomizer = nil;

            
            if (i == null) i = nil;
            dice_list = self.randomizer.$roll_barabara(2, 6);
            dice = dice_list.$sum();
            return "(" + (command) + ") ＞ " + (dice) + "[" + (dice_list.$join(",")) + "]: " + (self.$result_collecting(i, dice, aatto_param));}, {$$s: self});
          return results.$join("\n");} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        return $def(self, '$result_collecting', function $$result_collecting(i, dice, aatto) {
          
          if (($truthy($rb_le(dice, aatto)) && ($truthy($rb_gt($rb_minus(aatto, 2), i))))) {
            return "！ああっと！"
          } else if ($truthy($rb_le($rb_minus(aatto, 2), i))) {
            return "成功（追加分）"
          } else {
            return "成功"
          }
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
