require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $hash2 = Opal.hash2, $truthy = Opal.truthy, $to_ary = Opal.to_ary, $send = Opal.send, $rb_plus = Opal.rb_plus, $rb_le = Opal.rb_le, $def = Opal.def, $rb_minus = Opal.rb_minus, $rb_gt = Opal.rb_gt, $rb_ge = Opal.rb_ge, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'TalesFromTheLoop');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "TalesFromTheLoop");
        $const_set($nesting[0], 'NAME', "ザ・ループTRPG");
        $const_set($nesting[0], 'SORT_KEY', "さるうふTRPG");
        $const_set($nesting[0], 'HELP_MESSAGE', "■ 判定コマンド(nTFLx-x+x or nTFLx+x-x)\n" + "  (必要成功数)TFL(判定ダイス数)+/-(修正ダイス数)\n" + "\n" + "※ 必要成功数と修正ダイス数は省略可能\n" + "\n" + "例1) 必要成功数1、判定ダイスは能力値3\n" + "      1TFL3\n" + "\n" + "例2）必要成功数不明、あるいはダイスボットの成功判定を使わない、判定ダイスは能力値3+技能1で4、アイテムの修正+1\n" + "      TFL4+1\n" + "\n" + "例3）必要成功数1、判定ダイスは能力値2+技能1で3、コンディションにチェックが2つ、アイテムの修正+1\n" + "      1TFL3-2+1\n" + "     あるいは以下のようにカッコ書きで内訳を詳細に記述することも可能。\n" + "      1TFL(2+1)-(1+1)+1\n" + "     修正ダイスのプラスとマイナスは逆でもよい。\n" + "      1TFL(2+1)+1-(1+1)\n");
        self.$register_prefix("(\\d+)?(TFL)");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, $b, self = this, parser = nil, parsed = nil, difficulty = nil, dice_pool = nil, ability_dice_text = nil, success_dice = nil;

          
          parser = $$$($$('Command'), 'Parser').$new(/\d*TFL\d+/, $hash2(["round_type"], {"round_type": self.$round_type()}));
          parsed = parser.$parse(command);
          if (!$truthy(parsed)) {
            return nil
          };
          $b = $send(parsed.$command().$split("TFL", 2), 'map', [], "to_i".$to_proc()), $a = $to_ary($b), (difficulty = ($a[0] == null ? nil : $a[0])), (dice_pool = ($a[1] == null ? nil : $a[1])), $b;
          dice_pool = $rb_plus(dice_pool, parsed.$modify_number());
          if ($truthy($rb_le(dice_pool, 0))) {
            dice_pool = 1
          };
          $b = self.$make_dice_roll(dice_pool), $a = $to_ary($b), (ability_dice_text = ($a[0] == null ? nil : $a[0])), (success_dice = ($a[1] == null ? nil : $a[1])), $b;
          return self.$make_dice_roll_text(difficulty, dice_pool, ability_dice_text, success_dice);
        });
        self.$private();
        
        $def(self, '$make_dice_roll_text', function $$make_dice_roll_text(difficulty, dice_pool, ability_dice_text, success_dice) {
          var dice_count_text = nil, push_dice = nil, reroll_command = nil;

          
          dice_count_text = "(" + (dice_pool) + "D6) ＞ " + (ability_dice_text) + " 成功数:" + (success_dice);
          push_dice = $rb_minus(dice_pool, success_dice);
          if ($truthy($rb_gt(push_dice, 0))) {
            
            dice_count_text = "" + (dice_count_text) + " 振り直し可能:" + (push_dice);
            reroll_command = "\n振り直しコマンド: TFL" + (push_dice);
          };
          if ($truthy($rb_gt(difficulty, 0))) {
            if ($truthy($rb_ge(success_dice, difficulty))) {
              return $$('Result').$success("" + (dice_count_text) + " 成功！" + (reroll_command))
            } else {
              return $$('Result').$failure("" + (dice_count_text) + " 失敗！" + (reroll_command))
            }
          };
          return "" + (dice_count_text) + (reroll_command);
        });
        return $def(self, '$make_dice_roll', function $$make_dice_roll(dice_pool) {
          var self = this, dice_list = nil, success_dice = nil;

          
          dice_list = self.randomizer.$roll_barabara(dice_pool, 6);
          success_dice = dice_list.$count(6);
          return ["[" + (dice_list.$join(",")) + "]", success_dice];
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
