require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_gt = Opal.rb_gt, $rb_le = Opal.rb_le, $rb_divide = Opal.rb_divide, $send = Opal.send, $rb_plus = Opal.rb_plus, $def = Opal.def, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'InfiniteFantasia');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "InfiniteFantasia");
        $const_set($nesting[0], 'NAME', "無限のファンタジア");
        $const_set($nesting[0], 'SORT_KEY', "むけんのふあんたしあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "1D20に目標値を設定した場合に、成功レベルの自動判定を行います。\n" + "例： 1D20<=16\n");
        return $def(self, '$result_1d20', function $$result_1d20(total, _dice_total, cmp_op, target) {
          var output = nil;

          
          if ($eqeq(target, "?")) {
            return $$('Result').$nothing()
          };
          if (!$eqeq(cmp_op, "<=")) {
            return nil
          };
          if ($truthy($rb_gt(total, target))) {
            return $$('Result').$failure("失敗")
          };
          output = ($truthy($rb_le(total, $rb_divide(target, 32).$to_i())) ? ("32レベル成功(32Lv+)") : ($truthy($rb_le(total, $rb_divide(target, 16).$to_i())) ? ("16レベル成功(16Lv+)") : ($truthy($rb_le(total, $rb_divide(target, 8).$to_i())) ? ("8レベル成功") : ($truthy($rb_le(total, $rb_divide(target, 4).$to_i())) ? ("4レベル成功") : ($truthy($rb_le(total, $rb_divide(target, 2).$to_i())) ? ("2レベル成功") : ("1レベル成功"))))));
          return $send($$('Result').$new(), 'tap', [], function $$1(r){var $a;

            
            if (r == null) r = nil;
            r['$text='](output);
            r['$success='](true);
            if ($truthy($rb_le(total, 1))) {
              
              r['$critical='](true);
              return ($a = [$rb_plus(r.$text(), "/クリティカル")], $send(r, 'text=', $a), $a[$a.length - 1]);
            } else {
              return nil
            };});
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
