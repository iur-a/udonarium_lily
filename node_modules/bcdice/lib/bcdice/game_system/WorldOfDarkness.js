require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $rb_lt = Opal.rb_lt, $rb_gt = Opal.rb_gt, $to_ary = Opal.to_ary, $rb_plus = Opal.rb_plus, $rb_times = Opal.rb_times, $rb_minus = Opal.rb_minus, $eqeq = Opal.eqeq, $def = Opal.def, $send = Opal.send, $eqeqeq = Opal.eqeqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'WorldOfDarkness');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "WorldOfDarkness");
        $const_set($nesting[0], 'NAME', "ワールド・オブ・ダークネス");
        $const_set($nesting[0], 'SORT_KEY', "わあるとおふたあくねす");
        $const_set($nesting[0], 'HELP_MESSAGE', "・判定コマンド(xSTn+y or xSTSn+y or xSTAn+y)\n" + "　(ダイス個数)ST(難易度)+(自動成功)\n" + "　(ダイス個数)STS(難易度)+(自動成功) ※出目10で振り足し、振り足し分の出目1で打ち消されない\n" + "　(ダイス個数)STB(難易度)+(自動成功) ※出目10で振り足し、振り足し分の出目1で打ち消される\n" + "　(ダイス個数)STA(難易度)+(自動成功) ※出目10は2成功 [20thルール]\n" + "\n" + "　難易度=省略時6\n" + "　自動成功=省略時0、出目1で打ち消されない自動成功を指定\n");
        self.$register_prefix("\\d+ST");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var $a, $b, self = this, difficulty = nil, auto_success = nil, enabled_reroll = nil, enabled_reroll_with_botch = nil, enabled_20th = nil, md = nil, dice_pool = nil, sequence = nil, total_success = nil, total_botch = nil, once_success = nil, dice = nil, ten_success = nil, success = nil, botch = nil;

          
          difficulty = 6;
          auto_success = 0;
          enabled_reroll = false;
          enabled_reroll_with_botch = false;
          enabled_20th = false;
          md = command.$match(/^(\d+)(ST[SAB]?)(\d+)?([+-]\d+)?/);
          dice_pool = md['$[]'](1).$to_i();
          
          switch (md['$[]'](2).valueOf()) {
            case "STS":
              enabled_reroll = true
              break;
            case "STA":
              enabled_20th = true
              break;
            case "STB":
              enabled_reroll_with_botch = true
              break;
            default:
              nil
          };
          if ($truthy(md['$[]'](3))) {
            difficulty = md['$[]'](3).$to_i()
          };
          if ($truthy(md['$[]'](4))) {
            auto_success = md['$[]'](4).$to_i()
          };
          if ($truthy($rb_lt(difficulty, 2))) {
            difficulty = 6
          };
          sequence = [];
          sequence.$push("DicePool=" + (dice_pool) + ", Difficulty=" + (difficulty) + ", AutomaticSuccess=" + (auto_success));
          if ($truthy($rb_gt(difficulty, 10))) {
            difficulty = 10
          };
          total_success = 0;
          total_botch = 0;
          once_success = false;
          $b = self.$roll_wod(dice_pool, difficulty), $a = $to_ary($b), (dice = ($a[0] == null ? nil : $a[0])), (ten_success = ($a[1] == null ? nil : $a[1])), (success = ($a[2] == null ? nil : $a[2])), (botch = ($a[3] == null ? nil : $a[3])), $b;
          sequence.$push(dice.$join(","));
          total_success = $rb_plus(total_success, success);
          total_botch = $rb_plus(total_botch, botch);
          if (($truthy($rb_gt(success, 0)) || ($truthy($rb_gt(ten_success, 0))))) {
            once_success = true
          };
          if ($truthy(enabled_20th)) {
            total_success = $rb_plus(total_success, $rb_times(ten_success, 2))
          } else {
            
            total_success = $rb_plus(total_success, ten_success);
            if (($truthy(enabled_reroll) || ($truthy(enabled_reroll_with_botch)))) {
              while ($truthy($rb_gt(ten_success, 0))) {
              
                $b = self.$roll_wod(ten_success, difficulty), $a = $to_ary($b), (dice = ($a[0] == null ? nil : $a[0])), (ten_success = ($a[1] == null ? nil : $a[1])), (success = ($a[2] == null ? nil : $a[2])), (botch = ($a[3] == null ? nil : $a[3])), $b;
                sequence.$push(dice.$join(","));
                total_success = $rb_plus(total_success, $rb_plus(success, ten_success));
                if ($truthy(enabled_reroll_with_botch)) {
                  total_botch = $rb_plus(total_botch, botch)
                };
              }
            };
          };
          total_success = $rb_minus(total_success, [total_success, total_botch].$min());
          total_success = $rb_plus(total_success, auto_success);
          if ($truthy($rb_gt(total_success, 0))) {
            
            sequence.$push("成功数" + (total_success));
            return $$('Result').$success(sequence.$join(" ＞ "));
          } else if (($truthy($rb_gt(total_botch, 0)) && ($eqeq(once_success, false)))) {
            
            sequence.$push("大失敗");
            return $$('Result').$fumble(sequence.$join(" ＞ "));
          } else {
            
            sequence.$push("失敗");
            return $$('Result').$failure(sequence.$join(" ＞ "));
          };
        });
        return $def(self, '$roll_wod', function $$roll_wod(dice_pool, difficulty) {
          var self = this, dice = nil, success = nil, botch = nil, ten_success = nil;

          
          dice = $send($$('Array'), 'new', [dice_pool], function $$1(){var self = $$1.$$s == null ? this : $$1.$$s, dice_now = nil;
            if (self.randomizer == null) self.randomizer = nil;

            
            dice_now = self.randomizer.$roll_once(10);
            return dice_now;}, {$$s: self});
          dice['$sort!']();
          success = 0;
          botch = 0;
          ten_success = 0;
          $send(dice, 'each', [], function $$2(d){var $ret_or_1 = nil;

            
            if (d == null) d = nil;
            if ($eqeqeq(10, ($ret_or_1 = d))) {
              return (ten_success = $rb_plus(ten_success, 1))
            } else if ($eqeqeq(Opal.Range.$new(difficulty,10, true), $ret_or_1)) {
              return (success = $rb_plus(success, 1))
            } else if ($eqeqeq(1, $ret_or_1)) {
              return (botch = $rb_plus(botch, 1))
            } else {
              return nil
            };});
          return [dice, ten_success, success, botch];
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
