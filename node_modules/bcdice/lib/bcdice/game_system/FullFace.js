require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $eqeq = Opal.eqeq, $send = Opal.send, $rb_le = Opal.rb_le, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $rb_ge = Opal.rb_ge, $thrower = Opal.thrower, $hash2 = Opal.hash2, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'FullFace');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.round_type = $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "FullFace");
        $const_set($nesting[0], 'NAME', "フルフェイス");
        $const_set($nesting[0], 'SORT_KEY', "ふるふえいす");
        $const_set($nesting[0], 'HELP_MESSAGE', "■判定　x+bFF<=a[,t][&d]   x:ヒート(省略時は3) b:判定修正 a:能力値 t:難易度(省略可) d:基本ダメージ(省略可)\n" + "\n" + "例)FF<=2:     能力値2で判定し、その結果(成功数,1の目の数,6の目の数,バースト)を表示。\n" + "   6FF<=3:    ヒート6,能力値3で戦闘判定し、その結果( 〃 )を表示。\n" + "   8+2FF<=3:  ヒート8,判定修正+2,能力値3で戦闘判定し、その結果( 〃 )を表示。\n" + "   FF<=2,1:   能力値2,難易度1で判定し、その結果(成功数,1の目の数,6の目の数,成功・失敗,バースト)を表示。\n" + "   6FF<=3,2&1:ヒート6,能力値3,難易度2,基本ダメージ1で戦闘判定し、その結果(成功数,1の目の数,6の目の数,ダメージ,バースト)を表示。\n" + "\n" + "■ジャンク表　JKT\n" + "\n");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          return (self.sort_barabara_dice = true);
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($truthy(($ret_or_1 = self.$resolute_action(command)))) {
            return $ret_or_1
          } else {
            return self.$roll_tables(command, $$('TABLES'))
          }
        });
        self.$private();
        
        $def(self, '$resolute_action', function $$resolute_action(command) {try { var $t_eval_return = $thrower('eval_return'); 
          var self = this, m = nil, heat_level = nil, modify = nil, status_no = nil, target_no = nil, damage_no = nil, dice_array = nil, dice = nil, ones = nil, sixs = nil, success_num = nil, ones_total = nil;

          
          m = /^(\d*)([+\d]+)*FF<=(\d)(,(\d))?(&(\d))?$/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          heat_level = m['$[]'](1).$to_i();
          if ($eqeq(heat_level, 0)) {
            heat_level = 3
          };
          modify = $$('Arithmetic').$eval("0" + (m['$[]'](2)), self.round_type);
          status_no = m['$[]'](3).$to_i();
          target_no = m['$[]'](5).$to_i();
          damage_no = m['$[]'](7).$to_i();
          dice_array = [];
          dice = self.randomizer.$roll_barabara(heat_level, 6).$sort();
          ones = dice.$count(1);
          sixs = dice.$count(6);
          success_num = $send(dice, 'count', [], function $$1(val){
            
            if (val == null) val = nil;
            return $rb_le(val, status_no);});
          dice_array.$push(dice.$join(","));
          if ($truthy($rb_gt(modify, 0))) {
            
            dice = self.randomizer.$roll_barabara(modify, 6).$sort();
            ones = $rb_plus(ones, dice.$count(1));
            success_num = $rb_plus(success_num, $send(dice, 'count', [], function $$2(val){
              
              if (val == null) val = nil;
              return $rb_le(val, status_no);}));
            dice_array.$push(dice.$join(","));
          };
          ones_total = ones;
          while ($truthy($rb_gt(ones, 0))) {
          
            dice = self.randomizer.$roll_barabara(ones, 6).$sort();
            ones = dice.$count(1);
            ones_total = $rb_plus(ones_total, ones);
            success_num = $rb_plus(success_num, $send(dice, 'count', [], function $$3(val){
              
              if (val == null) val = nil;
              return $rb_le(val, status_no);}));
            dice_array.$push(dice.$join(","));
          };
          return $send($$('Result').$new(), 'tap', [], function $$4(result){var $a, command_out = nil, result_txt = nil, damage = nil, sequence = nil;

            
            if (result == null) result = nil;
            command_out = "(" + (heat_level) + ($$('Format').$modifier(modify)) + "FF<=" + (status_no);
            if ($truthy($rb_ge(sixs, 2))) {
              
              result['$fumble='](true);
              result['$condition='](false);
            } else {
              
              result['$condition=']($rb_gt(success_num, 0));
              result['$critical=']($rb_gt(ones_total, 0));
            };
            result_txt = [];
            result_txt.$push("成功度(" + (success_num) + ")");
            if ($truthy($rb_gt(ones_total, 0))) {
              result_txt.$push("1の目(" + (ones_total) + ")")
            };
            if ($truthy($rb_gt(sixs, 0))) {
              result_txt.$push("6の目(" + (sixs) + ")")
            };
            if ($truthy($rb_gt(target_no, 0))) {
              
              command_out = $rb_plus(command_out, "," + (target_no));
              if ($truthy($rb_ge(success_num, target_no))) {
                
                result_txt.$push("成功");
                result['$condition='](true);
              } else {
                
                result_txt.$push("失敗");
                result['$condition='](false);
              };
            };
            if ($truthy($rb_gt(damage_no, 0))) {
              
              command_out = $rb_plus(command_out, "&" + (damage_no));
              damage = $rb_plus(damage_no, ones_total);
              result_txt.$push("ダメージ(" + (damage) + ")");
            };
            if ($truthy(result['$fumble?']())) {
              result_txt.$push("バースト")
            };
            command_out = $rb_plus(command_out, ")");
            sequence = [command_out, dice_array.$join("+").$to_s(), result_txt.$join(",").$to_s()].$compact();
            return ($a = [sequence.$join(" ＞ ")], $send(result, 'text=', $a), $a[$a.length - 1]);});} catch($e) {
            if ($e === Opal.t_eval_return) return $e.$v;
            throw $e;
          }
        });
        $const_set($nesting[0], 'TABLES', $hash2(["JKT"], {"JKT": $$$($$('DiceTable'), 'Table').$new("ジャンク表", "2D6", ["命欲しさに重要な情報を吐いた。セッションのボスに関する情報を得る。", "ユニットの機密文書だ。この戦闘で獲得したユニットがあるなら、そのうち好きなユニット1つを経験点を消費せずに常備化できる。", "違法アップロードされた個人情報のデータだ。このセッション中、エネミーと出会ったとき、詳細なデータが即座に公開される（GMはできるだけ拒否しないこと）。", "鍵を持っていた。近くに施錠された扉や箱などがあるなら、そのうちの1つを開けることができる。", "何も見つけられなかったが、敵を倒したことによって自信を得た。このセッション中のみ、1回だけ自身の判定で出た6の目を1つ消すことができる。", "自爆装置だ。何も残らなかった。", "使い捨ての武器を手に入れた。このセッション中のみ、1回だけ「近接攻撃5『本能』」の攻撃アクションを行える。", "敵が改心した。倒されたキャラクターがまだ生きているなら、そのうちの一人が君たちに協力を申し込んでくる（このセッション限定の恩恵「人脈：協力者」を得る）。", "金を手に入れた。キャラクター全員、アフターフェイズに配布される経験点が1増加する。", "強力なユニットを隠し持っていた。好きな組織専用ユニットを1つ獲得する。", "大金を手に入れた。キャラクター全員、アフターフェイズに配布される経験点が3増加する。"])}).$freeze());
        return self.$register_prefix("([+\\d]+)*FF", $$('TABLES').$keys());
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
