require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $truthy = Opal.truthy, $def = Opal.def, $eqeq = Opal.eqeq, $rb_plus = Opal.rb_plus, $neqeq = Opal.neqeq, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'NervWhitePaper');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "NervWhitePaper");
        $const_set($nesting[0], 'NAME', "新世紀エヴァンゲリオンRPG NERV白書/使徒降臨");
        $const_set($nesting[0], 'SORT_KEY', "しんせいきえうあんけりおんああるひいしいねるふはくしよしとこおりん");
        $const_set($nesting[0], 'HELP_MESSAGE', "■通常ロール(NR)：成功、失敗、絶対成功、絶対失敗を表示します。\n" + "例) NR\n" + "\n" + "■長所ロール(NA)：成功、失敗、絶対成功、絶対失敗を表示します。\n" + "例) NA\n" + "\n" + "■短所ロール(ND)：成功、失敗、絶対成功、絶対失敗を表示します。\n" + "例) ND\n" + "\n");
        self.$register_prefix("N[RAD]");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil, $ret_or_2 = nil;

          if ($truthy(($ret_or_1 = ($truthy(($ret_or_2 = self.$resolute_regular_action(command))) ? ($ret_or_2) : (self.$resolute_advantage_action(command)))))) {
            return $ret_or_1
          } else {
            return self.$resolute_disadvantage_action(command)
          }
        });
        self.$private();
        
        $def(self, '$resolute_regular_action', function $$resolute_regular_action(command) {
          var self = this, m = nil, dices = nil, dice_text = nil, dice_add = nil, output = nil;

          
          m = /NR/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dices = self.randomizer.$roll_barabara(2, 6);
          dice_text = dices.$join(",");
          dice_add = dices.$sum();
          output = "(NR) ＞ " + (dice_text);
          if ($eqeq(dice_add, 7)) {
            
            output = $rb_plus(output, " ＞ 絶対成功");
            return $$('Result').$critical(output);
          } else if ($eqeq(dice_add, 2)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($eqeq(dice_add, 12)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($eqeq(dice_add.$modulo(2), 0)) {
            
            output = $rb_plus(output, " ＞ 失敗");
            return $$('Result').$failure(output);
          } else {
            
            output = $rb_plus(output, " ＞ 成功");
            return $$('Result').$success(output);
          };
        });
        
        $def(self, '$resolute_advantage_action', function $$resolute_advantage_action(command) {
          var self = this, m = nil, dices = nil, dice_text = nil, dice_add = nil, output = nil;

          
          m = /NA/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dices = self.randomizer.$roll_barabara(2, 6);
          dice_text = dices.$join(",");
          dice_add = dices.$sum();
          output = "(NA) ＞ " + (dice_text);
          if ($eqeq(dice_add, 7)) {
            
            output = $rb_plus(output, " ＞ 絶対成功");
            return $$('Result').$critical(output);
          } else if ($eqeq(dice_add, 2)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($eqeq(dice_add, 12)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($eqeq(dices['$[]'](0), dices['$[]'](1))) {
            
            output = $rb_plus(output, " ＞ 失敗");
            return $$('Result').$failure(output);
          } else {
            
            output = $rb_plus(output, " ＞ 成功");
            return $$('Result').$success(output);
          };
        });
        return $def(self, '$resolute_disadvantage_action', function $$resolute_disadvantage_action(command) {
          var self = this, m = nil, dices = nil, dice_text = nil, dice_add = nil, output = nil;

          
          m = /ND/.$match(command);
          if (!$truthy(m)) {
            return nil
          };
          dices = self.randomizer.$roll_barabara(2, 6);
          dice_text = dices.$join(",");
          dice_add = dices.$sum();
          output = "(ND) ＞ " + (dice_text);
          if ($eqeq(dice_add, 7)) {
            
            output = $rb_plus(output, " ＞ 絶対成功");
            return $$('Result').$critical(output);
          } else if ($eqeq(dice_add, 2)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($eqeq(dice_add, 12)) {
            
            output = $rb_plus(output, " ＞ 絶対失敗");
            return $$('Result').$fumble(output);
          } else if ($neqeq(dice_add, 7)) {
            
            output = $rb_plus(output, " ＞ 失敗");
            return $$('Result').$failure(output);
          } else {
            return nil
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
