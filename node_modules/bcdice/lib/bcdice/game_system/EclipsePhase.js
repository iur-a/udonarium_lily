require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeq = Opal.eqeq, $rb_divide = Opal.rb_divide, $truthy = Opal.truthy, $rb_le = Opal.rb_le, $rb_ge = Opal.rb_ge, $rb_minus = Opal.rb_minus, $def = Opal.def, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'EclipsePhase');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "EclipsePhase");
        $const_set($nesting[0], 'NAME', "エクリプス・フェイズ");
        $const_set($nesting[0], 'SORT_KEY', "えくりふすふえいす");
        $const_set($nesting[0], 'HELP_MESSAGE', "1D100<=m 方式の判定で成否、クリティカル・ファンブルを自動判定");
        return $def(self, '$result_1d100', function $$result_1d100(total, _dice_total, cmp_op, target) {
          var dice_value = nil, dice_ten_place = nil, dice_one_place = nil, diff_threshold = nil;

          
          if ($eqeq(target, "?")) {
            return nil
          };
          if (!$eqeq(cmp_op, "<=")) {
            return nil
          };
          dice_value = total['$%'](100);
          dice_ten_place = $rb_divide(dice_value, 10).$to_i();
          dice_one_place = dice_value['$%'](10);
          if ($eqeq(dice_ten_place, dice_one_place)) {
            
            if ($eqeq(dice_value, 99)) {
              return $$('Result').$fumble("決定的失敗")
            };
            if ($eqeq(dice_value, 0)) {
              return $$('Result').$critical("00 ＞ 決定的成功")
            };
            if ($truthy($rb_le(total, target))) {
              return $$('Result').$critical("決定的成功")
            };
            return $$('Result').$fumble("決定的失敗");
          };
          diff_threshold = 30;
          if ($truthy($rb_le(total, target))) {
            if ($truthy($rb_ge(total, diff_threshold))) {
              return $$('Result').$success("エクセレント")
            } else {
              return $$('Result').$success("成功")
            }
          } else if ($truthy($rb_ge($rb_minus(total, target), diff_threshold))) {
            return $$('Result').$failure("シビア")
          } else {
            return $$('Result').$failure("失敗")
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
