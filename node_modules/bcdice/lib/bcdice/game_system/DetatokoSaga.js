require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $send2 = Opal.send2, $find_super = Opal.find_super, $def = Opal.def, $truthy = Opal.truthy, $hash2 = Opal.hash2, $rb_plus = Opal.rb_plus, $to_ary = Opal.to_ary, $rb_minus = Opal.rb_minus, $eqeq = Opal.eqeq, $neqeq = Opal.neqeq, $rb_ge = Opal.rb_ge, $rb_gt = Opal.rb_gt, $eqeqeq = Opal.eqeqeq, $thrower = Opal.thrower, $rb_divide = Opal.rb_divide, $rb_times = Opal.rb_times, $send = Opal.send, $defs = Opal.defs, $nesting = [], nil = Opal.nil, $$$ = Opal.$$$;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'DetatokoSaga');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "DetatokoSaga");
        $const_set($nesting[0], 'NAME', "でたとこサーガ");
        $const_set($nesting[0], 'SORT_KEY', "てたとこさあか");
        $const_set($nesting[0], 'HELP_MESSAGE', "・通常判定　xDS or xDSy or xDS>=t or xDSy>=t or xDS+z>=t or xDSy+z>=t\n" + "　(x＝スキルランク、y＝現在フラグ値(省略時0)、z＝修正値(省略時０)、t＝目標値(省略時８))\n" + "　例）3DS　2DS5　0DS　3DS>=10　3DS7>=12 2DS3+1 3DS2+1>=10\n" + "・判定値　xJD or xJDy or xJDy+z or xJDy-z or xJDy/z\n" + "　(x＝スキルランク、y＝現在フラグ値(省略時0)、z＝修正値(省略時０))\n" + "　例）3JD　2JD5　3JD7+1　4JD/3\n" + "・体力烙印表　SST (StrengthStigmaTable)\n" + "・気力烙印表　WST (WillStigmaTable)\n" + "・体力バッドエンド表　SBET (StrengthBadEndTable)\n" + "・気力バッドエンド表　WBET (WillBadEndTable)\n");
        self.$register_prefix("\\d+DS", "\\d+JD");
        
        $def(self, '$initialize', function $$initialize(command) {
          var $yield = $$initialize.$$p || nil, self = this;

          $$initialize.$$p = null;
          
          $send2(self, $find_super(self, 'initialize', $$initialize, false, true), 'initialize', [command], null);
          self.sort_add_dice = true;
          return (self.d66_sort_type = $$$($$('D66SortType'), 'ASC'));
        });
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, result = nil, $ret_or_1 = nil;

          
          self.$debug("eval_game_system_specific_command begin string", command);
          result = self.$checkRoll(command);
          if ($truthy(result)) {
            return result
          };
          result = self.$checkJudgeValue(command);
          if ($truthy(result)) {
            return result
          };
          self.$debug("各種表として処理");
          return self.$roll_tables(($truthy(($ret_or_1 = $$('ALIAS')['$[]'](command))) ? ($ret_or_1) : (command)), $$$(self.$class(), 'TABLES'));
        });
        
        $def(self, '$checkRoll', function $$checkRoll(string) {
          var $a, $b, $c, self = this, m = nil, skill = nil, flag = nil, operator = nil, value = nil, target = nil, $ret_or_1 = nil, result = nil, modifyText = nil, total = nil, rollText = nil, totalResult = nil, success = nil;

          
          self.$debug("checkRoll begin string", string);
          m = /^(\d+)DS(\d+)?(([+-\/])(\d+))?(?:>=(\d+))?$/i.$match(string);
          if (!$truthy(m)) {
            return nil
          };
          skill = m['$[]'](1).$to_i();
          flag = m['$[]'](2).$to_i();
          operator = m['$[]'](4);
          value = m['$[]'](5).$to_i();
          target = ($truthy(($ret_or_1 = ($a = m['$[]'](6), ($a === nil || $a == null) ? nil : $a.$to_i()))) ? ($ret_or_1) : (8));
          result = self.$translate("DetatokoSaga.DS.input_options", $hash2(["skill", "flag", "target"], {"skill": skill, "flag": flag, "target": target}));
          modifyText = self.$getModifyText(operator, value);
          if (!$truthy(modifyText['$empty?']())) {
            result = $rb_plus(result, self.$translate("DetatokoSaga.DS.modifier", $hash2(["modifier"], {"modifier": modifyText})))
          };
          $c = self.$getRollResult(skill), $b = $to_ary($c), (total = ($b[0] == null ? nil : $b[0])), (rollText = ($b[1] == null ? nil : $b[1])), $c;
          result = $rb_plus(result, " ＞ " + (total) + "[" + (rollText) + "]" + (modifyText));
          totalResult = self.$getTotalResultValue(total, value, operator);
          result = $rb_plus(result, " ＞ " + (totalResult));
          if (!$truthy(modifyText['$empty?']())) {
            
            switch (operator.valueOf()) {
              case "+":
                total = $rb_plus(total, value)
                break;
              case "-":
                total = $rb_minus(total, value)
                break;
              default:
                nil
            }
          };
          success = self.$getSuccess(total, target);
          result = $rb_plus(result, " ＞ " + (success));
          result = $rb_plus(result, self.$getCheckFlagResult(total, flag));
          return result;
        });
        
        $def(self, '$getRollResult', function $$getRollResult(skill) {
          var self = this, diceCount = nil, dice = nil, diceText = nil, total = nil;

          
          diceCount = $rb_plus(skill, 1);
          if ($eqeq(skill, 0)) {
            diceCount = 3
          };
          dice = self.randomizer.$roll_barabara(diceCount, 6);
          diceText = dice.$join(",");
          dice = dice.$sort();
          if ($neqeq(skill, 0)) {
            dice = dice.$reverse()
          };
          total = $rb_plus(dice['$[]'](0), dice['$[]'](1));
          return [total, diceText];
        });
        
        $def(self, '$getSuccess', function $$getSuccess(check, target) {
          var self = this;

          if ($truthy($rb_ge(check, target))) {
            return self.$translate("DetatokoSaga.DS.success")
          } else {
            return self.$translate("DetatokoSaga.DS.failure")
          }
        });
        
        $def(self, '$getCheckFlagResult', function $$getCheckFlagResult(total, flag) {
          var self = this, will = nil;

          
          if ($truthy($rb_gt(total, flag))) {
            return ""
          };
          will = self.$getDownWill(flag);
          return self.$translate("DetatokoSaga.less_than_flag", $hash2(["will"], {"will": will}));
        });
        
        $def(self, '$getDownWill', function $$getDownWill(flag) {
          var self = this, dice = nil;

          
          if ($truthy($rb_ge(flag, 10))) {
            return "6"
          };
          dice = self.randomizer.$roll_once(6);
          return "1D6->" + (dice);
        });
        
        $def(self, '$checkJudgeValue', function $$checkJudgeValue(string) {
          var $a, $b, self = this, m = nil, skill = nil, flag = nil, operator = nil, value = nil, result = nil, modifyText = nil, total = nil, rollText = nil, totalResult = nil;

          
          self.$debug("checkJudgeValue begin string", string);
          m = /^(\d+)JD(\d+)?(([+-\/])(\d+))?$/i.$match(string);
          if (!$truthy(m)) {
            return nil
          };
          skill = m['$[]'](1).$to_i();
          flag = m['$[]'](2).$to_i();
          operator = m['$[]'](4);
          value = m['$[]'](5).$to_i();
          result = self.$translate("DetatokoSaga.JD.input_options", $hash2(["skill", "flag"], {"skill": skill, "flag": flag}));
          modifyText = self.$getModifyText(operator, value);
          if (!$truthy(modifyText['$empty?']())) {
            result = $rb_plus(result, self.$translate("DetatokoSaga.JD.modifier", $hash2(["modifier"], {"modifier": modifyText})))
          };
          $b = self.$getRollResult(skill), $a = $to_ary($b), (total = ($a[0] == null ? nil : $a[0])), (rollText = ($a[1] == null ? nil : $a[1])), $b;
          result = $rb_plus(result, " ＞ " + (total) + "[" + (rollText) + "]" + (modifyText));
          totalResult = self.$getTotalResultValue(total, value, operator);
          result = $rb_plus(result, " ＞ " + (totalResult));
          result = $rb_plus(result, self.$getCheckFlagResult(total, flag));
          return result;
        });
        
        $def(self, '$getModifyText', function $$getModifyText(operator, value) {try { var $t_return = $thrower('return'); 
          var operatorText = nil, $ret_or_1 = nil;

          
          if ($eqeq(value, 0)) {
            return ""
          };
          operatorText = (function() {if ($eqeqeq("+", ($ret_or_1 = operator))) {
            return "＋"
          } else if ($eqeqeq("-", $ret_or_1)) {
            return "－"
          } else if ($eqeqeq("/", $ret_or_1)) {
            return "÷"
          } else {
            $t_return.$throw("")
          }})();
          return "" + (operatorText) + (value);} catch($e) {
            if ($e === $t_return) return $e.$v;
            throw $e;
          }
        });
        
        $def(self, '$getTotalResultValue', function $$getTotalResultValue(total, value, operator) {
          var self = this;

          
          switch (operator.valueOf()) {
            case "+":
              return $rb_plus("" + (total) + "+" + (value) + " ＞ ", self.$translate("DetatokoSaga.total_value", $hash2(["total"], {"total": $rb_plus(total, value)})))
            case "-":
              return $rb_plus("" + (total) + "-" + (value) + " ＞ ", self.$translate("DetatokoSaga.total_value", $hash2(["total"], {"total": $rb_minus(total, value)})))
            case "/":
              return self.$getTotalResultValueWhenSlash(total, value)
            default:
              return self.$translate("DetatokoSaga.total_value", $hash2(["total"], {"total": total}))
          }
        });
        
        $def(self, '$getTotalResultValueWhenSlash', function $$getTotalResultValueWhenSlash(total, value) {
          var self = this, quotient = nil, result = nil;

          
          if ($eqeq(value, 0)) {
            return self.$translate("DetatokoSaga.division_by_zero_error")
          };
          quotient = $rb_divide($rb_times(1.0, total), value).$ceil();
          result = $rb_plus("" + (total) + "÷" + (value) + " ＞ ", self.$translate("DetatokoSaga.total_value", $hash2(["total"], {"total": quotient})));
          return result;
        });
        $const_set($nesting[0], 'ALIAS', $send($hash2(["StrengthStigmaTable", "WillStigmaTable", "StrengthBadEndTable", "WillBadEndTable"], {"StrengthStigmaTable": "SST", "WillStigmaTable": "WST", "StrengthBadEndTable": "SBET", "WillBadEndTable": "WBET"}), 'transform_keys', [], "upcase".$to_proc()).$freeze());
        $defs(self, '$translate_tables', function $$translate_tables(locale) {
          
          return $hash2(["SST", "WST", "SBET", "WBET"], {"SST": $$$($$('DiceTable'), 'Table').$from_i18n("DetatokoSaga.table.SST", locale), "WST": $$$($$('DiceTable'), 'Table').$from_i18n("DetatokoSaga.table.WST", locale), "SBET": $$$($$('DiceTable'), 'Table').$from_i18n("DetatokoSaga.table.SBET", locale), "WBET": $$$($$('DiceTable'), 'Table').$from_i18n("DetatokoSaga.table.WBET", locale)})
        });
        $const_set($nesting[0], 'TABLES', self.$translate_tables("ja_jp").$freeze());
        return self.$register_prefix($$('TABLES').$keys(), $$('ALIAS').$keys());
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
