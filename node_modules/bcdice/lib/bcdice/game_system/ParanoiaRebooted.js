require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeqeq = Opal.eqeqeq, $def = Opal.def, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_plus = Opal.rb_plus, $send = Opal.send, $rb_ge = Opal.rb_ge, $rb_lt = Opal.rb_lt, $rb_minus = Opal.rb_minus, $to_ary = Opal.to_ary, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'ParanoiaRebooted');

        var $nesting = [self].concat($parent_nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "ParanoiaRebooted");
        $const_set($nesting[0], 'NAME', "パラノイア リブーテッド");
        $const_set($nesting[0], 'SORT_KEY', "はらのいありふうてつと");
        $const_set($nesting[0], 'HELP_MESSAGE', "※コマンドは入力内容の前方一致で検出しています。\n" + "・通常の判定　NDx\n" + "　x：ノードダイスの数.マイナスも可.\n" + "　ノードダイスの絶対値 + 1個(コンピュータダイス)のダイスがロールされる.\n" + "例）ND2　ND-3\n" + "\n" + "・ミュータントパワー判定　MPx\n" + "  x：ノードダイスの数.\n" + "　ノードダイスの値 + 1個(コンピュータダイス)のダイスがロールされる.\n" + "例）MP2\n");
        self.$register_prefix("ND", "MP");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, $ret_or_1 = nil;

          if ($eqeqeq(/^ND/i, ($ret_or_1 = command))) {
            return self.$get_node_dice_roll(command)
          } else if ($eqeqeq(/^MP/i, $ret_or_1)) {
            return self.$get_mutant_power_roll(command)
          } else {
            return nil
          }
        });
        self.$private();
        
        $def(self, '$generate_roll_results', function $$generate_roll_results(dices) {
          var computer_dice_message = nil, results = nil;

          
          computer_dice_message = "";
          results = dices.$dup();
          if ($eqeq(results['$[]'](-1).$to_i(), 6)) {
            
            results['$[]='](-1, "C");
            computer_dice_message = "(Computer)";
          };
          return [results, computer_dice_message];
        });
        
        $def(self, '$get_node_dice_roll', function $$get_node_dice_roll(command) {
          var $a, $b, self = this, m = nil, parameter_num = nil, dice_count = nil, dices = nil, success_rate = nil, results = nil, computer_dice_message = nil;

          
          self.$debug("eval_game_system_specific_command Begin");
          m = /^ND((-)?\d+)/i.$match(command);
          if (!$truthy(m)) {
            return ""
          };
          self.$debug("command", command);
          parameter_num = m['$[]'](1).$to_i();
          dice_count = $rb_plus(parameter_num.$abs(), 1);
          dices = self.randomizer.$roll_barabara(dice_count, 6);
          success_rate = $send(dices, 'count', [], function $$1(dice){
            
            if (dice == null) dice = nil;
            return $rb_ge(dice, 5);});
          if ($truthy($rb_lt(parameter_num, 0))) {
            success_rate = $rb_minus(success_rate, $send(dices, 'count', [], function $$2(dice){
              
              if (dice == null) dice = nil;
              return $rb_lt(dice, 5);}))
          };
          self.$debug(dices);
          $b = self.$generate_roll_results(dices), $a = $to_ary($b), (results = ($a[0] == null ? nil : $a[0])), (computer_dice_message = ($a[1] == null ? nil : $a[1])), $b;
          self.$debug("eval_game_system_specific_command result");
          return "(" + (command) + ") ＞ [" + (results.$join(", ")) + "] ＞ 成功度" + (success_rate) + (computer_dice_message);
        });
        return $def(self, '$get_mutant_power_roll', function $$get_mutant_power_roll(command) {
          var $a, $b, self = this, m = nil, parameter_num = nil, dice_count = nil, dices = nil, failure_rate = nil, message = nil, results = nil, computer_dice_message = nil;

          
          self.$debug("eval_game_system_specific_command Begin");
          m = /^MP(\d+)/i.$match(command);
          if (!$truthy(m)) {
            return ""
          };
          self.$debug("command", command);
          parameter_num = m['$[]'](1).$to_i();
          dice_count = $rb_plus(parameter_num.$abs(), 1);
          dices = self.randomizer.$roll_barabara(dice_count, 6);
          failure_rate = dices.$count(1);
          message = ($eqeq(failure_rate, 0) ? ("成功") : ("失敗(" + (failure_rate) + ")"));
          $b = self.$generate_roll_results(dices), $a = $to_ary($b), (results = ($a[0] == null ? nil : $a[0])), (computer_dice_message = ($a[1] == null ? nil : $a[1])), $b;
          self.$debug(dices);
          self.$debug("eval_game_system_specific_command result");
          return "(" + (command) + ") ＞ [" + (results.$join(", ")) + "] ＞ " + (message) + (computer_dice_message);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
