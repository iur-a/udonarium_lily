require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $eqeqeq = Opal.eqeqeq, $truthy = Opal.truthy, $send = Opal.send, $rb_plus = Opal.rb_plus, $def = Opal.def, $eqeq = Opal.eqeq, $rb_ge = Opal.rb_ge, $rb_lt = Opal.rb_lt, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'TherapieSein');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting), $proto = self.$$prototype;

        $proto.randomizer = nil;
        
        $const_set($nesting[0], 'ID', "TherapieSein");
        $const_set($nesting[0], 'NAME', "セラフィザイン");
        $const_set($nesting[0], 'SORT_KEY', "せらふいさいん");
        $const_set($nesting[0], 'HELP_MESSAGE', "・一般判定：TS[n][±m][@t]　　[]内のコマンドは省略可能。クリティカル無。\n" + "・戦闘判定：OP[n][±m][@t]　　[]内のコマンドは省略可能。クリティカル有。\n" + "\n" + "「n」で能力値修正などを指定。\n" + "「±m」で達成値への修正値を追加指定。+5+1-3のように、複数指定も可能です。\n" + "「@t」で目標値を指定。省略時は達成値のみ表示、指定時は判定の正否を追加表示。\n" + "\n" + "【書式例】\n" + "・TS → ダイスの合計値を達成値として表示。\n" + "・TS4 → ダイス合計+4を達成値表示。\n" + "・TS4-1 → ダイス合計+4-1（計+3）を達成値表示。\n" + "・TS2+1@10 → ダイス合計+2+1（計+3）の達成値と、判定の成否を表示。\n" + "・OP4+3+1 → ダイス合計+4+3+1（計+8）を達成値＆クリティカル表示。\n" + "・OP3@12 → ダイス合計+3の達成値＆クリティカル、判定の成否を表示。\n");
        self.$register_prefix("TS", "OP");
        
        $def(self, '$eval_game_system_specific_command', function $$eval_game_system_specific_command(command) {
          var self = this, output = nil, $ret_or_1 = nil, hasCritical = nil, target = nil, $ret_or_2 = nil, modify = nil, modifyAddString = nil, modify_list = nil;

          
          output = ($eqeqeq(/(TS|OP)(\d+)?(([+-]\d+)*)(@(\d+))?$/i, ($ret_or_1 = command.$upcase())) ? (((hasCritical = $$('Regexp').$last_match(1)['$==']("OP")), (target = ($truthy(($ret_or_2 = $$('Regexp').$last_match(6))) ? ($ret_or_2) : (0)).$to_i()), (modify = ($truthy(($ret_or_2 = $$('Regexp').$last_match(2))) ? ($ret_or_2) : (0)).$to_i()), (modifyAddString = $$('Regexp').$last_match(3)), (modify_list = modifyAddString.$scan(/[+-]\d+/)), $send(modify_list, 'each', [], function $$1(i){
            
            if (i == null) i = nil;
            return (modify = $rb_plus(modify, i.$to_i()));}), self.$checkRoll(hasCritical, modify, target))) : (nil));
          return output;
        });
        
        $def(self, '$checkRoll', function $$checkRoll(hasCritical, modify, target) {
          var self = this, dice_list = nil, dice = nil, diceText = nil, successValue = nil, modifyText = nil, targetText = nil, result = nil;

          
          dice_list = self.randomizer.$roll_barabara(2, 6);
          dice = dice_list.$sum();
          diceText = dice_list.$join(",");
          successValue = $rb_plus(dice, modify);
          modifyText = self.$getValueText(modify);
          targetText = ($eqeq(target, 0) ? ("") : (">=" + (target)));
          result = "(2D6" + (modifyText) + (targetText) + ")";
          result = $rb_plus(result, " ＞ " + (dice) + "(" + (diceText) + ")" + (modifyText));
          if (($truthy(hasCritical) && ($eqeq(dice, 12)))) {
            
            result = $rb_plus(result, " ＞ クリティカル！");
            return result;
          };
          result = $rb_plus(result, " ＞ " + (successValue) + (targetText));
          if ($eqeq(target, 0)) {
            return result
          };
          if ($truthy($rb_ge(successValue, target))) {
            result = $rb_plus(result, " ＞ 【成功】")
          } else {
            result = $rb_plus(result, " ＞ 【失敗】")
          };
          return result;
        });
        return $def(self, '$getValueText', function $$getValueText(value) {
          
          
          if ($eqeq(value, 0)) {
            return ""
          };
          if ($truthy($rb_lt(value, 0))) {
            return value.$to_s()
          };
          return "+" + (value);
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
