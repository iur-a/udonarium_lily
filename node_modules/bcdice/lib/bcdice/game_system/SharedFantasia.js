require('./../opal');

Opal.queue(function(Opal) {/* Generated by Opal 1.7.4 */
  var $module = Opal.module, $klass = Opal.klass, $const_set = Opal.const_set, $def = Opal.def, $eqeq = Opal.eqeq, $truthy = Opal.truthy, $rb_gt = Opal.rb_gt, $rb_plus = Opal.rb_plus, $nesting = [], nil = Opal.nil;

  return (function($base, $parent_nesting) {
    var self = $module($base, 'BCDice');

    var $nesting = [self].concat($parent_nesting);

    return (function($base, $parent_nesting) {
      var self = $module($base, 'GameSystem');

      var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

      return (function($base, $super, $parent_nesting) {
        var self = $klass($base, $super, 'SharedFantasia');

        var $nesting = [self].concat($parent_nesting), $$ = Opal.$r($nesting);

        
        $const_set($nesting[0], 'ID', "SharedFantasia");
        $const_set($nesting[0], 'NAME', "Shared†Fantasia");
        $const_set($nesting[0], 'SORT_KEY', "しえああとふあんたしあ");
        $const_set($nesting[0], 'HELP_MESSAGE', "2D6の成功判定に 自動成功、自動失敗、致命的失敗、劇的成功 の判定があります。\n" + "\n" + "SF/ST = 2D6のショートカット\n" + "\n" + "例) SF+4>=9 : 2D6して4を足した値が9以上なら成功\n");
        self.$register_prefix("SF", "ST");
        
        $def(self, '$change_text', function $$change_text(string) {
          
          return string.$gsub(/S[FT]/i, "2D6")
        });
        return $def(self, '$result_2d6', function $$result_2d6(total, dice_total, _dice_list, cmp_op, target) {
          var critical = nil, fumble = nil, totalValueBonus = nil;

          
          if ($eqeq(target, "?")) {
            return $$('Result').$nothing()
          };
          if (!$truthy([">=", ">"]['$include?'](cmp_op))) {
            return nil
          };
          critical = false;
          fumble = false;
          if ($eqeq(dice_total, 12)) {
            critical = true
          } else if ($eqeq(dice_total, 2)) {
            fumble = true
          };
          totalValueBonus = ($eqeq(cmp_op, ">=") ? (1) : (0));
          if ($truthy($rb_gt($rb_plus(total, totalValueBonus), target))) {
            if ($truthy(critical)) {
              return $$('Result').$critical("自動成功(劇的成功)")
            } else if ($truthy(fumble)) {
              return $$('Result').$failure("自動失敗")
            } else {
              return $$('Result').$success("成功")
            }
          } else if ($truthy(critical)) {
            return $$('Result').$success("自動成功")
          } else if ($truthy(fumble)) {
            return $$('Result').$fumble("自動失敗(致命的失敗)")
          } else {
            return $$('Result').$failure("失敗")
          };
        });
      })($nesting[0], $$('Base'), $nesting)
    })($nesting[0], $nesting)
  })($nesting[0], $nesting)
});
